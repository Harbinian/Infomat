# 3001 单流程编制功能需求

## 1. 业务目标

3001 为业务人员提供无状态的流程编制页面。用户可以直接新建流程，也可以导入现行或历史结构化 JSON 继续整理。用户主动导出的 JSON 是唯一保存方式。

流程是本功能的管理抓手：一份 JSON 只对应一个流程；流程向下管理业务行为、判断节点、现行执行岗位、已有工作角色、输入输出和跨部门关系，向上为未来生成并关联正式制度提供依据。

## 2. 使用角色

- 编制人：在3001中整理流程并导出未审核 JSON。
- Codex：在线下对 JSON 做预审，输出独立问题清单，不在 JSON 中写入审核意见。
- 归口部门的部门MDM审核员：在MDM平台中核对预览摘要和审核依据，确认后执行受控导入。
- 外部门联系人和外部门审核员：在MDM平台承接待办中分别补充本部门实际内容、记录本部门决定。

编制人通过公司局域网直接访问3001。3001不登录、不识别账号，也不产生审核任务。

## 3. 本期范围

### 3.1 新建和导入

- 页面提供“新建流程”入口，打开空白单流程编辑页。
- 新建时不自动填写流程名称、制度编号、业务行为或判断节点。
- 页面只提供结构化 JSON 导入，不再提供制度、表单、操作说明或其他参考材料上传入口。
- 历史 `document-structured-output-v2` 文件包含多个流程时，3001拆成多个候选，用户逐一编辑并分别导出。
- `process-governance-v1`可以继续导入；一个`process-governance-v2`JSON只能包含一个流程，3001导出统一使用v2。

### 3.2 分区式文字编制

- 顶部保留“新建流程、导入结构化文件、导出当前流程、清空当前页面”。
- “文字编制”内部设置“基本信息、目的与范围、术语定义、流程步骤、表单与记录、导出检查”六个分区，一次只显示一个工作区。
- “流程步骤”内部设置“业务行为、流程关系、输出物与数据、跨部门承接”四个子分区，不提供“部门内调用”填写入口。
- “流程关系”顶部常驻显示“顺序、判断分支和流程内部回路怎么选？”，不折叠，也不在每张关系卡片中重复。没有流程关系或存在多条关系时均只显示一次。
- 常驻说明使用并列卡片：顺序表示当前节点完成后默认进入下一业务行为，判断节点可以用一条无条件顺序关系表示默认继续路径；判断分支按某个明确结果进入后续步骤；流程内部回路退回本流程前面已经存在的步骤重新处理。桌面端三类并列显示，移动端上下排列。
- 常驻说明必须强调：承接顺序或判断分支的后续节点仍可以是普通业务行为；只有该节点本身还需要继续判断时，才选择判断节点。并固定显示“审核通过后顺序进入下一业务行为、审核不通过时回路退回”的示例。
- 每张关系卡片的“关系类型”字段下方提示：“默认继续下一步选择‘顺序’；按明确判断结果进入后续步骤选择‘判断分支’；需要退回前面步骤重新处理，选择‘流程内部回路’。”
- 业务行为、流程关系、输出物与数据、跨部门承接四个分区统一使用条目侧栏。桌面端在左侧选择和上移／下移条目，右侧一次只编辑当前条目；移动端侧栏位于编辑区上方并允许横向滚动。
- 调整顺序只交换`behaviors[]`、`flow_relations[]`、`data_objects[]`或`cross_department_handoffs[]`中对应完整对象的位置，不重新生成技术标识，也不改写任何对象内容或跨数组引用。流程关系的数组顺序不代表流程先后关系，流程图仍只依据明确填写的起点、终点和关系类型生成。
- “导出检查”必须把业务内容问题标明为“提示项（不阻止导出）”，把JSON结构或技术引用损坏标明为“阻断项”。系统生成的每条提示只定义一个确定缺陷，不得使用“或”合并问题。每个提示项可以点击，页面按问题对象切换到相应编制分区；字段问题直接聚焦存在问题的输入控件并显示红色呼吸高亮，没有具体字段的结构性问题才高亮卡片或分区。
- 新建流程默认进入“基本信息”。候选切换、未导出内容保护和导出失败保留当前页面继续沿用。

### 3.2.1 结构化学习评分

- “导出检查”顶部自动显示“结构化学习评分 v1（试行）”，包括结构完整性、展示分、A—D等级、有效行为链长度、行为链系数和六个维度得分。
- 结构完整性满分100分：技术结构15分、基础信息10分、业务行为25分、行为关系20分、数据与承接20分、表单结构10分。各维度先保留1位小数，再相加。
- 技术结构由序列化解析3分、当前版本兼容3分、结构规则及技术引用4分、导出回读3分、内容与引用保持2分组成。当前文件发生技术阻断时，展示分最高为59分。
- 展示分为“结构完整性×行为链系数”。有效行为链长度为0—1、2、3、4、5个及以上时，系数分别为0.80、0.85、0.90、0.95和1.00。显式回路边不重复计数，孤立行为不增加最长有效链。
- 展示等级按展示分确定：A不低于90，B不低于75，C不低于60，D低于60。
- 判断节点至少有两条完整出口。出口可以是判断分支、默认顺序、流程内部回路或完整的跨部门承接。一条无条件顺序关系可以作为默认继续路径；判断分支和回路必须填写条件；承接出口的目标节点可以是普通业务行为。
- 本期不判断分支条件是否互斥、流程设计是否合理、部门职责是否正确或业务事实是否真实。评分结果不是正式PMO评审结论。
- 必填项中的“待填写、待补充、待确认、未知、暂无、无、不适用、N/A”按未完成处理；选填项可以留空或使用占位说明。
- 无数据对象时，数据对象子项为0/15；无表单时，表单结构维度为0/10；无跨部门承接时，承接子项按不适用计5/5。页面明确提醒用户不得为提高分数虚构对象。
- 每条已登记承接关系按五项各1分检查：方向、本流程有效锚点、外部门或“待在MDM平台明确责任部门”、传递数据或承接事项、触发条件或完成标准。外部门流程与行为、外部门是否确认、外部门完成标准、返回路径、证明材料以及前置输入实际属于哪条外部流程均不扣当前编制人的结构分。
- `behavior_description`、输入说明和办理时限等字段本期不计分，只作后续集中评审预告。
- 计分扣项按维度显示，并复用导出检查的点击定位能力。评分只存在当前页面，不写入JSON、不改变未导出状态、不使用浏览器持久化。
- 页面复用现有结构版本和校验接口执行技术评分。校验服务不可用或前后端版本不一致时，页面显示“暂不能生成最终分数”并允许重新评分，不把系统故障算作用户扣分。

### 3.3 只读跨职能流程图预览

- 主工作区设置“文字编制”和“跨职能流程图预览”两个互斥标签。新建空白流程默认打开文字编制；导入包含已命名业务行为的文件后，自动打开预览。
- 文字编制页持续说明预览条件：填写至少一个业务行为名称后可以查看节点；只有填写明确的流程关系后才显示箭头。系统不得按行为录入顺序或名称推测关系。
- 预览采用BPMN 2.0.2最小图形子集，不生成BPMN XML，也不向业务用户展示完整BPMN术语。画布上方常驻七类业务图例和阅读提示，并提供画布外的三步示例。阅读提示必须补充：“带条件的前进箭头表示判断分支；标有‘回路’的返回箭头表示退回前序步骤。”
- 每个执行部门形成一条横向泳道。归口部门在已参与执行时排在最上方，其他参与部门按组织目录顺序排列；执行岗位显示在节点第二行。无法识别部门的历史执行值进入“执行部门待明确”泳道，原值不得改写。
- 业务行为使用圆角矩形，判断使用带“×”菱形，并行拆分和并行汇合使用带“＋”菱形，节点类型为空时使用虚线矩形。会签只显示“会签×N”标记，完整会签部门继续在文字编制中查看。
- 本流程关系使用实线实心箭头。判断条件写在对应箭头上，回路使用带“回路”文字的实线回返箭头；回路和返回关系不参与主线前后层级计算。
- 判断节点表达条件判断，流程内部回路表达在明确触发条件下返回本流程前序行为。回路是关系，不要求另建一个位于判断节点之后的节点。判断节点的明确出口可以由本流程顺序、判断分支、流程内部回路或跨部门承接构成；一条顺序关系可以作为不填条件的默认继续路径。承接这些关系的目标节点可以是普通业务行为，不因承接判断结果而自动成为判断节点。
- 节点名称、岗位和关系条件必须完整显示，不得截断。预览可以按中文字符增加显示用换行，并按实际行数调整节点高度；判断和并行菱形必须同时扩大宽度和高度，使名称和岗位留在图形内部。
- 相邻层级为两个节点的半宽、最长220图形单位的关系标签和48图形单位安全间距预留空间，中心间距不得小于440图形单位。单一相邻顺序关系继续使用直角连线；多个判断分支和跨层关系使用上方独立轨道；显式“流程内部回路”使用下方独立轨道。同一目标的多条回路按稳定关系顺序外扩，线路、标签和点击区域不得重合。
- 对非回路关系构建的有向图如果形成闭环，参与闭环的判断分支、顺序或并行关系进入可定位的“关系类型请核对”清单。页面说明“该关系与其他非回路关系形成闭环；如果这是退回前序行为，请选择‘流程内部回路’。”该提示不阻止导出，不自动改关系类型，也不改变图中关系语义。
- 跨部门承接显示在泳道区域外，使用虚线空心箭头连接。前置输入按“外部门承接卡片 → 本流程行为”绘制；后续承接按“本流程行为 → 外部门承接卡片”绘制，有返回要求时再连接本流程恢复行为。承接卡缺失内容显示“待明确”，不得补造业务事实。
- 导入JSON中已有的内部流程调用在调用部门泳道内显示为粗边框节点。3001不提供新增或编辑入口；用户点击该节点时，页面说明由MDM平台正式功能维护。
- 主图不显示数据对象、表单、完整会签部门、流程阶段或推测的开始和结束事件。用户点击本流程节点、关系或承接卡时，页面返回文字编制并定位对应卡片。
- 关系类型、起点、终点或跨部门发送行为缺失或引用无效时，流程图不得绘制错误箭头，并显示可定位的未绘制提示。
- 用户可以平移、缩放、适应画布、重置视图和展开查看，但不能拖动节点、增删图形或连接箭头。完整画布拟合比例不低于0.60时默认显示全图；低于0.60时默认聚焦泳道表头和前两层节点，并显示“当前已聚焦流程开头，点击‘适应画布’查看完整流程”。“适应画布”始终显示全部内容，“重置视图”恢复自适应默认视野。桌面端画布高度不超过约520像素，移动端约420像素。
- 图例示例状态、展开状态和计算坐标只存在当前页面运行内存，不写入JSON。图形加载失败时，页面必须保留文字内容、导入和导出能力。

### 3.4 无状态边界

- 服务默认监听`0.0.0.0:3001`，公司局域网用户通过服务器局域网地址直接访问。
- 不写数据库。
- 不使用 `localStorage`、`sessionStorage`、IndexedDB、Cookie或服务端草稿恢复。
- 导入的结构化文件只在当前浏览器内存中处理，处理完成后不写服务端文件。
- 刷新或关闭页面后内容清空。
- 新建、导入另一文件或清空当前页面前，向用户提供“取消、先导出、放弃当前内容”。
- 3001不得通过API、数据库、队列、回调、共享会话或状态轮询与MDM平台通信。
- 3001不得调用DeepSeek，也不得把MDM-AI助手或认证网关作为启动、访问或导出的前置条件。

### 3.5 流程内容

归口部门在MDM平台确认审核导入前，流程至少应补齐：

- 发起部门、编制人、流程名称、归口部门。
- 流程目的和适用范围。
- 至少一个业务行为。
- 每个业务行为明确执行主体，填写完成标准和输出结果；触发条件和前置条件至少填写一项。

能力域和业务能力属于可选归类结果，缺失时保持“未归类”，不阻止3001导出或MDM平台受控预览。

制度名称、制度编号和制度关联不是新流程审核或导入的前置条件。正式制度在流程生效后另行生成和关联。

### 3.6 流程、执行岗位和工作角色

- 新增步骤时，步骤类型默认留空，由用户主动选择业务行为、判断节点、并行分支或并行汇合。
- “业务行为或节点名称”是流程节点的主内容，用于流程图、流程关系和工作角色绑定。编制人可以在“具体做什么（补充说明，可选）”中解释实际操作，帮助PMO和部门业务人员沟通。
- `behavior_name`保存业务行为或节点名称，`behavior_description`保存“具体做什么”。补充说明不替代名称，不显示在流程图节点上，留空时不阻止草稿导出或PMO审核。
- 导入缺少`behavior_description`的历史文件时，3001在当前页面内存中补为空字符串。3001不得把业务行为名称复制成补充说明，也不得推测实际操作。
- 判断节点至少有两条明确出口。判断分支和回路的条件应互斥并覆盖相应结果；一条顺序关系可以作为默认继续路径，不要求填写条件。3001只提示，不阻止草稿导出。
- 允许有明确退出条件的流程内部回路；跨部门承接链禁止形成回路。
- 3001允许记录并行分支和汇合，但不验证完整性。
- 业务行为使用“执行部门和执行岗位”选择器。岗位目录由`/api/enums`从仓库花名册只读提供，不调用MDM平台。
- 执行部门可以选择组织目录中的任一部门。所选执行部门与流程归口部门相同时，页面说明为本部门执行；二者不同时，页面说明为跨部门执行。页面不新增执行范围字段，仍通过`current_actor_role`中的部门名称判断。
- 执行岗位只显示所选执行部门在花名册中的岗位，不得混入其他部门岗位。花名册中存在名称与部门看似不一致但确实归入该部门的岗位时，3001仍按花名册原值显示，不擅自调整部门或岗位名称。
- 新增行为不默认选择部门或岗位。具体岗位以“部门名称 + 岗位名称”保存到`current_actor_role`；“全公司通用”保存为`全公司`。
- 导入值未被当前花名册收录时，3001保留原值并醒目标注；花名册岗位加载失败时，也不得清除已有值。
- 工作角色与岗位严格区分。岗位容纳工作角色，一个工作角色可以由多个岗位容纳；3001不得根据岗位生成或推测正式工作角色。
- 普通编制页面不新增或编辑正式工作角色，新业务行为的`work_role`为`null`。导入文件中已有的合法`work_role`隐式保留并随再次导出带回。
- 已有工作角色仍必须绑定具体业务行为，并保持“XX行为的发起／办理／审核／批准／判断／发送／接收／会签角色”的全称，不得退化为“申请人、审核人、批准人”等孤立名称。

本期不增加“业务流程／行政流程”分类字段，统一按不存在行政流程处理。

### 3.7 跨部门承接、内部调用预览和会签

- “承接”只指跨部门业务行为。同一部门内流程之间称为“内部流程调用”。
- 业务行为卡片分别提供“登记本行为开始前需要外部门提供的输入”和“登记本行为完成后需要外部门承接的事项”。3001在`cross_department_handoffs[]`中创建同一条承接记录，分别写入`handoff_direction=inbound_prerequisite`或`outbound_followup`，并把当前业务行为写入`anchor_behavior_ref`。
- “跨部门承接”子分区引用并编辑同一条承接记录，不复制业务字段。页面允许补充来源部门、目标部门、传递数据或承接事项、触发条件、完成标准、外部门流程与行为、返回数据和本流程恢复行为。
- 一项业务行为可以创建多条跨部门承接。每个承接部门形成独立记录。
- 用户不能确定外部门时，必须选择“待在MDM平台明确责任部门”，由MDM平台形成`pending_assignment`待办；3001不得从文字、岗位或流程名称猜测责任部门。
- 业务行为卡片中的每条承接同时提供“完善承接信息”和“删除这条承接”。删除时，3001按`handoff_ref`定位当前承接，并在确认信息中写明承接部门、处理事项和随承接一并删除的内容。用户取消时不得修改任何数据；用户确认后只删除当前承接，业务行为和其他承接保持不变。
- 删除已经关联跨部门承接的业务行为前，3001必须说明将同时删除多少条承接记录，并由用户确认。
- 跨部门承接必须关联本流程有效业务行为，并说明传递数据或承接事项。外部门流程和行为可暂缺，3001记录治理提示但不阻止导出。
- 接收行为是后续承接行为的前置条件。
- 一项发送行为可以向多个部门发送同一输出，但每个部门形成独立承接关系。
- 主流程分别指定发给承接部门的输入数据、完成标准、承接完成后返回的数据和恢复执行位置；承接部门不得修改恢复位置。
- 恢复位置只保存在主流程调用关系中，不写入承接流程。
- 部门内调用属于MDM平台正式功能的预览。新建流程保持`internal_process_calls: []`；3001不新增、不编辑也不校验业务完整性。导入文件中已有的`internal_process_calls[]`在当前页面内存中隐式保留，流程图可以预览，再次导出时不得清空或改写。
- 会签表示知悉，不表示审核、批准或承接。3001记录需要确认知悉的部门；全部会签对象确认知悉后，会签行为才完成。
- 会签未完成时流程不得结束，即使批准行为已经完成。

### 3.8 数据和表单

- 用户填写的输出物登记为待治理数据对象，生成隐藏技术标识。
- 3001只记录数据对象名称、产生行为和使用行为，不判断主数据、黄金源、数据责任或质量规则。
- “表单与记录”作为独立编制页面。用户先选择表单或记录，再选择主表或某张明细表结构，最后编辑当前结构下的填写项。
- 用户文案使用“表单或记录名称”“表单或记录编号（如有）”“主表结构／主表标题”“明细表结构／明细表标题”和“填写项”。
- 新建表单只建立一个空主表框架，不自动生成名称、编号、标题或填写项。
- 每张表单正常情况下只有一个主表，可以增加多张明细表；表单与业务行为保持可选关联。
- “填写项类型”使用标准类型下拉选择，不提供自由填写入口。标准类型为文本、长文本、数字、日期、日期时间、金额、枚举、布尔、部门、人员、文件编号、签名、图片、附件和二维码。
- 导入旧JSON中的非标准填写项类型时，3001保留原值并标注“当前类型字典未收录”，不得静默清空或猜测替换；用户主动修改后只能选择标准类型。类型字典读取失败时，选择器不可修改已有值，其他编制和导出功能继续可用。
- 不新增JSON字段：`area_type: "基本信息"`表示主表，`area_type: "明细清单"`表示明细表，`areas[].items[]`保存填写项。
- 导入缺少主表、包含多个主表或存在未分类结构时，3001保留全部内容并提示用户确认，不静默改写，也不以此阻止草稿导出。

### 3.9 编制参考材料

- 页面暂停提供编制参考材料入口、拖放区、编辑区和缺失提示。
- 新建流程的`reference_materials`为空数组。
- 导入的旧JSON如果已有参考材料，3001在当前页面内隐式保留，再导出时原样带回；页面不展示、不编辑，也不自动清空。
- 导入的现行或历史JSON如果已有`internal_process_calls[]`，3001只在流程图中预览，并在再次导出时原样带回；页面不提供填写入口。
- 服务端确定性解析能力保留用于历史文件迁移和回归测试，不作为业务用户页面功能。

### 3.10 导出和审核边界

- 3001允许导出不完整草稿。
- 导出文件名统一为 `未审核-发起部门-流程名称-导出时间.json`。
- JSON不包含审核状态、审核意见或批准标记。
- Codex预审材料和PMO审核材料与流程JSON严格分开。
- 3001硬校验只保证JSON可读和单文件引用不损坏。唯一入口、结束条件、循环、会签、并行汇合和跨流程关系只提示或原样导出。
- 判断节点出口检查同时统计从该节点发出的有效本流程顺序、判断分支、流程内部回路和完整的后续承接。顺序出口可以作为默认继续路径，不要求填写条件，但必须填写目标行为；判断分支和回路必须填写条件及目标行为；后续承接至少满足本节五项结构信息。前置输入不是判断出口。显式流程内部回路不参与主线入口和出口数量计算。
- 3001导出仍标记为“未审核”。只有归口部门MDM审核员登录MDM平台完成审核，且服务端复核和原子写入成功后，内容才能进入治理数据。
- “导出检查”在评分和提示项之后、导出按钮之前显示两张静态卡片。第一张卡说明业务评审完成后的MDM平台受控导入和正式编辑路径；第二张卡将系统动力学评价明确标记为“后续规划”。
- 系统动力学评价预告只说明未来将结合正式流程、运行数据和实际反馈观察长期运行影响。预告不得给出分数、上线时间或固定评价模型，也不得替代业务评审、审核和管理决策。
- 两张卡片不提供按钮、链接、上传或状态写入，不调用MDM平台接口，也不写入导出JSON。

## 4. 明确不做

- 不在3001保存草稿、账号或审核状态。
- 不在3001使用DeepSeek辅助抽取或填报。
- 不在页面提供编制参考材料上传或编辑。
- 不在3001新增或编辑部门内调用；该能力由MDM平台正式功能维护。
- 不在普通编制页面新增或编辑正式工作角色。
- 不要求局域网用户通过MDM-AI助手或认证网关访问3001。
- 不在3001生成制度、维护制度审批或关联正式制度。
- 不在3001实施完整流程引擎、跨流程完整性校验或数据治理。
- 不修改MDM平台现有代码、RBAC或数据库。

## 5. 页面顶部信息

页面顶部只显示工具名称、用途说明和操作入口。页面不显示DeepSeek停用说明，也不显示3001无状态、MDM平台通信或线下审核边界的通用声明。上述边界继续作为产品和技术规则执行。

## 6. 验收标准

1. 用户无需先上传文件即可创建空白流程。
2. 页面刷新后无法恢复内容，浏览器存储中不存在草稿。
3. 新建、导入其他文件或清空前出现三选一保护。
4. 新增步骤的类型为空，不默认认定业务行为或判断节点。
5. 3001导出符合`process-governance-v2`，根对象只有一个`process`；v1导入后导出统一升级为v2。
6. 旧版多流程JSON可拆分为多个候选，候选逐一导出后内容不串流程。
7. 导出文件名包含“未审核”、发起部门、流程名称和导出时间。
8. 页面顶部不显示DeepSeek停用说明或系统边界通用声明；页面及服务端不包含DeepSeek调用入口或健康状态。
9. 新增业务行为的`work_role`为空；导入已有合法工作角色后可无损再次导出。
10. 新建流程默认显示文字编制；导入包含业务行为的文件后默认显示只读流程图。
11. 没有明确流程关系时，流程图只显示孤立节点，不按录入顺序生成箭头。
12. 图中可以识别跨部门承接和历史内部流程调用；点击跨部门承接后定位到对应文字卡片，点击内部流程调用后只提示在MDM平台正式功能中维护。
13. 图形操作不修改流程JSON，导出结果不包含坐标、标签页状态或图形配置。
14. 自动测试和真实页面主路径通过。
15. 页面和服务端结构版本不一致时，页面在新建或导出前明确提示保持当前页面打开并重启3001服务，不得显示JSON解析错误、旧版字段缺失或误报技术引用损坏。
16. 不设置`STRUCTURED_OUTPUT_HOST`时，3001监听`0.0.0.0:3001`；局域网用户可通过服务器局域网地址完成新建、编辑和导出，全程不依赖DeepSeek、MDM-AI助手或认证网关。
17. 新建流程没有参考材料入口，导出结果包含空数组；导入含历史参考材料的文件后再次导出，历史内容不丢失。
18. 具体岗位、“全公司通用”、未收录旧岗位、花名册加载失败和空岗位五种情况均不会误填或清空已有数据。
19. 新建表单形成一个空主表，可以分别维护多张明细表和各自填写项；导出后重新导入，主明细归属、业务行为关联和技术引用保持一致。
20. 连续填写表单名称、编号等相邻字段时，不因分区或侧栏标签刷新丢失刚输入的值。
21. 用户可以在业务行为卡片中创建一条或多条跨部门承接，并在“跨部门承接”分区继续编辑同一条记录；用户可以取消或确认删除其中一条承接，取消后全部数据保持不变，确认后业务行为和其他承接保持不变；删除发送行为前会显示关联承接数量并要求确认。
22. 新建流程不显示部门内调用填写入口；导入含`internal_process_calls[]`的历史JSON后，流程图可以只读预览，再次导出和重新导入时历史调用保持不变。
23. 执行部门下拉可以选择全部组织部门；选择归口部门以外的部门时，岗位下拉只显示该部门的花名册岗位，并明确提示当前属于跨部门执行。现行花名册把岗位归入哪个部门，3001就按该归属显示。
24. 桌面端和移动端始终显示七类图例及阅读提示；展开或收起三步示例不会修改当前流程JSON。
25. 归口部门和其他参与部门按规定形成横向泳道，岗位显示在节点第二行；未识别的历史执行值进入“执行部门待明确”泳道并保持原值。
26. 判断条件、并行关系、回路、会签标记、跨部门承接和历史内部流程调用按本节图形规则显示；缺少有效端点或发送行为时不绘制错误箭头。
27. 由约40个行为、6个部门、判断、并行、回路和承接组成的代表性流程在桌面端和移动端没有节点重叠、泳道错位或关键标签遮挡。
28. 连续中文长名称和长条件完整显示；节点标签位于节点边界内，关系标签不超过220图形单位，相邻层级中心间距不小于440图形单位。
29. “编制→审核→批准＋两条退回”场景中的判断分支和两条回路分别使用可辨认、可点击的线路轨道；两条同条件回路的标签不重合。
30. 非回路闭环生成可定位但不阻断导出的“关系类型请核对”清单，模型生成前后输入JSON完全一致，页面不自动修订任何关系类型。
31. 普通流程默认显示全图；完整拟合比例低于0.60的大流程默认聚焦开头并显示提示。“适应画布”和“重置视图”分别恢复完整视野和自适应默认视野。
32. v2图形方向只读取承接事实，不把坐标、页面状态或推测内容写入JSON。导出后重新导入时，除合法的`exported_at`变化外，业务字段和技术引用保持一致。
33. 用户可以分别填写业务行为或节点名称和“具体做什么”补充说明；导出后重新导入，两项内容保持独立且不丢失。
34. 缺少`behavior_description`的历史`process-governance-v1`和`document-structured-output-v2`文件可以继续导入。3001只补空值，不复制业务行为名称，不阻止导出。
35. 用户可以在业务行为侧栏上移或下移任一非边界项；移动后当前行为仍保持选中，所有稳定引用和值保持不变，导出再导入后按新顺序显示。
36. 导出检查中的每条业务提示只描述一个确定缺陷，系统文案不使用“或”合并不同问题。点击字段问题后，页面切换到正确分区，聚焦相应`input`、`select`或`textarea`并显示红色呼吸高亮；触发条件和前置条件同时为空时，同一提示项高亮两个字段。没有具体字段的结构性问题定位到对应卡片或分区。提示项不阻止导出，JSON结构或技术引用损坏仍阻止导出。
37. 判断节点使用“回路＋顺序”或“回路＋跨部门承接”表达两个明确出口时，不再误报出口少于两条；“回路＋无条件顺序”中的顺序关系作为默认继续路径，不再误报缺少条件。顺序出口缺少目标、判断分支或回路缺少条件或目标、跨部门承接缺少触发条件或承接部门时，仍显示可定位提示。
38. 没有流程关系和存在多条流程关系时，顺序、判断分支与流程内部回路说明均只显示一次，不遮挡新增、编辑或删除操作。
39. 桌面端三类说明并列显示，移动端上下排列；文案完整，无横向溢出。流程图阅读提示与实际顺序、判断分支和回路图形一致。
40. 说明文案和页面切换不修改当前流程；导出后重新导入，除合法的`exported_at`变化外，业务字段和技术引用保持一致。
41. 新增或主动修改填写项时，“填写项类型”只能从15项标准类型中选择，页面不提供自由文本输入。
42. 导入含非标准填写项类型的历史JSON后，原值和其他内容保持不变，页面显示未收录提示；改选标准类型后提示消失，导出再导入时所选标准值保持不变。
43. 填写项类型字典读取失败时，页面禁用类型选择器并保留已有值；表单其他字段、当前页面数据和导出功能不得因此丢失或出现未处理异常。
44. 用户可以在四个流程步骤分区的条目侧栏中选择条目，并上移或下移任一非边界项；移动后右侧继续编辑被移动的条目。首项不能继续上移，末项不能继续下移。
45. 顺序调整后，各对象的技术标识、业务字段和跨数组引用保持不变。流程图不得因流程关系数组顺序变化而推测或改变流程先后关系。
46. 调整四类流程步骤条目顺序后导出并重新导入，页面按调整后的数组顺序显示；除合法的`exported_at`变化外，对象内容和技术引用保持一致。
47. 进入“导出检查”后，页面自动生成结构完整性、展示分、等级、行为链系数和六项维度得分；重新评分不修改流程内容或未导出状态。
48. 评分正确处理1—5个行为、孤立节点、显式回路、非回路环和并行结构；技术阻断时展示分最高为59分。
49. “回路＋无条件顺序”和“回路＋完整跨部门承接”均可构成两个判断出口；顺序承接的普通业务行为不被要求改成判断节点。
50. 校验服务不可用或前后端版本不一致时，页面不产生最终分数、不扣用户分，并保留当前流程供用户重新评分或继续导出检查。
51. 用户可以分别登记前置输入和后续承接；流程图箭头方向正确，有返回要求时连接本流程恢复行为。
52. 责任部门未知时可以明确选择“待在MDM平台明确责任部门”；外部门流程、行为、确认、返回和证据缺口只形成治理提示，不阻止导出、不扣当前编制人的结构分。
53. v1承接关系按后续承接迁移，原文件不被修改；v2导出后可以重新导入，稳定引用和业务内容不丢失。
54. 重新评分前后JSON完全一致；没有跨部门承接时承接子项按不适用获得5分。
55. 两张后续提示卡只在“导出检查”显示，桌面端并排、移动端上下排列；卡片中不包含按钮或链接。
56. 3001全部用户可见文案使用“MDM平台”，不使用端口编号指代该平台。
57. 后续提示卡不增加API、结构规则、JSON字段、浏览器持久化或MDM平台通信。
