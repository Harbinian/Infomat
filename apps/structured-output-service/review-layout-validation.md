# 3001 业务核对布局：验证与发布记录

## 2026-09-08 GLTX-JY-27-A 导入检查后的前端修复

用户在完成原文件检查后授权修复。已处理4项已确认缺陷：外侧关系按规划折点绘制，回路线与条件标签不再错位；待确认操作不再作为创建行为推导；清晰检查定位选中节点或起点，并在画布不足时缩小至完整可见；字段使用方式、取值方式和对象字段引用提示直接打开字段关系控件。相关边界测试另修复公共退回连接点净空不足及原生自环端点无效问题。业务待定值、来源制度职责冲突和桌面原文件保持原状。

先在隔离工作树和127.0.0.1:33001验证，再同步到原192.168.19.199:3001。原PID24904未重启；4项静态文件的HTTP响应均为200，字节摘要与候选完全相同。health仍记录服务启动时的提交295faf0e3c92d954f55616354013c702096f46b1，不把它表述为本次静态修复提交。本次修复以e3547caed14ed283b3bf68559c57a00be4bd5b50为工作区基线，静态文件同步时尚未提交。

- 应用18个测试脚本全部通过；根test:codex-context及git diff --check通过。新增v4至v7待确认操作、明确创建和多创建冲突回归，并保留既有v1至v7兼容及编辑安全检查。
- 真实Microsoft Edge在1699×828和1280×720 CSS像素、浏览器100%缩放下，候选和原3001均通过绘图及定位回归：9条外侧关系无无关节点穿越，实际标签中心与规划一致且互不重叠，所有关系端点有效；全图适配、起点及选中节点清晰定位、字段使用方式和取值方式聚焦通过。节点适配比例由画布决定，不把它混同浏览器缩放。
- 对象字段引用定位通过真实控件临时清除引用后触发，检查后恢复原引用；新增字段来源的未应用输入在取消导航后保留，明确放弃后未进入JSON。
- 原实例业务提示由163项变为160项，减少的是3条未来数据误报。70个字段的140项待确认提示及其他业务缺项继续保留；没有将建议写入业务事实。
- 原文件导入后的当前内容经实际下载、重新导入保持一致，仅导出时间更新。重复规范化未新增差异，损坏JSON导入失败后当前草稿保持。桌面源JSON的SHA-256仍为d6df9300e7f098bab1f2b0e048522c011467e8cb3aca25c4cddc257e221230af。
- 浏览器控制台warning/error及页面异常为0；请求仅涉及候选和原实例，无3000或外部业务请求；Cookie、localStorage、sessionStorage、IndexedDB均无业务内容。
- npm audit --json已执行，但审计服务以ECONNRESET中断，本轮依赖安全审计未完成。未新增或升级依赖。自动化阶段未覆盖人工Windows中文输入法和真实Excel/WPS剪贴板操作；用户随后确认已完成人工验证，确认记录见下文。

2026-09-08，用户在本次对话中明确确认“我已完成人工验证，提交推送吧”。本次3001前端修复的人工验证已由用户确认完成，并获得提交、推送授权。确认依据为该用户声明，不补写未提供的逐项用例或操作结果；JSON中的业务待定事实和来源制度责任冲突继续保留，按原业务确认边界处理。

README、PRD、Tech-Spec和操作引导已同步；未改变Schema、摘要、字段、枚举、API、导入导出格式或业务术语，因此无需更新结构契约和术语表。本次修复已实现并在原3001开启，未构成业务验收。浏览器回归模块为scripts/process-diagram-rendering-browser-scenario.js；备份、逐文件摘要、测试日志、截图及运行证据位于artifacts/structured-output-service/fix-gltx-jy27-20260908/。

## 2026-09-08 生命周期状态冲突、导入与下载修复

用户两张截图均指向`/data_objects/0/lifecycle/entry_state/identifiability`的`const`错误及其父状态的`if/then`错误。已用固定v7夹具复现完全相同的两项严格校验结果：选择不会匿名处理后，仅`identifiability_applicability`被更新，`identifiability`残留待确认；原选择框随后隐藏，用户无法在原处改回。全量校验把该残留错误扩散到导入、批量字段引用、表格应用和下载。另发现未先选对象时，生命周期空页面提示选择却没有选择框。

修复包括：匿名处理选择成对更新且保持可编辑；不自动修正导入或重绘中的原值，提供显式确认修正；允许该类已知错误保留原值进入页面；无关编辑可以保留未变化的既有生命周期错误，新增或其他错误仍阻止应用。未选对象时提供选择框。所有草稿下载直接序列化当前快照，取消结构校验、迁移回读、业务提示确认和服务状态的下载门禁；未应用输入保护、实际文件摘要及正确下载基线保留，下载过程中出现的新修改不会被旧快照覆盖。

候选Edge浏览器的16项检查通过：异常v7原值导入、提示、关闭校验网络后的实际下载、异常文件重新导入、批量引用字段、保留不会选项、显式修正、改回会时保留待确认、重复选择一致性、完整v7严格校验、生命周期后修改数据、数据后修改生命周期、下载前保留未应用输入、显式应用后真实下载及重新导入、空存储、无页面错误。对象说明、生命周期、追加的表单引用在往返中均保留。未取得用户截图对应的完整原JSON；本轮验证使用相同错误的可复现夹具，不能表述为该原文件已经逐字段验收。

新增单元回归覆盖原值保持、重复迁移、受限错误识别、无关错误拒绝、匿名处理选择组合、离线下载、不覆盖下载期间的新值、失败不更新下载基线及未应用输入保护。应用完整`npm.cmd test`、根`test:codex-context`和`git diff --check`通过。补充真实网页表格验证：存在原生命周期错误时修改数据说明并应用成功，生命周期原值保持。v7 Schema与严格API结果保持不变，没有新依赖、数据库或浏览器持久化。README、PRD、Tech-Spec、帮助说明及项目运行规则同步；无新术语或定义变化，未改术语表。技术和模拟操作结果不替代真实业务验收或Windows输入法人工验证。测试脚本、样本和下载文件位于被忽略的`artifacts/lifecycle-fix/`。

11:03同步到正式3001，共11个变更文件，含项目运行规则的一条下载规则。发布前逐文件核对基线并备份；首页和相关静态资源均HTTP 200，字节与候选相同。PID24904未变化，健康状态ok、v7 released、Schema摘要及本地Cytoscape资源正常。正式Edge浏览器重复上述16项完整场景通过；生命周期修正入口在1920×1080、1920×900、1699×828、1536×864、1280×720均无页面或工作区横向溢出，查看及缩放不改变JSON。正式截图已查看。正式备份、文件摘要及证据位于`artifacts/lifecycle-fix-20260908/`。用户原报错标签页的未下载输入未被读取、刷新或清空，其恢复需求仍待用户确认。

## 2026-09-08 编制页面下一步引导

在已有8步帮助说明基础上，编制页面顶部增加带目标名称的“下一步”、上一步、当前操作说明和“这一步怎么做”章节链接。用户打开流程后主动进入引导，保留原默认页面；依次核对范围、人员动作、路线退回、表单字段、对象字段与引用、数据使用关系，最后检查下载。自由切换其他任务时显示明确的返回目标。导航位置只存在候选级页面内存中，不改变业务数据、完成状态或下载状态。

候选Edge浏览器使用此前经用户授权选取的财务部固定资产编码与标签管理JSON副本。8步×5种CSS可视区（1920×1080、1920×900、1699×828、1536×864、1280×720）共40个状态均无横向溢出，主按钮位于可视区内；连续前后步切换保持JSON全部属性不变。三选项分别验证：继续编辑保留输入及步骤；明确放弃后继续且JSON不变；明确应用成功后才推进。人为构造同字段来源冲突时应用失败，步骤与中文输入均保留。章节帮助打开正确步骤且opener为空，原未应用输入不变；重新导入重置引导位置，自由导航可返回暂停位置。

最后一步经真实下载事件取得文件，除`export_meta.exported_at`外与源导入内容逐属性一致；重新导入下载文件后全部事实及引用保持。浏览器本地存储、会话存储、IndexedDB和Cookie为空，没有3000或第三方请求，没有页面及控制台错误。补充空白新建验证：7次键盘回车推进后JSON不变；故意把流程名称改为数字时，最终按钮被现有下载回读保护阻断，原草稿保留。更新后的帮助首段在1280×720无横向溢出。截图已实际查看。证据位于候选应用被忽略的`artifacts/next-step/`。

应用完整`npm.cmd test`通过；补充导航适配器验证候选隔离、非法目标、经典界面回退及不写业务事实。最后的按钮忙碌状态调整后，`test-review-workspace.js`和`test-compact-task-ui.js`再次通过，真实浏览器上述完整场景也通过。根`test:codex-context`和`git diff --check`通过。README、PRD、Tech-Spec及帮助页同步；未增加业务术语或改变定义，无需修改术语表、结构契约及迁移规则。原配色与内嵌样式保持不变，无新依赖。

已同步到正式3001，共9个文件；发布前逐文件核对基线并备份。首页、引导模块、布局样式和帮助HTML均HTTP 200且字节与候选相同；健康状态ok、v7 released、Schema摘要及本地Cytoscape资源正常，PID24904未变化，无需重启。正式Edge浏览器再次逐步进入7个核对位置，帮助定位到第8章，实际下载并重新导入后，除导出时间外全部内容与原导入数据一致。正式页面错误为空。正式备份、文件摘要和运行证据位于`artifacts/next-step-20260908/`，候选详细浏览器证据位于`artifacts/next-step/`。

上述结果属于技术验证和模拟操作，真实业务人员理解程度、真实Windows中文输入法候选窗仍未验收。没有将模型走查当作业务验收。

## 2026-09-08 操作说明补充

用户要求在帮助中增加逐步说明，解决现有页面之间的认知跨度。新增`public/operation-guide.html`及独立CSS、JS；帮助菜单和原操作帮助页提供新标签页入口。说明分8步，涵盖打开文件、范围、人员动作、路线条件、表单字段、对象字段引用、数据使用、检查下载。各步列出实际入口、具体操作、虚构费用申请示例及继续条件；“应用修改”和“下载保存”分别说明，不把阅读进度写成流程完成状态。

说明使用既有配色，采用目录、单步正文及上一步／下一步。原编制页面的布局、内嵌样式、业务模型和编辑逻辑不变。新标签使用`noopener noreferrer`，说明不读取原页面或业务文件，不调用业务API，不写入浏览器存储。README、PRD与Tech-Spec同步；未引入新的业务术语或修改定义，术语表及结构契约无需变更。

定向检查：`test-review-workspace.js`、`test-compact-task-ui.js`、`test-cognitive-load-ui.js`、`test-service-contract.js`、根`test:codex-context`、新增JS语法检查及`git diff --check`均通过。独立Edge浏览器验证8步×5档可视区，共40个状态，每次仅显示一步，无横向溢出；目录跳转和键盘前后步切换通过。原页面存在未应用中文输入时，打开、阅读并关闭说明页后，JSON、编辑会话、当前候选、环节、视图、详情滚动及输入值均不变，不触发应用或放弃；说明页无业务存储和控制台错误。截图已实际查看。

本机测试证据位于候选应用被忽略的`artifacts/operation-guide/`。这些结果证明入口、阅读交互及输入保护，不代表已经通过真实业务人员的理解程度测试。

09:45已同步到正式3001。`/operation-guide.html`、配套CSS、JS和包含帮助入口的首页均200，HTTP字节与候选文件相同；服务PID24904保持不变，健康状态正常且v7结构摘要不变。正式浏览器复验帮助菜单入口、原操作帮助页入口、独立说明页及键盘前后步骤通过；“跳到当前步骤”保留当前章节。备份及发布文件摘要位于正式应用被忽略的`artifacts/operation-guide-20260908/`。此次仅发布帮助静态文件和入口，未重启服务、改动业务数据或扩大到其他应用。

## 2026-09-08 发布前复核

用户已认可3011设计并明确授权推到3001，同时指定共享目录中的真实流程JSON进行验证。原约束继续有效：保留配色及现有视觉系统，只调整布局、信息层级和操作路径；使用情况清单不进入3001或3000；不改变业务结构、枚举、稳定引用、导入导出格式及持久化边界。此前“正式3001未授权切换”的表述仅适用于9月7日。

### 真实文件与新旧信息对照

样本来自用户指定目录的财务部文件：`未审核-财务部-固定资产编码与标签管理-20260812-085547-v7版本.json`，46291字节，SHA-256为`daaf4863aec91d8491fe993f4c3cb928e81eb24341092bffbc9607afbbe9e265`。只复制到候选工作树被忽略的测试目录，未修改共享源文件。目录名称中的95分不作为本次质量评定或业务验收证据。

同一真实文件分别导入切换前正式3001及候选3011，并从两版可见保存按钮取得实际下载文件，重新通过文件选择控件导入。结果如下：

| 核对项 | 实际结果 |
|---|---|
| 内容数量 | 两版均5个业务行为、4条流程关系、2个数据对象、2张表单、9个表单字段 |
| 导入差异 | 两版导入结果按每个属性比较完全一致；两版均报告0项规范化变化 |
| 原值保留 | 两版实际下载仅变更`export_meta.exported_at`；其余全部属性与导入结果相同 |
| 新旧下载对比 | 除各自导出时间外全部属性相同；数组顺序保持 |
| 回读 | 两份真实下载文件重新导入后全部属性不变 |
| 修改往返 | 只在本机副本修改一个环节说明；输入与取消切换不改变JSON，显式应用后仅该说明改变；实际下载和重新导入准确保留修改 |
| 源文件的空缺 | 两个数据对象原本均无对象字段定义，9个表单字段的`data_field_ref`原本均为`null`；新版保留并明确提示尚未引用，没有自动补造关联 |

这证明该真实样本没有因布局升级丢失信息，不代表所有共享文件的业务事实或自评得分已被核验。

### 旧入口对应关系

| 原有内容或操作 | 当前入口及复核 |
|---|---|
| 流程边界、编制信息、来源、规范化差异 | 流程信息；文件→查看文件信息；仍访问原字段和来源记录 |
| 动作、执行主体、触发、时限、完成标准、前提及会签 | 流程图→环节→人员与动作／条件与异常；复用原属性补丁编辑器 |
| 判断、退回、并行、关系端点及条件 | 流程图或环节清单→关系详情；原关系向导及校验保留 |
| 历史触发、输入输出、控制节点的原执行主体、动作、时限及完成标准 | 恢复为可展开的只读内容；虚构历史夹具在真实浏览器中核对可见值 |
| 旧跨部门诊断及归档 | 环节详情保留旧记录提示；检查与下载→跨部门事实核对继续使用原诊断与归档入口 |
| 数据／表单在环节中的使用方式 | 关联入口显示创建、更新、使用或表单处理方式；原关系管理入口保留 |
| 数据对象、生命周期、来源、表单区域、字段取值及所有关联 | 全流程数据与表单→对象→管理关联与字段；原逐项管理及九类表格仍可主动打开 |
| 未关联对象、共享字段下钻 | 全流程目录包含未关联项；虚构夹具验证表单→字段→对象定义的三级路径 |
| 节点与关系排序 | 环节清单保留原排序按钮；键盘排序验证仅改变指定数组顺序、技术引用不变；存在未应用输入时先显示三分支保护 |
| 七步指引、检查、阶段文件 | 帮助、检查与下载及折叠的阶段下载入口；原内部步骤映射保留 |

本轮补齐的入口遗漏包括历史说明、旧跨部门提示、关联操作方式、控制节点旧时限与前提／会签入口、清单排序。普通核对摘要保持分组显示，没有重新改配色、字体或组件外观。

### 本轮技术验证

- 独立Microsoft Edge 152无头浏览器，Windows平台，CSS内容可视区1699×828、缩放比例1。用户原应用内浏览器页保持原状。实际操作不是人工Windows输入法测试。
- 在1920×1080、1920×900、1699×828、1536×864、1280×720分别检查流程摘要、条件编辑、流程信息、全流程目录、检查与下载，共25个状态；页面、工作区和详情均无横向溢出。
- 浏览器存储运行时检查：两版样本页面的localStorage、sessionStorage、IndexedDB及业务Cookie均为空；请求均限各自实例，无3000或第三方请求；被测流程无控制台warning/error及未处理页面错误。
- 既有真实浏览器大样本：40行为、80关系、30对象、120数据操作、10表单、200对象字段及200表单字段。首次显示中位数92.3ms，模式切换83.4ms，选择82.9ms，图命令119.3ms，归并86ms，实际下载回读522.4ms；全部既定门槛通过，无超过1秒的长任务或绘制间隔。
- 性能脚本适配当前入口：数据任务先进入目录，再明确打开数据图；节点选择以已显示的摘要作为完成条件；旧界面仍以属性输入显示为准。测试开始激活独立测试页，避免下载页抢占活动标签导致浏览器后台限频。未放宽阈值。
- 本轮最终`npm.cmd test`的18个脚本全部通过，包含新默认开关及显式0回退、历史信息可读性和原排序契约。原HTML内嵌样式与Git基线一致，新增CSS不声明颜色、背景、字体、边框或阴影。锁文件及依赖未改。

本机证据位于候选应用被忽略的`artifacts/review-release/`：原样本副本、classic/review/edited下载文件、编辑与历史入口结果、大样本日志和截图。旧记录中的Edge、下载回读、浏览器性能、存储及网络待验证项已由上述结果补齐。真实Windows中文输入法候选窗和业务人员现场操作仍未验收；既有依赖审计的5项告警未在本次布局升级中处理。

默认开关改为未设置或`1`启用，显式`0`回退。UI配置的既有字段及`compact_task_ui_status=candidate`兼容值保持不变，该值不作为本次软件部署或业务验收状态。v7结构和健康接口的发布状态不变。

### 正式3001切换结果

2026-09-08 09:06（北京时间）已将审核后的14个应用文件复制到`E:\CA001\Infomat\apps\structured-output-service`，从该明确工作目录启动3001，PID为24904，原监听地址`http://192.168.19.199:3001/`保持不变。四项任务开关实际开启。服务报告HEAD为`295faf0e3c92d954f55616354013c702096f46b1`；本次改动尚未提交，该提交号不包含本次布局代码，实际发布内容以下列HTTP文件摘要及本机清单识别。

| 正式运行检查 | 结果 |
|---|---|
| 首页、`/api/health`、`/api/schema` | 均200；`status=ok`、`schema_version=process-governance-v7`、`release_status=released` |
| 结构摘要 | `e1d5b33ba80393c0d02c1a48540dca5a67947295c66a7d1f0fbf7e20a25eaacb`，与切换前一致 |
| 首页SHA-256 | `a293a057131d3a1c738a32352b8c9c3e25ca56e63aa14eb78ef08e21b1efd26f`，HTTP字节与已验证候选相同 |
| 新JS、CSS及本地Cytoscape | 均200，HTTP字节与正式目录对应文件一致 |
| 正式浏览器09:07复验 | 独立Edge在正式地址导入同一真实样本；5行为、4关系、2对象、2表单、9字段保留；实际下载仅变更导出时间，重新导入全部属性不变 |
| 正式浏览器运行边界 | 页面错误、控制台warning/error、外部请求为空，业务浏览器存储为空 |

第一次切换因检查脚本错误地把Cytoscape映射到`public/vendor/`而触发自动回退；服务实际资源来自`node_modules/cytoscape/dist/cytoscape.min.js`。旧文件恢复后服务健康，修正检查路径再切换成功。失败与回退记录保留为`attempt-1-rollback.json`，没有隐去本次尝试。

备份目录为`E:\CA001\Infomat\apps\structured-output-service\artifacts\review-layout-release-20260908`，含切换前文件、逐文件摘要、运行记录及日志。若需回退，只处理3001：恢复清单中原已存在的文件，以显式开关`0`在原地址重启，并复验健康、结构摘要和首页；新增加的布局模块可保留为未使用文件，无需删除历史资产。已有公共模块的工作树换行差异不纳入发布，不复制或格式化无关模块。

README、PRD、Tech-Spec、验证记录和受影响测试已同步。业务字段、Schema、术语、目录职责及真源路由均未改变，因此无需修改`docs/glossary.md`、根路由或目录AGENTS。未暂存、提交或推送Git；未修改共享源文件；未操作3000、5173、数据库或其他业务资产。主仓库同期出现的其他未跟踪交付物保持原状。

当前状态：布局已实现、正式3001已开启，用户已认可设计；软件与浏览器验证通过，真实流程业务验收及Windows中文输入法人工验收仍未完成。

## 2026-09-07 历史候选记录

日期：2026-09-07，北京时间。状态：候选已实现并在3011开启；正式3001未切换，人工与业务验收未完成。

## 目标与授权范围

本次服务于业务人员独立核对、补充已有单流程文件。调整布局、信息层级和操作路径，保留现有配色、字体、图标、边框、阴影、按钮和输入框外观。布局草图不作为最终视觉风格授权。

导入已有流程后默认查看流程图；图与详情并排，按环节查看人员与动作、条件与异常、数据与表单，再展开所需输入。详情沿稳定引用进入表单、表单字段和对象字段定义，并能返回。全流程目录保留未关联对象，批量编辑作为主动打开的辅助工具。七步保留为指导、检查分组与阶段下载语义。

使用情况清单不在3001或3000中实现。本次不改变v7结构、接口、枚举、标识和导入导出格式；不增加持久化或3000通信。不提交、推送、发布或重启正式3001，不连接数据库。

本次借鉴的主要是操作组织方式：[NN/g的渐进披露](https://www.nngroup.com/articles/progressive-disclosure/)建议先呈现主要操作，再由用户展开次要能力；[Carbon的编辑模式](https://carbondesignsystem.com/community/patterns/edit-pattern/)说明，需要参照主视图时可以采用侧面板。将这些原则用于摘要、并排详情和主动打开的批量工具，是本项目的设计判断，不代表需要采用外部产品的配色或组件，也不能替代本项目的用户测试。资料核对日期为2026-09-07。

## 实施隔离与运行证据

| 项目 | 实测结果 |
|---|---|
| 主仓库 | `E:\CA001\Infomat`，HEAD `295faf0e3c92d954f55616354013c702096f46b1`；本次复核工作树干净 |
| 候选工作树 | `E:\CA001\Infomat-3001-review-layout`，分支`codex/3001-review-layout`，同一HEAD之上的未提交改动 |
| 正式3001 | `http://192.168.19.199:3001`，PID 29512，候选开关关闭 |
| 候选3011 | `http://127.0.0.1:3011`，PID 23368，候选开关开启，仅本机访问 |
| 20:42 HTTP复核 | 两实例首页、health、schema及本地Cytoscape资源均200；候选新增JS和CSS均200 |
| 结构 | 两实例均为`process-governance-v7`、`released`，Schema摘要相同；此状态指已有结构版本，不表示本次界面已发布 |
| 正式首页 | 716332字节，SHA-256 `22b4b6a98e55a726183f0f418cf783ec69b2c6f128611aed7de250b175a0d8ba`，与本次开始基线及主仓库文件相同 |

Schema摘要：`e1d5b33ba80393c0d02c1a48540dca5a67947295c66a7d1f0fbf7e20a25eaacb`。

正式健康接口报告的进程提交为`53cd5b99aeb51be97ac6f105a32571a438ff584d`，与主仓库当前HEAD不同；不能据此声称进程已经运行当前HEAD。正式进程的实际工作目录未可靠确定。候选健康接口的提交号也不包含本次未提交改动，因此以隔离路径、实际文件和HTTP字节核对识别候选。详细运行结果位于本机被忽略的`artifacts/review-layout/runtime.json`。

候选使用原锁文件执行`npm.cmd ci --ignore-scripts --no-audit`，未新增依赖，`package-lock.json`未改。启动变量为`STRUCTURED_OUTPUT_PORT=3011`、`STRUCTURED_OUTPUT_HOST=127.0.0.1`、`STRUCTURED_OUTPUT_COMPACT_TASK_UI_ENABLED=1`。只隔离端口不足以保护正式服务，因此同时隔离了工作树和`public`文件。

## 实际改动

| 文件 | 责任 |
|---|---|
| `public/index.html` | 四项任务入口、摘要与编辑装配、目录、路径返回、显式编辑会话、阶段下载入口和切换保护 |
| `public/review-workspace.js` | 按候选保存内存路径、阅读位置和尺寸；稳定引用解析；既有字段类型引用同步 |
| `public/review-workspace.css` | 布局、尺寸、滚动、可见输入组及窄窗口操作栏布局 |
| `scripts/review-layout-fixture.js` | 可重复生成的虚构费用申请测试样本，不含真实业务资料 |
| `scripts/test-review-workspace.js` | 实际导航与应用适配器、候选隔离、同字段冲突、共享引用及视觉边界回归 |
| `scripts/test-compact-task-ui.js`、`package.json` | 当前导航名称与测试入口同步 |
| README、PRD、Tech-Spec及本记录 | 当前候选行为、运行方式、验证结果与限制 |

未改变目录责任、真源路由或业务术语，因此根入口、目录AGENTS、契约和`docs/glossary.md`无需修改。

## 自动检查

| 检查 | 结果与边界 |
|---|---|
| `npm.cmd test` | 18个测试脚本全部通过：含服务契约、v1至v6兼容输入、v7往返、图形与生命周期、字段复用、编辑会话、异步冲突、网页表格和候选布局 |
| 最后修改后的定向复验 | `test-review-workspace.js`、`test-compact-task-ui.js`、`test-cognitive-load-ui.js`再次通过；新导航适配器检查覆盖目录保持、逐层路径、原环节恢复和清单替代视图 |
| 视觉边界 | 原HTML内嵌样式与HEAD基线逐字比较通过；新增CSS不声明颜色、背景、字体、边框或阴影属性 |
| `npm.cmd run test:codex-context` | 根目录规则装配与预算检查通过 |
| `git diff --check` | 通过；仅有Git的LF/CRLF转换提示 |
| 模型性能 | 既有40节点等大样本回归通过；最后一次输出中位数：流程模型1.0ms，模式模型切换1.2ms，图命令1.4ms，归并1.7ms，JSON往返4.6ms。这是Node中的模型计算，不是浏览器页面性能 |
| `npm.cmd audit --json` | 未通过：5个依赖告警条目，1个高等级（fast-uri），4个中等级（@xmldom/xmldom、qs、body-parser、express）。来自未变更的锁文件，未在本次布局任务中升级依赖；未据此判断本页面的可利用性 |

自动检查不证明真实业务事实正确，不代替浏览器实际下载、操作系统输入法或人工体验。

## 可见界面模拟走查

操作使用Codex应用内浏览器和可见控件，依据当前页面逐步选择、输入和返回；使用固定虚构夹具：2个业务行为、2条关系（含退回）、2个数据对象、2张表单、1个对象字段，其中一个对象和一张表单未关联。夹具通过实际v7校验。

这属于模拟业务操作，不是招募真实业务人员进行的可用性研究。没有编造用户评价、任务耗时改善比例或认知负担评分。

| 场景 | 实际证据 |
|---|---|
| 导入与入口 | 经文件选择器导入夹具后，流程图任务选中，状态为“当前内容与导入文件一致” |
| 图与环节清单 | 可从图形或环节清单选择“核对费用申请”，显示三组摘要；选择清单项后保留清单视图 |
| 人员与关系修改 | 修改执行岗位和退回条件；未应用时触发三选项保护；应用后摘要/编辑值保留并显示未下载状态；全屏进入和退出保持当前关系 |
| 分组输入 | “条件与异常”首个可见输入获得焦点；非起始环节不出现要求填写起始条件的输入；中文文本通过控件输入保持完整 |
| 下钻与返回 | 环节→表单→表单字段→对象字段定义；路径注明对象类型。返回环节时详情滚动位置由484恢复为484，焦点回到刚才的表单入口 |
| 全流程目录 | 未关联数据和表单均可进入；目录对象详情留在“全流程数据与表单”，逐层进入字段不截断路径；回到流程图恢复此前环节 |
| 集中整理 | 表单详情→管理关联与字段→返回原对象详情，仍是原表单；批量工具默认关闭 |
| 批量编辑保护 | 数据对象表只出现一个具体新增入口；修改说明后切回逐项整理触发保护，选择应用并继续后内容保留 |
| 对象应用与撤销 | 修改未关联对象说明，摘要显示新说明和“有未下载修改”；一次撤销恢复“当前内容与导入文件一致” |
| 检查定位 | 六方面检查中的节点名称问题可以进入正确节点编辑面板；阶段草稿保留折叠入口 |
| 文件菜单 | 查看文件信息可显示真实导入文件名、来源和SHA-256；1280窗口修正后菜单位于x=364至724，落在主内容x=332至1264以内 |
| 控制台 | 本次检查的候选页面warning/error为空 |

真实DOM测量覆盖1920×1080、1920×900、1699×828、1536×864、1280×720五种CSS内容可视区。流程图摘要、条件编辑、流程信息、全流程目录、检查与下载均未出现页面级横向或纵向溢出；被测工作区和详情无横向溢出。详情及工作区使用内部纵向滚动。测试中已用`innerWidth/innerHeight`复核尺寸；早期页面曾处于默认1280×720，不把早期截图误记为1699×828。

走查发现并修正：导入误进流程信息、目录对象跳回流程图、目录字段路径被重置、分组编辑焦点落空、非起始环节摘要误提示起始条件、窄窗口文件菜单裁切和状态被隐藏。没有把这些缺陷的第一稿记为通过。

## 尚未验证及发布条件

1. Microsoft Edge不可用，工具返回`Browser is not available: edge`。上述浏览器结果均为应用内浏览器，不是Edge100%验收。
2. 真实Windows中文输入法候选窗、选词和连续输入未验证；普通中文填入及事件级自动测试不能替代它。
3. 点击保存已出现文件名和“当前内容已下载”等页面反馈，但工具的download事件等待超时，未取得实际下载文件；浏览器下载后重新导入的完整链路未验证。自动测试中的v7往返已通过，二者分别记录。
4. 未运行固定大样本的真实浏览器性能脚本。当前可用浏览器能力与该外部Playwright脚本不一致；模型性能结果不能说明页面无冻结。
5. 新增代码没有持久化或外部通信入口，但本轮未取得浏览器存储和网络请求的运行时检查证据，不能将源码检查写成运行时边界验收。
6. 未进行真实编制人、真实业务核对人的现场走查，也未完成正式发布。候选可用于审阅；上述浏览器专项、下载链路、运行时边界及人工门槛补齐前，不宣布本次界面升级已正式验收。

正式3001保持原状态。后续若发布，需要单独核对实际服务文件来源、完整候选差异、上述未验证项以及切换和恢复方式；不能只凭健康接口的已有v7 released字段宣布本次界面发布成功。
