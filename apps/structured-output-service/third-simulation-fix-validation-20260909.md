# 第三次模拟后的交互修复记录

日期：2026-09-09。已实现并在3001新页面复验；未提交、未发布新结构版本，未进行人工输入法或业务验收。

## 本轮实际变更

| 原报告项 | 处理结果 | 实现与验证 |
|---|---|---|
| I01 更新字段按钮无响应 | 已修复 | 按钮事件读取实际的gridRowId；原来读取不存在的rowId。回车导航不再截获按钮。鼠标、Enter、Space均已在真实页面打开选择器，原已选字段保持勾选。 |
| I02 粘贴失败容易遗漏 | 已改善 | 继续拒绝写入派生只读列，避免猜测覆盖；当前表格内新增粘贴反馈和三列引用粘贴说明。已验证无效字段粘贴同时在表格内和任务栏提示未写入。 |
| I03 非必填误报为空 | 未关闭，当前未复现 | 导入代表文件，保留false修改说明后应用成功；新增回归验证false可应用、null仍报必填错误。源码的文本转换及布尔选项均正确。本轮早期“布尔值生成空字符串”的判断没有得到证据支持，已撤回相关试探修改，不宣称修复。 |
| I04 检查跳转停留生命周期 | 已修复 | 数据行为关系的检查路径同时选中数据对象、切换数据流并打开对应关系属性。使用第三次模拟中间草稿，从生命周期进入检查后点击校对更新字段缺项，页面打开校对数据操作，并可直接打开选择器。 |
| I05 判断节点旧字段只读 | 保留当前规则 | 当前README明确保留判断节点历史字段只读。没有把流程控制节点改成承担业务责任的普通行为，也没有删除旧值。本轮不改变该产品规则。 |
| I06 建立多分支重复选择 | 未实施设计建议 | 这是批量建关系的效率改进，当前向导仍逐条确认类型、起点和终点；本轮未引入自动沿用或猜测。 |
| I07 全图细条 | 已有阅读路径，未新增布局改动 | 新页面导入代表文件后默认显示40%清晰视图；保留已有全图、清晰检查及全屏入口。未将低缩放概览当作内容缺失。 |
| I08 已有字段却提示未建立 | 已修复 | 已有字段而没有选中项时显示“请选择左侧字段查看或维护”；真正没有字段时保留原提示。 |
| I09 表单操作要求英文代码 | 已修复 | 中文原生多选，按住Ctrl选择多个操作；支持顿号分隔的中文单格粘贴，也兼容原英文机器值。工作副本以数组读取和保存，避免已选多项在重绘后无法显示。 |

## 验证结果

- 完整应用测试：`npm.cmd test`通过，覆盖18组现有回归入口，包括版本兼容、表格、稳定字段复用和编辑会话。
- 专项事件测试：`node scripts/test-guided-interaction-regressions.js`通过。
- 表格新增回归：false保留、null拒绝、中文和英文多操作粘贴、非法操作拒绝、数组应用；`node scripts/test-web-grid-editor.js`通过。
- 电脑插件真实页面：更新字段鼠标/Enter/Space、中文多操作选择及应用、中文单格粘贴、表内错误反馈、生命周期上下文检查跳转均完成。
- 代表文件导入→修改多操作→恢复原操作→下载：除导出包标识和时间外，递归结构比较一致；包含稳定标识、126个对象字段、126个表单字段及全部关系。仅对象键顺序变化不算内容差异。
- 真实页面使用Codex内置Chromium，验证1699×828桌面视口；不是Microsoft Edge或Windows中文输入法人工验收。Chrome连接的文件选择器返回不允许设置文件，未将这一工具限制归因于3001。
- 本轮读取的页面错误日志为空；未新增浏览器存储或后端持久化。
- 实时3001：首页、`/api/schema`及本地NativeWebGrid、适配器、Cytoscape资源均HTTP 200；`/api/health`为ok、v7 released，摘要仍为`e1d5b33ba80393c0d02c1a48540dca5a67947295c66a7d1f0fbf7e20a25eaacb`。HTTP首页已包含修复后的行标识读取代码。健康接口的Git提交号不代表这些未提交文件已发布。

验证文件位于仓库根目录`artifacts/3001-third-fixes-20260909/`：`roundtrip.json`、`roundtrip-result.json`、`check-route.png`及`multi-select.png`。原资料及第三次模拟最终文件未修改。

## 生效与剩余边界

3001现有进程直接提供public目录静态文件，新打开的页面已加载本轮改动。既有页面仍可能运行旧脚本；应先主动下载当前草稿，再重新打开或刷新并导入。此次没有重启或替换其他服务，没有更改v7 Schema、业务枚举、导出格式、数据库或正式业务事实。

I03仍需取得可重复的最小输入与操作序列。I05属于当前明确规则，I06仍为未实施的效率建议。以上事项不得计为本轮已修复缺陷。
