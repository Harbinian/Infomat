# 3001 编制交互修复验证记录

候选3011运行检查：首页、结构接口、本地Cytoscape资源均HTTP 200；健康状态ok，v7 released，结构摘要为`e1d5b33ba80393c0d02c1a48540dca5a67947295c66a7d1f0fbf7e20a25eaacb`。该发布标记描述已有结构版本，不表示本次交互修改已正式发布。

Chrome测试页日志捕获3条异步消息通道关闭错误，未取得足够来源证据，不能报告控制台零错误。Edge随后断开；Chrome文件选择也因扩展权限失败。核心交互通过与这些工具、控制台限制分别记录。

2026-09-09。依据同日“产品制造大纲编制与审批”模拟操作中发现的问题修复。本次保留原有未提交改动，不修改业务源JSON或v7数据结构。

| 原问题 | 本次处理 | 验证 |
| --- | --- | --- |
| UI-01 更新字段入口不出现 | 操作改变后重绘依赖区域并恢复勾选项焦点 | Edge候选：勾选更新、选择校对字段、确认、应用、重新打开仍显示已选字段 |
| UI-02 字段名称Tab失焦 | 名称/类型变化只更新字段列表文字，不替换输入控件 | Edge候选：名称→类型→业务定义连续Tab正确；离开返回后名称与人员类型保留 |
| UI-03 新增对象仍选旧对象 | 清理旧表格上下文后再应用图命令返回的选择 | Edge候选：连续建立两个对象，第二个对象自动选中且名称可编辑 |
| UI-05 控制节点误报执行部门待选择 | 关系下拉选项按节点类型显示流程控制 | 执行实际选项函数，覆盖普通行为及三类控制节点；该下拉选项未完成浏览器抽样 |
| UI-07 对象字段摘要占据表单入口 | 摘要默认折叠，保留返回定义入口 | Chrome空白样本截图与DOM确认折叠、表单新增入口可见；未验证126字段样本 |
| UI-08 引导数字易误解为完成进度 | 明示手动阅读位置及下一步推进方式 | Chrome截图确认文案可见；保持既有手动导航，不推断完成状态 |

现有应用 `npm.cmd test` 全部通过；新增 `test-guided-interaction-regressions.js` 执行实际事件分支和图命令函数，检查依赖控件刷新、字段控件不被重建、旧表格选择不覆盖新增选择。该测试不能替代浏览器焦点验证。

Edge的3011隔离候选验证了未应用修改离开时显示“继续编辑／放弃修改并继续／应用修改并继续”，以及继续编辑后输入保留。文件选择器被扩展文件访问权限阻断，因此本轮采用空白流程人工式点击建立最小样本；没有宣称完整原始文件端到端回归或平均耗时改善。

UI-04属于当前控制节点历史普通行为字段只读规则。本次未将历史动作说明改造成判断条件；新增判断语义需要单独明确字段和旧数据兼容方案。UI-06的全图缩小是长流程总览与可读性的取舍，当前工作区已有清晰检查与定位节点改动，本次保留，不将该项列为已修复。

正式3001未重启，原标签页未刷新，原未下载草稿仍由原页面保留。静态文件已修改，后续新打开页面可能读取新文件；已经打开的原页面不会自动更新。候选验证不是正式发布、操作系统中文输入法验收或业务验收。
