# AGENTS.md

本文件是 Infomat 的自动加载根入口，只保留全仓硬规则和任务路由。详细执行办法由任务相关的 Implementation 文档按需承载。

## 当前阶段

仓库当前处于“流程地图与数据地图的梳理与沉淀”阶段，默认分析对象是业务流程、数据和责任关系，不是具体应用系统。MDM 平台开发暂时搁置；只有用户明确要求进入对应应用时，才修改应用资产。

## 全仓硬规则

- 先判断主责资产和唯一真源，再读取、修改或生成内容。跨越资料、应用、PMO、脚本或 AI 工作区时，不顺手移动文件、重排目录或修改无关资产。
- 运行状态、Git 状态、版本、端口、服务、数据库和发布记录必须实时检查。`MEMORY.md` 只记录长期上下文，不能替代当前执行规则或实时证据。
- 保护用户已有改动。不得执行 `git reset`、`git clean`、自动暂存、提交或推送；发现目标文件存在重叠改动时，先定位冲突，不猜测覆盖。
- 不读取、输出或提交 Secret。只检查私有配置文件是否存在、是否被忽略和由哪个入口使用；未经明确授权，不连接生产或正式数据库，不触发真实通知、迁移、同步或发布。
- 用户指定一个端口、服务或资产时，只操作该对象。监听端口不等于应用可用；成功结论必须取得该资产规定的健康或内容证据。
- 代码、脚本、接口、数据库结构、前端行为、启动命令或测试命令变化时，必须同步相关文档。新增术语或改变术语含义时同步 `docs/glossary.md`；无需同步时在交付说明中写明理由。
- 3000 或 3001 的结构、字段、状态、枚举、标识、引用、权限或导入导出格式变化时，必须在同一版本工作中处理受支持旧数据。不得静默删除、清空、猜测或用默认值掩盖无法迁移的内容。
- 自动化、静态检查、隔离测试、候选服务和软件发布不能替代实时运行证据、人工体验或业务验收。结论必须区分“已实现”“已准备”“已开启”和“已验收”。

## 任务路由

<!-- codex-context-routing:authoritative -->

| 任务类型 | 必须按需读取 |
|---|---|
| 普通只读问答、解释或审查 | 任务直接相关的真源；不强制读取 `CODEX.md` 或全部根文档 |
| 仓库变更、跨目录实施或正式交付 | `CODEX.md` |
| 判断仓库收纳、生成物或历史资产 | `REPOSITORY_BOUNDARY.md` |
| 判断目录责任、可改范围或局部入口 | `DIRECTORY_OWNERSHIP.md`；局部入口完整清单只在该文件登记 |
| 判断真源、生成器、受控派生文件和消费方 | `MAINLINE_MAP.md` |
| 数据治理、3000、3001、流程地图或数据地图 | `docs/architecture/data-governance-operating-rules.md` 和对应目录入口 |
| 运行、端口、服务、数据库、同步或迁移 | 对应 `package.json`、目录 README、`scripts/README.md` 和实时状态 |
| 应用、PMO、资料或脚本专项任务 | 先进入主责目录，再按该目录 `AGENTS.md` 路由到 README、规格、真源和测试 |
| 历史事实或先前结论 | 先核对当前真源和运行基线，再按关键词读取 `MEMORY.md` 或历史材料 |

## 数据治理与表达底线

- 数据治理任务必须区分流程事实、治理结论、改进建议和待定事项；没有来源或有权主体确认时，不得补造业务事实、责任、主数据结论或系统选型。
- 业务人员说明并确认实际做法；MDM 工作组组织结构核验；PMO 跟踪行动项；有权主体作决定。系统实现、数据库、页面和模型输出不能单独替代业务确认。
- 3001 保持无状态、单流程并独立于 3000。用户主动下载是唯一持久化动作；任何可见输入不得在切换、重绘、下载、替换或离页时静默丢失或静默应用。
- 中文技术、业务和管理内容按 `.agents/skills/technical-chinese-writer/SKILL.md` 审校。制度原文、引文、编号、数据、字段、枚举、路径和错误码保持原样；人员、部门和系统动作不得混写。

## 交付口径

最终说明只报告实际完成并取得证据的内容，至少区分实际变更、文档同步、验证结果、未验证项和剩余风险。不得把计划写成完成、把建议写成决定，或把技术检查写成业务验收。
