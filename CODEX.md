# CODEX.md

本文件是 Infomat 仓库变更任务的按需执行手册。普通只读问答不需要默认读取本文件；执行跨目录修改、代码或脚本变更、正式文档交付、迁移、发布准备时，先遵守根 `AGENTS.md`，再按本文件执行。

## 1. 开始变更

开始前完成与任务风险相称的只读检查：

1. 明确用户目标、主责资产、允许修改范围和完成标准。
2. 运行 `git status --short` 并记录当前提交，确认目标文件是否已有用户改动。
3. 按根 `AGENTS.md` 的任务路由读取职责、真源、局部入口和相关规格；不无差别加载全部根文档。
4. 运行状态、版本、端口、数据库和发布情况采用实时证据，不沿用历史计划或记忆结论。
5. 判断操作是否会写数据库、覆盖派生文件、触发服务、通知、同步、迁移或发布；超出用户授权时停止该动作。

如果任务跨越多个资产，先确定主责资产和数据流，再决定是否需要修改消费方。脚本可以读取多个目录，不表示这些目录属于同一责任范围。

## 2. 保护现有工作和敏感信息

- 脏工作区只修改本次任务直接相关的文件。不得回滚、清理、格式化、暂存或提交无关改动。
- 修改前读取目标文件；用户改动与本次修改重叠时，保留用户内容并定位冲突。无法安全合并时，报告具体位置并停止该文件的自动改写。
- 不读取或输出 `.env`、本机私有环境文件中的 Secret 值。只检查路径、忽略状态、变量名和脱敏布尔结果。
- 临时日志、截图、模型答复、测试输出和浏览器状态进入被忽略的 `artifacts/` 或系统临时目录，不进入正式资料。
- 删除、覆盖、迁移和权限变化必须先解析准确目标、说明恢复方法并取得相应授权。不得使用 `git reset --hard` 或 `git clean`。

## 3. 实施方法

- 先修改真源，再按固定生成命令更新受控消费文件；不得手工修改展示副本代替生成器。
- 代码和文档改动保持聚焦，不顺带重构、批量格式化、移动目录、改名或引入无关依赖。
- 新增脚本前确认它属于仓库级 `scripts/`、应用目录还是 PMO 目录。仓库级脚本必须说明输入、输出或副作用、是否写文件、是否写数据库和验证命令。
- 界面任务复用现有设计和数据链路。静态装配检查不能替代真实浏览器中的焦点、输入、溢出、控制台、存储和网络行为。
- 发现重复规则时，只有主体、条件、对象、时序、输入输出、异常处理和验证要求完全一致，才能删除重复正文；其他位置改为链接或短路由。

## 4. 代码与文档同步

代码、脚本、接口、数据库结构、前端行为、启动命令或测试命令变化时，检查并按影响面更新：

- 主责目录的 README、`AGENTS.md`、需求、技术规格、接口或测试说明；
- 根 README、`CODEX.md`、`DIRECTORY_OWNERSHIP.md`、`MAINLINE_MAP.md` 或 `scripts/README.md`；
- 使用手册、迁移说明、PMO 真源或受控交付物；
- `docs/glossary.md`，仅当新增术语、缩写或改变术语含义时。

文档必须区分当前规则和历史基线。历史 plans/specs 只用于追溯，不得覆盖当前入口。确认无需同步的文件时，在最终说明中写明判断依据。

## 5. 3000 / 3001 版本与旧数据

结构、数据库、接口字段、状态、枚举、标识、引用、权限或导入导出格式变化前，先盘点受影响的现有数据和历史文件，说明：

- 旧字段到新字段的映射和兼容截止条件；
- 无法自动迁移的内容及人工处理办法；
- 失败判定、恢复、回滚或补偿方法；
- 迁移前后数量、关键字段、稳定标识和引用关系的核对方式。

3000 的旧数据属于持久化业务数据。升级必须提供受控迁移或兼容读取，具备执行前备份、幂等保护、失败回滚或补偿和执行后核对；数据库变更先 dry-run 或执行等价非破坏性演练。

3001 保持无状态。历史 JSON 只能在用户导入后的当前页面内存中兼容迁移，不修改用户选择的源文件，不新增数据库、服务端草稿、浏览器持久化或 3000 通信；导出统一生成当前支持版本。

每次相关升级至少验证：上一受支持版本导入当前版本、当前版本导出后重新导入、重复执行迁移、迁移失败后保住当前草稿和源文件。缺少影响分析、兼容方案或验证结果时，不得宣布版本完成或部署。

## 6. 验证选择

验证必须证明本次结果，不为形式运行无关全套测试：

- 文档和规则改动：结构、链接、术语、字节预算、差异和相关静态门禁；
- 代码改动：相关单元或集成测试、类型或语法检查、构建和代表性运行检查；
- 权限：有权、无权、跨部门或跨范围路径；
- 数据迁移：dry-run、幂等、数量、关键字段、引用、失败恢复和回滚；
- 服务启动：监听、健康接口和应用内容；端口监听本身不是成功；
- 界面：主要流程、焦点、中文输入、溢出、控制台、存储和代表性可视区；
- 正式文档或可视交付物：内容、编号、来源和最终渲染。

固定命令从根 `package.json`、应用 `package.json`、主责目录 README 和 `scripts/README.md` 获取。Codex 上下文架构变更运行 `npm run test:codex-context`。无法运行某项验证时，说明原因、替代检查和仍未排除的风险。

## 7. 状态和交付

状态必须分别记录：

| 状态 | 可以使用的证据 |
|---|---|
| 已实现 | 代码、文档或脚本已经存在，并取得相应静态或自动测试结果 |
| 已准备 | 数据结构、配置或迁移前置已经就绪，但运行入口尚未开启 |
| 已开启 | 实际开关、运行提交、目标范围和健康证据均已核对 |
| 已验收 | 真实业务对象、日期和确认主体已经完成验收 |

最终回复说明任务目标、实际修改、文档同步、验证命令与结果、未验证项和剩余风险。不得只写“已完成”，也不得把候选验证、隔离测试或软件发布写成正式运行或业务验收。
