# CODEX.md

本文件补充跨资产变更、迁移、发布准备和验证选择。按根 [AGENTS.md](AGENTS.md) 路由读取相关章节；简单局部修改不需要通读本文件。

## 变更与授权

修改前核对目标文件的 Git 状态；涉及运行或发布结论时记录对应提交和实时状态。用户已有修改与任务重叠时保留其内容，只有无法安全合并的部分才暂停。根入口的 Secret、外部写入和资产边界持续适用。

用户授权的本地实施包含必要的只读检查、隔离验证、修复和受影响部分的复验。常规实现选择无需再确认。正式数据库、真实通知、权限、迁移和发布按具体目标及已有授权执行；授权不足时，先完成可供审查的本地准备。

临时日志、截图、模型输出和夹具进入被忽略的 `artifacts/` 或系统临时目录。只读测试不应改变正式数据；运行陌生命令前核对其副作用，不能仅凭 `test` 或 `dry-run` 名称判断安全。

## 真源和文档

先修改真源，再按固定命令更新受影响的受控消费文件。生成器、输出及消费关系不清楚时查 [MAINLINE_MAP.md](MAINLINE_MAP.md)；目录责任不清楚时查 [DIRECTORY_OWNERSHIP.md](DIRECTORY_OWNERSHIP.md)。

文档同步以信息是否变化为准：

- 行为、接口、结构、命令或使用方式改变：更新承载该信息的 README、规格、契约或使用说明。
- 目录责任或硬规则改变：更新对应入口；新增或删除局部入口时更新唯一注册块。
- 新增术语或改变含义：更新 `docs/glossary.md`。
- 内部实现调整且现有说明仍准确：无需改文档，也无需逐份声明未修改原因。

新增或修改脚本时，在脚本头部或主责 README 说明输入、输出、副作用和验证入口。历史计划只用于追溯，不覆盖当前规格。

## 3000 / 3001 兼容升级

仅在结构、字段、枚举、状态、标识、引用、权限或导入导出格式变化时执行本节。受支持旧数据的处理属于同一版本工作：

- 盘点受影响旧数据，确定字段映射、兼容截止条件和无法自动迁移的内容。
- 明确失败判定、人工处理、恢复或补偿，以及数量、关键字段、稳定标识和引用的核对方法。
- 3000 持久化数据迁移须有备份、幂等、回滚或补偿；先进行 dry-run 或等价非破坏性演练。
- 3001 旧 JSON 只在用户导入后的当前页面内存中迁移；不改源文件，不增加持久化或 3000 通信，导出当前支持版本。
- 验证上一受支持版本导入、当前版本导出后重导入、重复迁移，以及失败后源文件和当前草稿保持不变。缺少兼容方案或结果时不得宣布版本完成或部署。

## 验证选择

从主责目录的 `package.json`、README 或测试说明选取与改动有关的检查。已通过且未受新改动影响的检查不重复执行。

| 变化或结论 | 所需证据 |
|---|---|
| 指令入口、路由、注册或上下文预算 | `npm run test:codex-context`；该入口只读仓库，夹具写系统临时目录，不连接数据库、不启动服务 |
| 文档或技能内容 | 受影响的事实、结构、引用和链接；技能用其格式校验器及相关合同检查 |
| 代码 | 能覆盖变更行为的测试、语法或类型检查；必要时构建或运行 |
| 授权和数据范围 | 有权、无权及越权路径，服务端校验不可由前端替代 |
| 迁移 | 本文兼容升级要求及目标库的受控迁移规则 |
| 服务启动 | 指定对象的监听、健康接口和应用内容 |
| UI 或图形 | 受影响的真实浏览器流程、焦点、输入、几何或溢出；涉及存储、网络和权限时补充对应检查 |
| 正式可视交付物 | 内容、编号、来源及最终渲染；不可渲染时说明替代检查和限制 |

纯叙述、注释或说明文字修改不默认触发应用回归、依赖审计、性能测试或服务启动。结构、权限、迁移和真实发布仍执行其必需门禁。测试不只复述实现或匹配措辞，应验证输出、行为或关键不变量。

## 交付证据

报告实际结果、验证范围和实质限制，篇幅按任务规模决定。涉及状态时使用以下口径：

| 状态 | 证据边界 |
|---|---|
| 已实现 | 代码、文档或脚本存在，并有相应技术验证 |
| 已准备 | 数据结构、配置或迁移前置就绪，入口尚未开启 |
| 已开启 | 实际开关、运行提交、目标范围和健康证据已核对 |
| 已验收 | 真实业务对象、日期和有权确认主体的验收记录 |

候选服务、静态检查、隔离测试和软件发布均不能代替真实运行、人工输入法体验或业务验收。
