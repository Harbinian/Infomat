# Infomat 首批仓库减重执行记录

> 日期：2026-09-07
>
> 状态：已按用户确认范围执行归档及原目录移除；提交推送按用户后续明确授权执行。
>
> 执行基线：`master`，提交 `53cd5b99aeb51be97ac6f105a32571a438ff584d`。

## 1. 范围与实际结果

本次只处理 `.tmp/u8125_schemadoc_20260901/` 中的 U8 12.5 帮助文档解包副本。用户确认的处理方式为：在仓库外保留已校验的压缩归档，再移除原工作目录，并同步忽略规则和资料查找说明。

该目录原有 20,071 个文件，逻辑大小为 137,577,552 字节，约 131.20 MiB。全部文件已保存在外部 ZIP 中，原目录于 2026-09-07 13:34（Asia/Shanghai）移除。

| 项目 | 结果 |
|---|---|
| 原目录文件 | 20,071 个，已从工作目录移除 |
| 原目录逻辑大小 | 131.20 MiB |
| ZIP 逻辑大小 | 32,793,781 字节，31.27 MiB |
| 逐文件校验清单 | 3,353,292 字节，约 3.20 MiB |
| 扣除 ZIP、清单及归档说明后的减少量 | 约 96.7 MiB，按文件逻辑大小计算 |
| Git 变更范围 | 20,071 项解包文件删除及 6 个规则、说明文件；其他任务改动不纳入本次提交 |

以上减少量不代表 NTFS 实际分配空间，不包含仓库外其他工作树或既有工具缓存。仓库中新增和更新了少量说明文字；提交前的精确字节核验见 [最终核验记录](../../../Infomat-archive/2026-09-07/completion-check.json)。归档移除阶段没有修改 Git 索引，后续提交不重写既有 Git 历史。

## 2. 长期归档与恢复

本机归档目录：`E:\CA001\Infomat-archive\2026-09-07\`。

- [ZIP 归档](../../../Infomat-archive/2026-09-07/u8125_schemadoc_20260901.zip)
- [逐文件清单](../../../Infomat-archive/2026-09-07/u8125_schemadoc_20260901.manifest.json)
- [完整恢复验证记录](../../../Infomat-archive/2026-09-07/verification.json)
- [归档与恢复说明](../../../Infomat-archive/2026-09-07/README.md)

ZIP 的 SHA-256 为：

```text
FA2785CCCC244A007CBDBF7FFB17AFBC65E7E655263D1122BB9655ED71D4ED79
```

需要读取资料时，先校验 ZIP 哈希，再解压到一个新的空目录。压缩包内保留顶层目录 `u8125_schemadoc_20260901/` 及全部相对路径。若需要恢复原路径，应先确认 `.tmp/u8125_schemadoc_20260901/` 不存在，再恢复该目录，避免覆盖后续工作。

归档是当前机器的本地资产，不会随 Git 自动分发。其他机器需要这份资料时，应取得该归档并核验内容。本次保留的是解包副本，未声称已找到或重新生成原始 `U8125 SchemaDoc.chm`。

## 3. 验证证据

归档及移除前已完成：

1. 核对源目录的绝对路径及上级目录，确认没有符号链接或目录联接，且源目录没有未提交修改。
2. 重新核对当前源目录全部 20,071 个文件的路径、大小和 SHA-256，与已准备的恢复清单完全一致。
3. 将 ZIP 转存到长期归档位置，并确认转存前后 ZIP 哈希一致。
4. 从长期归档 ZIP 完整解压到新建临时目录，再逐文件核对路径、大小和 SHA-256，20,071 项全部一致。
5. 在移除前再次检查当前源文件内容；通过后仅移除批准的单一目录。
6. 清除本任务创建的临时恢复目录和临时 ZIP 副本，长期归档保持可访问。

归档后已通过 `npm run test:codex-context`，变更范围的 `git diff --check` 通过；说明中的文件链接检查未发现失效路径。在首批提交前的核验时点，Git 索引条目摘要、提交号保持不变，20,071 条未暂存删除与归档清单逐项对应。忽略规则仅匹配指定 U8 解包目录，未扩展到 `.tmp/` 内其他目录。

执行前记录的 43 项既有保护文件中，复查时 42 项内容摘要保持一致，1 份既有业务草案在执行期间出现更新；该草案不在本次写入范围内，其当前内容保持原样。详细结果记录在 `completion-check.json`。这些检查证明本次文件迁移及说明更新的结果，不替代应用运行检查或业务验收。

## 4. 文档与规则同步

- `.gitignore` 只新增 `/.tmp/u8125_schemadoc_20260901/`，避免这一解包目录恢复后再次成为新增文件来源；忽略规则不会自动取消已跟踪文件的跟踪。
- `DIRECTORY_OWNERSHIP.md` 补充 `.tmp/` 的当前职责及归档查找入口。
- `docs/archives/README.md` 登记仓库外参考资料归档。
- 本目录 README 增加执行记录；2026-09-01 U8 表与字段关系报告只补充资料位置说明，历史结论保持原样。

应用代码、结构规则、接口、数据库、启动和测试命令未变化，因此无需修改应用文档、主线数据流或服务配置。未新增或改变业务术语，术语表无需同步。

## 5. 本次保留范围

OCR 环境、应用依赖、业务制度和表单原件、会议录音、存货冻结导出、运行数据库、账号材料及其余历史输出均保留。盘点发现的重复内容和 Git 大对象包仅列为后续候选，不属于本次移除授权。

原有未提交修改由各自任务继续维护。归档移除阶段没有运行 Git 暂存、提交、推送、清理、历史重写或对象裁剪命令，也未连接数据库或启停服务。

## 6. 提交边界

首批结果交付后，用户明确要求完成提交和推送。本次提交仅纳入上述 20,071 项删除、`.gitignore`、`DIRECTORY_OWNERSHIP.md`、归档索引、报告索引、U8 原报告的资料位置说明和本执行记录。长期 ZIP 与逐文件清单仍保留在仓库外，不随本次 Git 推送上传；其他任务的未提交内容保持原样。提交和远端结果以实际 Git 记录为准。
