# Process V7 迁移证据偏差与整改记录

> 状态：整改执行中  
> 首次记录时间：2026-08-26  
> 适用范围：3001 `process-governance-v7` 修复、3000 V7 预览与正式承接、M1/M2 迁移证据  
> 证据边界：本文是审计与整改记录，不替代运行配置、数据库真源、业务核对或发布批准。

## 1. 结论

M1 和 M2 已经写入当前 MySQL，迁移记录与其目标结构在本次只读检查中一致，V7 业务记录仍为 0。现有 8 个流程主档、16 个草稿和 13 个版本均为 V3；正文摘要检查未发现不一致。

当前没有找到能够证明 M1/M2 首次执行前已经完成全库备份、M0 和隔离演练的证据。原说明引用的 `output/process-v7-m0/` 目录已经不存在。受控备份目录仍保留 2026-08-25 的 SQL 备份，但该备份晚于 M1/M2，只能证明迁移后的文件留存状态，不能倒推迁移前门槛已经通过。

本轮保留 M1/M2，不回滚、不重放、不修改迁移时间，不新增 M3、唯一索引或其他数据库结构。代码修复和自动测试可以继续；正式库新备份、隔离恢复、服务重启、运行配置变更和真实试点仍需按计划取得独立批准。

## 2. 已确认事实

### 2.1 仓库和运行状态

- 只读检查时仓库 HEAD 为 `976d16a15fbe083ad6d6dcc75e24ebe3d772e410`，工作区为干净的 `master...origin/master`。
- 计划中的基线 `aa7842c165104c277fb2722cc376bd664cc98555` 已被后续提交取代；本轮保留后续提交，不执行回退。
- 3001 正在 `0.0.0.0:3001` 运行，健康接口返回 `status=ok`、`schema_version=process-governance-v7`、`release_status=released`、`app_commit=cc10cc0bef81ff207d018cd5cfb819e402eb646c`。
- 3000 在只读检查时没有监听端口 3000。本轮尚未启动或重启 3000。

### 2.2 数据库和迁移

- MySQL 版本为 `8.4.9`，事务隔离级别为 `REPEATABLE-READ`；`@@system_time_zone=UTC`，`@@session.time_zone=SYSTEM`。本轮只读补录时的`UTC_TIMESTAMP()`为`2026-08-25T19:03:38.000Z`。
- M1 `2026-08-24-process-v7-preview-review` 的数据库记录时间为 `2026-08-24T23:53:54.000Z`。
- M2 `2026-08-25-process-v7-formal-foundation` 的数据库记录时间为 `2026-08-24T23:54:12.000Z`。
- M1 四张预览表存在、结构匹配且各为 0 行。
- M2 的列、索引和 `process_v7_promotions` 表存在并匹配，提升表为 0 行。
- V7 正式草稿和正式版本均为 0 行；V7 审核任务空绑定、重复绑定和同草稿多条待审核任务均为 0。
- V3 当前为 8 个主档、16 个草稿和 13 个版本；16 个草稿和 13 个版本的内容摘要均匹配。
- 历史例外仍为 6 个空草稿 `document_id`、5 个空版本 `document_id`、2 个活动主档无当前版本。本轮不修改这些历史数据。
- R0仓库提交中的M1入口脚本、M1实现、M2入口脚本和M2实现的SHA-256已经写入仓库内外清单；摘要只证明所记录文件字节，不能代替迁移批准或首次执行前证据。

### 2.3 证据缺口

- `output/process-v7-m0/` 当前不存在，原备份恢复、M0 和隔离演练 JSON 无法从该路径复核。
- 最新保留备份 `C:\Users\charl\Documents\Infomat-Backups\infomat_mdm-m0-20260825071508.sql` 大小为 `78415231` 字节，SHA-256 为 `b1167a6d400048feaac53ec61371b4a23542cb37eafc6409f839ecf649a6c24e`。
- 该备份文件的写入时间晚于 M1/M2，因此只能归类为迁移后备份。
- 当前只读 M0 检查因没有可验证的备份恢复证据返回 `BACKUP_RESTORE_NOT_VERIFIED`，`ddl_allowed=false`。这项阻断不影响代码修改，但阻止数据库结构变更、受控部署和真实试点。

## 3. 整改动作和状态

| 整改动作 | 当前状态 | 关闭条件 |
|---|---|---|
| 冻结当前 Git、3001 健康、M1/M2、V3/V7 数量和摘要 | 已完成 | 仓库内外清单可以复核 |
| 永久记录 M1/M2 前置证据缺口 | 已完成 | 后续报告不得把事后证据改写为迁移前证据 |
| 修复 3001 历史关系导入、严格下载、错误码和安全门槛 | 代码、自动检查和候选实例真实浏览器技术检查已完成，发布门槛未关闭 | Windows 中文输入法人工门槛通过；正式运行实例在批准提交上完成代表性复核 |
| 加固 3000 开关、单流程范围、摘要、阻断项和并发 | 代码与模拟/接口回归已完成，隔离数据库门槛未关闭 | 新备份恢复后的隔离 MySQL 并发、故障回滚和 V3 回归通过 |
| 重新生成全库备份并在隔离 MySQL 恢复 | 未执行，待批准 | 目标数据库、备份位置和保密范围获明确批准 |
| 重启 3001、启动或重启 3000 | 未执行，待批准 | 对应服务取得单独批准，运行提交与批准提交一致 |
| 一个真实脱敏流程业务试点 | 未执行，待输入和批准 | `process_ref`、部门、四名业务角色、材料和窗口完整 |

## 4. 修复后的技术检查

### 4.1 3001

- `npm.cmd ci` 已完成；安装 137 个依赖，未引入新的第三方依赖。
- `npm.cmd test` 已通过。测试覆盖 v1 至 v6 真实夹具、历史控制节点关系、整批导入、错误响应、JSON 与 DOCX 安全边界、Worker 并发与超时、图形编辑、生命周期、字段复用和原生网页表格。
- `npm.cmd audit --json` 已通过；高危、严重、中危、低危和提示级漏洞均为 0。
- DOCX 100:1 压缩比门槛已按所有非空条目执行，不再对 1 MB 以下条目豁免；100:1、超过 100:1 和零压缩字节边界测试已通过。
- 在独立端口 `127.0.0.1:3101` 使用真实 Chromium 检查了 1920×1080、1536×864 和 1280×720；三个尺寸均未出现页面级横向溢出，最终控制台 warning/error 为 0。
- 公共 v3 培训样例导入后保留 6 条数据关系和 1 条表单处理关系；页面显示 7 项技术阻断，阶段下载和最终下载均未生成文件。
- 脱敏技术夹具完成“修改到已有 action 行为 → 阶段下载 → 最终下载 → 重新导入”；技术标识和操作类型保持不变。
- 浏览器中的无效 UTF-8、10 MB+1 文件、损坏 JSON 和不完整多候选文件均被拒绝，原候选流程保持不变。另以一份通过源版本校验的双候选文件验证目标迁移失败：候选 0 有效，候选 1 仅包含一项流程自环错误，系统整批拒绝且未安装部分候选。失败前后的候选顺序与索引、完整 JSON 摘要、图模式与视口、选择、撤销与重做内容、未应用图属性、NativeWebGrid 工作副本、未下载状态和最近下载基线均保持不变；保留的源 `File` 对象名称、大小、修改时间和 SHA-256 也保持不变。
- 浏览器网络请求只访问独立 3001 地址和本地资源；未请求 3000、CDN、`/api/session`、`/api/data` 或 `/api/export`。`localStorage`、`sessionStorage`、IndexedDB、Cache Storage 和 Cookie 均为空。
- 带唯一 canary 的业务内容导入前后，3001 应用目录文件数量、文件摘要和修改时间未因业务内容变化；服务输出未出现 canary。刷新和独立测试服务重启后不能恢复页面草稿。
- 最后一次全套自动测试中的固定性能夹具预热后取三次中位数：流程图显示约 1.0 ms、模式切换约 1.2 ms、图命令约 1.4 ms、200 字段归并约 1.7 ms、下载并重新导入约 4.3 ms；原生网页表格样本约 9.0 ms。这些数值只用于纯函数回归，不替代真实浏览器性能证据。
- 在独立候选实例和 Headless Chromium 151 中，以 40 个行为、80 条流程关系、30 个数据对象、120 条数据操作、60 条可见数据边、10 张表单及各 200 个数据字段和表单字段的严格有效 V7 样本，先预热一次，再连续测量三次并取中位数。首次显示为 124.9 ms，双向模式切换最大值为 105.7 ms，选择为 101.4 ms，图命令为 141.4 ms，200 字段归并为 123.0 ms，下载并重新导入为 554.9 ms；超过 1 秒的 Long Task 和绘制帧间隔均为 0，控制台 warning/error、页面错误和意外网络请求均为 0，全部固定门槛通过。
- 一次有界面 Chromium 诊断中，两个绘制帧探针本身达到 1627.9 ms，说明桌面后台帧调度已被节流；该样本在形成验收结论前被排除。可重复脚本现会在两个绘制帧的三次探针中位数超过 250 ms 时直接停止，避免把无效环境误判为应用性能失败或通过。
- 上述新增浏览器证据基于仓库 HEAD `aa2591e2414da07fd96bf060ff864e118396c8f0` 的候选工作区形成；当时工作区包含尚未提交的并行改动，因此健康接口中的 `app_commit` 只能标识基础提交，不能证明候选字节已经形成可部署的干净提交。
- Windows 默认中文输入法人工组合输入门槛尚未执行，正式运行实例也未在批准提交上完成代表性复核，因此 3001 批次 A 仍不能标记为完整发布验收通过。
- 新增的两个测试入口和验收口径已经同步到 3001 README 和技术规格。本轮没有修改 V7 Schema、`docs/norms/` 流程输入基线或业务术语，也没有引入新的业务概念，因此无需因这两项测试再修改 `docs/glossary.md`。

### 4.2 3000

- 以下回归已在 `apps/mdm-platform` 完整通过：`test:process-v7-preview-review`、`test:process-design`、`test:frontend`、`test:process-governance`、`test:mainline`、`test:role-workbench`。
- 合同和接口测试已覆盖默认关闭、单一精确 `process_ref`、摘要 v2、阻断项、提交/审核/发布 CAS、私有事务能力、锁后授权重读、管理员只读、调用方对象降级攻击、预览必要锁序、M1 六种一致性状态和 M2 对 M1 的前置检查。
- 最终只读差距审计发现“已发布 V7 主档可经通用下一版或旧结构化导入生成 V3 草稿”的绕过。本轮已经在路由和仓储双层返回 `409 V7_CONTENT_READ_ONLY`，并增加仓储直调、通用下一版 HTTP 和旧结构化导入 HTTP 回归；三个入口均在写入前停止。修复后复核确认 V3 的 A 版到 B 版通用路径仍返回 201，未发现剩余 P0/P1。
- 测试使用假连接、内存仓储、临时 SQLite 或本地 HTTP 边界；没有连接正式库执行 V7 写入，也没有替代真实隔离 MySQL 的双连接并发、死锁、故障回滚和恢复对比。
- 本轮没有运行 M1/M2 apply 或 rollback，没有新增 M3、唯一索引或其他 DDL。

### 4.3 当前运行边界

- 正式端口 3001 没有重启，服务端健康信息仍报告 `app_commit=cc10cc0bef81ff207d018cd5cfb819e402eb646c`。该进程直接读取共享工作区中的静态文件，因此本轮前端文件修改已经可能被该端口读取；这是一种“旧服务端进程 + 未批准工作区静态文件”的混合状态，不能记为批次 D 部署通过。
- 独立测试端口 3101 只用于技术检查，不是正式部署实例。
- 3000 没有启动或重启，V7 开关和试点引用没有修改。

## 5. 复核命令

```powershell
cd E:\CA001\Infomat\apps\mdm-platform
npm.cmd run inspect:process-v7-m0
npm.cmd run migrate:process-v7-preview:dry-run
npm.cmd run migrate:process-v7-formal:dry-run
```

上述命令只读当前数据库。任何 apply、rollback、备份、恢复或服务操作均不属于本记录已经完成的动作。

## 6. 尚未关闭的风险

- 原始迁移前证据缺口永久保留，后续新证据只能按原时间追加，不能替换本结论。
- 当前备份尚未在本轮隔离恢复，不能作为本轮部署或试点恢复门槛。
- 3001 的三档视口、异常恢复、无状态、合法多候选整批失败原子性和固定大样本真实浏览器性能检查已经完成；Windows 中文输入法人工验收和批准提交上的正式运行实例代表性复核尚未完成。
- 3000 的并发、锁顺序和失败回滚尚未在本轮隔离 MySQL 中完成。
- 当前新备份恢复演练尚未批准并执行，不能把模拟仓储或接口测试写成数据库并发验收。
- 正式端口 3001 的服务端提交与工作区静态文件不一致；在批准部署前不得把该混合状态写成正式运行提交。
- 尚未提供真实试点 `process_ref`、归口部门、承接部门和角色人员，因此不能形成业务验收结论。
