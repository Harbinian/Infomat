# Superpowers

本目录保存历史方案与计划文档，用于追溯讨论过程。

注意：

- 文中如出现 `mdm-platform/`、`cd mdm-platform` 等旧路径，请按当前仓库结构替换为 `apps/mdm-platform/`、`cd apps/mdm-platform`
- 文中如出现旧 AI 入口，仅作为历史记录理解；当前执行入口统一为 `AGENTS.md`、`CODEX.md` 和 `.agents/`
- MDM 平台开发当前处于搁置状态，以业务部门完成流程地图与数据地图梳理为前置条件

