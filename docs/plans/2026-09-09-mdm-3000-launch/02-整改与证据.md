# 3000 上线整改与证据

> 2026-09-11后续决定覆盖：所有`docs/`文件均不再是真源。本文只记录历史工作、用户决定和证据位置；技术检查不证明当前业务事实或实际授权。3001编制的未审核V7由用户手动上传3000治理、审核并形成正式版本，再依据治理结果生成程序文件。人员事实回到用户指定的行政人事部原始Excel，业务事实回到实际材料和业务确认。原有明确确认的验收前提保留，最新变化见第15节及03第13节C01。

> 更新日期：2026-09-11  
> 阶段：07全部已授权发布准备完成；发布仍阻断；停止，不进入08  
> 代码基线：`master`，`dec8996c375d780edc0106a4c32584d2dd664944`  
> 适用对象：信息化项目负责人、MDM工作组、开发、测试与运维人员

第1至7节保留第01阶段审查时的事实与定位，行号对应当时源码；第02、第03、第04阶段实际结果分别保留在第8、9、10节，第05阶段以第11节为准，第06阶段以第12节为准，第07阶段以第13节为准。早期“未实施”及停止记录只描述对应阶段当时的状态。

## 1. 第01阶段结论和范围

需要局部重构，不需要整体重写。F01至F08的代码或测试缺口在本地仍有依据；F09、F10是现有运行与产品能力的范围条件。必须先消除正式启动、公共初始化和首页的数据来源混用，再验证V7办理链路。保留Express、MySQL、当前前端、固定身份权限和原生V7事务。

用户已明确要求“继续下一阶段”，本轮据此进入01。第00阶段D00-01至D00-03仍未回答，不将继续审查解释为批准单流程首发、批准数据地图范围或批准真实发布。需求相关整改按条件列明，已有主链地图、三类模拟待办和OFF-01至OFF-10线下要求继续保留。

本阶段未修改应用行为，未启动服务、连接数据库、执行迁移、安装依赖或运行浏览器。F01/F04报告中的隔离启动结果未在本轮重演；F06只运行了只读路由盘点，没有重跑`test:security`。以下将静态确认、实际盘点和待复现条件分开记录。

## 2. 可追溯证据

- 基线与需求：[00-执行基线](00-执行基线.md)、[01-需求与验收](01-需求与验收.md)。
- 当前应用规则：[3000 AGENTS](../../../apps/mdm-platform/AGENTS.md)、[README](../../../apps/mdm-platform/README.md)、[package.json](../../../apps/mdm-platform/package.json)。
- 本机静态证据：[static-review-evidence.json](../../../artifacts/mdm-3000-launch/stage01-20260909-1512/static-review-evidence.json)。该文件位于被忽略的`artifacts/`，含16个审查文件的SHA-256、完整路由盘点、前端API字面量位置和SQLite顶层加载清单；不含凭据或业务数据。
- 实际只读命令：在仓库根执行`node apps/mdm-platform/scripts/audit-route-write-permissions.js --json`，被审计脚本退出码为1，原因是92条未分类记录。外层证据采集完成不代表审计通过。
- 静态提取发现258处API字面量引用、19个路由文件顶层直接加载`../db`。这两个数量包含重复引用、条件注册和遗留代码，不能作为独立接口数量、正式可达数量或漏洞数量。

## 3. F01至F10逐项核验

### F01｜P0：正式启动和公共页面仍依赖SQLite

- **代码位置**：`server/index.js:48`起注册路由；`server/routes/org.js:4`顶层加载`../db`；`server/db.js:13`创建目录、`:15`打开SQLite并继续执行结构初始化。`roleWorkbench.js:4`、`pageWorkflows.js:3`和`processGovernance.js:6`也有顶层加载，不能只修组织路由。
- **触发与复现条件**：设置正式模式、两个MySQL读模型，完成正常模块加载即会触发SQLite入口。登录后的`public/index.html:7238`调用`loadAllSafely()`，经`:7286`的`loadAll()`、`:7291`的`loadCatalog()`读取旧能力与流程目录。首页`roleWorkbench.js:529`的`loadTodos()`、`:812`的`loadEscalatedConflicts()`以及`:473`起的上下文读取仍使用SQLite。此处为静态调用链确认，本轮未启动重演。
- **影响任务**：R00-03及R00-04至R00-08的公共身份、目录和工作台；旧数据与正式MySQL可能不一致。已确认存在业务读取代码，仍不能宣称正式实例在双写或已发生数据损坏。
- **是否已修复**：未修复。`index.js`只检查生产身份读模型，没有同时拒绝流程读模型缺失或不一致；`:71`仍可能选择旧`processDesign`。
- **处理方案**：正式注册只保留必需且数据源明确的入口。身份、工作台、页面流程提示及流程治理必须去除顶层SQLite副作用，复用已有MySQL仓储。旧`capabilities/processes`公共初始化必须同时改为按需加载或接入已确认的MySQL目录，不能只注销后让登录报错。范围外旧API在服务端明确隔离，界面对应入口同步停止调用；旧记录保留并列明兼容读取或未来迁移去向。
- **验收方法**：隔离目录监控文件访问，正式启动及登录、首页、V7代表性请求均不创建、读写SQLite；数据库不可用、两读模型缺失/不一致时明确失败且不回退；关闭旧入口后登录和必需目录完整可用；已有V3/V7稳定标识及内容不变。

### F02｜P0：取得运行仓储时仍执行结构初始化

- **代码位置**：`server/auth.js:174`、`routes/org.js:88`取得身份仓储并调用`initSchema()`；`identityMysqlRepository.js:532`执行基础SQL、人员字段补齐和固定角色初始化。`:138`起含`ALTER TABLE`，`:166`调用`seedFixedAccessModel()`。
- **触发与复现条件**：进程内首次登录、鉴权或对应页面取得仓储；初始化Promise失败后重置，下次请求可能重试。相同模式还出现在`roleWorkbench.js:109/139/169`、`processGovernance.js:81/120/163`、`governanceGuidance.js:26/44`和`roles.js:17`。数据库是否实际改变取决于当时结构和种子逻辑，不将调用事实解释为每次请求都改表或重置账号。
- **影响任务**：所有身份与首页任务；运行账号需承担不应属于普通请求的结构维护职责；只修改登录入口会留下其他请求触发点。
- **是否已修复**：未修复。
- **处理方案**：运行仓储取得、只读结构检查、显式迁移分离。保留现有仓储工厂和测试注入；复用其实现，不另建通用数据库平台。正常请求缺表或漂移时返回可操作的不可用信息，不补表、不重新种角色。迁移账号和应用账号权限分别说明，真实授权不在本阶段变更。
- **验收方法**：在只具备规定运行权限的隔离MySQL账号下登录及调用首屏/治理接口，确认无DDL或角色初始化调用；缺结构明确拒绝；显式迁移首次、重复、漂移与中断场景另行验证。

### F03｜P0：正式会话和HTTPS访问配置未闭合

- **代码位置**：`server/index.js:27`的`express-session`没有`store`或应用专属`name`；Cookie为HttpOnly、SameSite=Lax、24小时，Secure随production开启；`:96`附近使用HTTP监听，入口未设置可信代理。当前安装的`express-session/README.md:36`说明MemoryStore不适用于生产，`:173`起说明Secure和代理配置条件。
- **触发与复现条件**：进程重启使内存会话消失；production直接HTTP或TLS在上游终止但应用不信任正确代理时，可能无法建立会话。真实域名、代理与部署方式尚未确认，不能称为已发生的生产故障。
- **影响任务**：R00-04至R00-08；登录持续性、停用撤权与重启恢复。
- **是否已修复**：未修复；已有HttpOnly/SameSite以及`identityMysqlRepository.js:552`起的账号状态、`auth_version`检查应保留。
- **处理方案**：优先评估复用MySQL保存会话，单独迁移准备表，运行不自动建表；明确重启保留或失效策略、清理、有效期和应用专属Cookie名称。按确定的TLS终止位置配置精确可信代理，不关闭正式Secure，不无条件信任代理头。会话缺少正式身份关键字段时按现行身份合同补充负向验证，不沿用兼容分支作为正式放行依据。
- **验收方法**：真实部署拓扑或明确的隔离代理中验证Cookie、正常/无效会话、停用、撤权、首次改密、重启与过期清理；日志和响应不泄露会话或凭据。环境未定时仅报告配置和隔离证据。

### F04｜P0：健康接口无法表示数据库就绪

- **代码位置**：`server/index.js:92`的`/api/health`固定返回`status: ok`、身份和治理模型版本，没有数据库检查或运行提交。
- **触发与复现条件**：进程已启动但MySQL不可用或首发结构缺失，接口仍按固定代码返回200。报告已隔离复现；本轮只确认响应实现未变。
- **影响任务**：R00-03、R00-08及发布判断，不能凭该响应宣布业务可用。
- **是否已修复**：未修复。
- **处理方案**：保留兼容的存活接口，新增带超时且成本受控的就绪检查，验证数据库、必要结构、两个读模型和实际运行版本。必要结构依据保留的首发调用链确定，未启用工作包不被误设为全站就绪前提。
- **验收方法**：正常、断库、缺结构、错配置和恢复分别返回预期状态；未就绪不报成功；能核对运行提交；响应不含Secret或业务正文。

### F05｜P0：共同启动入口不适合独立发布3000

- **代码位置**：`scripts/start-infomat-services.ps1:156`读取共同本机配置，`:174`起确认MySQL并执行`init:mysql`及实时检查，`:179`起停止MDM和PMO监听，再分别启动两项服务。
- **触发与复现条件**：执行根`start:infomat-services`；本轮只审查脚本，没有执行。
- **影响任务**：R00-08以及仅操作3000的范围约束，可能触发结构动作并中断无关PMO。
- **是否已修复**：未修复。应用`npm start`仅是Node前台入口，不具备完整的独立停止、重启、就绪和恢复能力。
- **处理方案**：保留共同开发脚本作为既有资产；增加只针对3000的运维入口，复用非敏感配置真源。启动不初始化数据库或账号，不操作其他服务；停止前核对进程归属和路径。按实际主机选择进程托管、异常恢复和日志轮转，暂不引入编排平台。
- **验收方法**：用隔离进程验证目标识别、端口被其他程序占用时拒绝、仅3000启停、启动无DDL、异常退出及恢复、就绪与版本核对。真实服务重启另按具体授权执行。

### F06｜P0：安全测试和路由登记仍有旧预期

> 以下为第01阶段原始审查记录。第04阶段已完成本地整改及隔离验证，当前结果见[第10节](#10-第04阶段实际整改与证据2026-09-10)；历史92条及旧行号保留用于追溯，不作为当前失败结果。

- **代码位置**：`scripts/test-user-password-scripts.js:55`要求废弃建号脚本成功；两个实际脚本在顶部返回`LEGACY_ACCOUNT_SCRIPT_RETIRED`并退出1。`test-security-routes.js:691`期待admin更新组织单元200，后续还期待admin新增岗位201。路由盘点读取所有路由文件并按签名及手工表分类，不理解完整注册和中间件顺序。
- **实际盘点结果**：总记录216条：permissionGuarded=64、integrationGuarded=2、businessGuarded=55、publicOrSelfService=3、followUp=0、unclassified=92；退出1。未分类分布：`processDesignMysql.js`47，`governanceGuidance.js`8，`processGovernance.js`8，`processDataGovernance.js`7，`processV7PreviewReview.js`7，`accounts.js`6，`org.js`4，`roles.js`4，`processDesignEditor.js`1。
- **影响任务**：R00-03及身份、部门、审核和发布全部关键任务；现有失败无法用来证明当前权限正确或错误。
- **是否已修复**：未修复。92条不是92个漏洞；手工“已分类”描述也包含过时管理员权限，不能直接当安全证据。`org.js:223`起的组织写入口前置拒绝，`:491`起的旧身份写入口前置410，后方同路径遗留实现不可按静态数量视为可达；`roles.js`等停用入口也应单列。
- **处理方案**：让废弃脚本测试验证拒绝且无写入；依据固定角色建立真实业务角色正向与反向路径，不恢复admin业务权限。盘点区分正式注册、隔离遗留、前置拒绝、公共/本人服务、真正业务写入，逐条追到权限、部门、对象状态、并发和审计检查，不能给92条整体放行。
- **验收方法**：先修测试合同，再运行受影响安全检查；真实MySQL中的身份、跨部门、并发与发布事务另取证。`test:security`本轮未运行，不能称为已本地复现整套测试失败或通过。

### F07｜P1：工作台未完整聚合V7任务，并吞掉部分查询错误

- **代码位置**：`routes/roleWorkbench.js:1010`的直接流程聚合只取`listHandoffQueue()`和`listHandoffConflictQueue()`；`:1111`的总聚合没有独立的V7预览核对、退回、正式审核或待发布读取。`:1074`已有后续数据治理工作包聚合。`:1066`与`:1090`捕获部分查询失败后返回空数组。
- **触发与复现条件**：存在仅属于V7新链路的待办，或直接流程/工作包仓储查询失败。前者缺少直接聚合路径，后者可能被呈现为没有该类事项；本轮未用真实账号浏览器验证。
- **影响任务**：R00-05至R00-07和OFF-06。只要部门或工作组需在线办理V7，就必须在启用前关闭本项。
- **是否已修复**：未修复。已有工作包入口和旧承接聚合不能证明V7新任务已接入。
- **处理方案**：沿用工作台事项结构、稳定ID和角色标记，从V7现有仓储聚合核对、退回、审核、待发布；完成或修订后刷新正确。错误明确为来源不可用，不能伪装为空。与F01一并消除首页SQLite读取，不新建任务引擎。
- **验收方法**：逐角色检查事项出现、定位当前案例/修订、完成消失、内容变化重开、多角色合并、跨部门拒绝；注入查询失败时展示不可用。分别记录技术身份验证与真实人员验收。

### F08｜P1：V7核对、范围决定和正式审核意见缺少未提交保护

- **代码位置**：`public/index.html:3169`生成直接保存于DOM的核对依据与选择；`:3399`保存一项后清缓存并整体重载视图；`:3421`范围决定、`:3511`正式审核也在重载后替换内容。`:6595`的hash保护只处理工作包弹窗，`:6616`离页保护只检查V3和工作包dirty标记；`:11222`退出登录后刷新。
- **触发与复现条件**：同页填写多项后只保存一项、切换案例/工作区、上传新修订、刷新或退出。当前未将这些V7输入接入dirty状态或重绘恢复。网络失败未重绘时输入可能仍在，不能笼统声称每次失败均丢失；401、409和后续重绘需逐场景验证。
- **影响任务**：R00-05至R00-07及OFF-09；V7在线办理前必须关闭。
- **是否已修复**：未修复；已有V3与工作包保护可借用交互方式，但不能认为已覆盖V7。
- **处理方案**：按案例、修订、核对项及编辑用途标识当前页面内未提交值；提交成功只清除对应编辑，其他输入在重绘后保留。所有离开/替换入口统一保护；409保留意见并让人核对新修订，不自动套用。覆盖范围决定、正式审核和提升目标输入，不仅处理核对textarea；不自动提交、不新增浏览器持久化。
- **验收方法**：同页两项输入保存其一，另一项保留；取消离开恢复原输入；切换、刷新、上传、退出、401/409/断网分别验证；检查中文输入法、焦点、滚动和键盘。静态测试不替代真实浏览器。

### F09｜范围条件：单流程试点无法直接承担多流程持续写入

- **代码位置**：`server/processV7TrialScope.js:6`读取单一技术标识，`:49`起精确匹配；预览仓储`:281`和`:879`从锁定记录再次检查；正式链`processDesignMysql.js:1058`的锁定上下文与提交/审核/发布事务共用范围限制。工作包的`processDataGovernanceScope.js`只接受唯一不可变版本ID。
- **触发与复现条件**：写入第二个流程或第二个工作包来源版本；按现有边界应拒绝，而非视为程序错误。
- **影响任务**：多流程同时在线办理或持续扩围；不影响原有多流程线下分析要求继续存在。
- **是否已修复**：属于有意设计的限制，不应“修复”为通配放行。
- **处理方案**：若首发要求多流程持续办理，先补启用主体、流程清单、生效、暂停、审计和旧记录可读的范围设计，作为第09阶段条件；不得轮换环境变量假装同时开放。若先单流程试点，保留现有边界，但它不能代替主链的线下验收。
- **验收方法**：当前单流程内成功、范围外拒绝；批准扩围后再验证两流程两部门并行隔离、暂停立即阻止写入且保留历史、事务内范围复核。正式范围未获批准不启用。

### F10｜范围条件：工作包完成没有发布正式数据地图

- **代码位置**：`server/processDataGovernanceRepository.js:768`的`completeWorkPackage()`锁定工作包、检查明细和事实问题、插入工作组审核、更新completed并写事件；没有正式数据地图写入。[专项设计](../../../apps/mdm-platform/docs/Process-Data-Governance-Design.md)第9节明确不包含数据地图写入。
- **触发与复现条件**：工作包完成后期待出现正式数据地图版本；当前代码不会产生该结果。
- **影响任务**：首发若承诺正式主数据清单、跨流程对象统一、正式数据地图发布，则缺少该产品闭环；仅完成工作包时不应冒充已发布数据地图。
- **是否已修复**：符合当前专项设计范围，不是已经确认的漏写缺陷。
- **处理方案**：复用已有绑定不可变版本、来源摘要、定向事实和工作组审核实现。正式地图如纳入建设，再明确结论版本、来源引用、确认主体、生效/撤回、同名不合并和未定项阻断；先评估现有模型，不用旧产品CRUD替代。
- **验收方法**：工作包与流程版本、正式地图版本分别取证；正式地图范围获确认后，验证两流程对象识别依据、不同对象同名隔离、待定不得发布、变更撤回保留历史、PMO可引用稳定事项。

## 4. 页面调用、数据来源与维护边界

下表来自当前前端字面量及调用函数、服务端注册与仓储实现，属于静态调用链，不是浏览器网络抓包。首发菜单尚未最终选定，因此分为公共依赖、V7办理链和条件入口；不能用隐藏菜单证明API已经隔离。

| 页面或触发 | 实际接口族与前端位置 | 服务端来源 | 后续处理和业务维护边界 |
|---|---|---|---|
| 登录、恢复会话、身份补全 | `/api/org/login`、`/session`、`/me`；`:7243/7264/7274`；`/api/csrf-token` | `org.js`/`auth.js`，正式分支为MySQL身份仓储；顶层SQLite和隐式初始化仍在 | 必须保留并修F01/F02/F03；admin管理账号与授权，不能确认业务事实 |
| 身份管理 | `/api/org/accounts`及账号操作、role-assignments、audit-events；`:13181`起；`/api/rbac` | `governanceAccessMysqlRepository`与固定`roleDefinitions` | 复用正式person链；旧`/org/users`写入及角色配置编辑保持停用，前端残留代码不视为有效能力 |
| 所有用户登录后的目录 | `/api/org/departments`、`/api/capabilities`、`/api/processes`；`:7291` | 部门是MySQL条件分支；能力、流程仍是SQLite `capabilities/processes`，关联该库的部门和能力记录 | 部门目录必留；旧能力/流程不属于V7正式版本源。公共加载先脱耦；是否保留旧读接口按真实业务需求确定 |
| 所有用户登录后的其余聚合 | `/api/mappings`、`/data-map/contexts`、`/todos`、`/terminology`、`/conflicts`；`:7286` | 对应mapping、dataMap、todo、terminology、conflict MySQL仓储 | 保留受支持数据合同；按权限/页面按需加载，避免无关模块失败阻断公共初始化；不得重新造第二套待办 |
| 我的工作台 | `/api/role-workbench`及`/api/activity/heatmap` | 工作台混用MySQL和SQLite；活动为`auditMysqlRepository` | 工作台必须MySQL化并补V7聚合；事项责任来自正式身份与对象关系，不从显示名推测 |
| 页面操作提示 | `/api/page-workflows`；`:6920` | `pageWorkflows.js`的身份读取可用MySQL，但待办和升级计数仍直接SQLite | 必须消除公共运行依赖，静态操作说明可保留；不让旧计数误导下一步 |
| V3编制、承接和冲突 | `/api/process-design/editor/*`、`/summary`、`/drafts/*`、`/cross-dept-handoffs/*`、`/handoff-conflicts/*`；`:2683`起 | 正式选择`processDesignMysql`；本地编辑器读取既有结构与目录；旧processDesign仅遗留 | 保留受支持V3完整JSON及revision并发，不能把V7写入旧编辑器；范围与审批不放宽 |
| V7预览和修订 | `/api/process-v7-preview/cases`、案例详情、新修订、核对、范围、提升；`:3133`至`:3494` | `processV7PreviewReviewRepository`、独立预览表及受控提升 | 复用；部门分别核对，工作组处理结构与授权发布；补工作台和页面内输入保护 |
| V7正式审核与读回 | `/api/process-design/drafts/:id/submit`、`/review-tasks/:id/decision`、`/drafts/:id/publish`、`/versions/:id/content`；`:3497`至`:3545` | 现有MySQL原生V7事务、正式版本、审核任务 | 不拆散锁顺序、事务身份复核或摘要绑定；固定版本作为下游依据 |
| 后续治理工作区 | `/api/process-data-governance/status`、`/workbench`、工作包/事实问题接口；`:3612`起 | `processDataGovernanceRepository`，绑定固定V7版本与来源摘要 | 默认关闭；启用范围待定。MDM工作组判断、部门仅回答具体事实、admin只读 |
| 流程治理快照与问题池 | `/api/process-governance/*`、`/api/process-governance/guidance/*` | processGovernance有读模型分支与SQLite顶层加载；指导意见为MySQL仓储但取得时仍初始化 | 保留现有MySQL部分，移除隐式副作用；制度/映射真源、工作组结构核验和PMO跟踪边界不变 |
| 旧业务地图及流程详情 | `/api/views/sankey`、`/api/views/processes/:id`；`:7844/10551/11694` | `views.js`直接SQLite目录、映射及字段表 | 旧图不等于多份V7正式版本组成的主链图；不能将其关闭后宣称已满足R00-01，具体承接需结合实际JSON核验 |
| 组织单元及岗位候选 | `/api/org-units`读写和activate；`:10730/12341`；实体表单`/api/positions`；`:10870` | `orgUnit.js`、`position.js`直接SQLite `org_unit/position` | 页面存在入口或候选加载；不能猜测等同于MySQL部门/岗位。首发不需要则服务端隔离；历史记录保留，真实组织维护按组织真源规则 |
| 产品族、产品 | `/api/product-families`、`/api/products`及activate/release/obsolete；`:10776/12421/12444`起 | SQLite `product_family/product`及旧实体生命周期 | 仍有页面调用，不是仅存废代码。若不在首发则隔离；若需要业务申请能力，先明确申请/审批/生效，不能直接沿用CRUD宣告完成 |
| 系统目录、分类、属性 | `/api/systems`、`/api/class-nodes`、`/api/attributes`服务端注册 | 相应路由直接SQLite | 本次在`public/`非压缩HTML/JS未找到这三族直接请求字面量；不能据此排除动态调用或其他消费者。先列隔离候选，不删除历史数据 |
| 字段与导入导出、质量 | `/api/field-entries`、`/field-identities`、`/import`、`/export`、`/quality/*` | 字段域及黄金源进度为MySQL；`quality.js:12`的dashboard仍SQLite且文件顶层加载 | 按接口拆分数据来源，不能把整个quality文件一律称MySQL或一律下线；字段审核仍由有权治理角色办理 |
| 外部标识与集成 | `/api/external/*`、`/api/integration/*`仍注册 | 对应路由顶层SQLite | 本轮没有外部接口或回写需求授权，列正式运行隔离候选；不调用真实外部服务、不删除凭据或历史同步记录 |

## 5. 局部整改方案和停止条件

### 5.1 必须保留复用

保留固定身份链及七角色十九权限、账号停用和`auth_version`机制、CSRF和既有口令约束、V3完整JSON兼容、V7预览专表及原生正式事务、稳定ID与内容摘要、已有MySQL字段/术语/待办/冲突仓储、后续数据治理工作包。既有隔离迁移与测试入口继续复用，但先核对真实依赖和副作用。

### 5.2 必须修复或隔离

| 实施阶段 | 最小改动范围 | 足够完成的判定 |
|---|---|---|
| 02 | F01/F02：正式路由注册、公共前端初始化、身份/工作台/页面提示/治理仓储取得；旧路径显式隔离 | 正式启动和代表性请求无SQLite、无DDL/角色种子、无隐式回退；必需目录和已有权限仍正确 |
| 03 | F03/F04/F05：会话、访问配置、就绪检查和3000独立运维 | 选定或隔离环境中登录、重启、数据库故障恢复均有证据；命令只影响3000 |
| 04 | F06：废弃脚本测试、管理员与业务角色用例、实际可达路由清单 | 当前角色模型正反向通过；未分类项逐条有可追溯结论；受支持V3/V7和发布原子性有对应验证 |
| 05 | F07/F08：工作台V7任务和统一页面内编辑保护 | 办理人可从待办完成任务；无错误空待办；未提交内容不静默丢失或提交 |

02不应先按不完整首发菜单一次性注销所有遗留接口。可以先完成身份、公共聚合、只读结构检查与运行配置校验等不依赖首发业务选择的部分；涉及保留旧业务读写或改变接口含义时，由D00-01及实际消费者核验限定。除身份等必要基础外，不顺带把所有旧产品和组织功能迁移一遍。

### 5.3 条件性能力与原有验收要求

- 多流程持续在线办理需要F09对应启用范围设计；单流程试点不能声称已满足常态扩围。
- 正式数据地图、统一主数据清单以及主数据业务申请分别由D00-01决定是否纳入首发；F10现有工作包不替代这些结果。
- R00-01至R00-03的主链整合、三类待办及OFF-01至OFF-10仍为已有上线前提。当前旧地图、工作台及模拟测试不能证明多份V7正式版本已按要求组成主链。实际JSON未核验时记录能力与证据缺口，不自动取消范围或伪造样本。
- 数据治理工作规则第十一节的状态表述需要与已有默认关闭实现同步；同步时保留启用、迁移、业务确认和范围门槛，不通过改措辞降低要求。本阶段登记明确修正方向，未修改该规则。

### 5.4 暂不进行的整理

不按行数拆分`processDesignMysql.js`或全面改写`public/index.html`，不更换前端框架，不引入微服务、Redis或知识图谱作为默认方案，不升级全部依赖，不整理无关历史资产。若本次函数难以隔离验证，可提取运行仓储工厂、会话/就绪配置或V7编辑状态；正式发布事务、事务内身份复核、锁顺序和并发条件保持在同一受控流程中。

## 6. 后续允许修改范围、兼容与数据库影响

以下为后续阶段的实施边界设计，不表示本轮已经实施或已经获准操作正式环境。

| 主责范围 | 可修改内容 | 兼容和禁止事项 |
|---|---|---|
| `apps/mdm-platform/server/`及`routes/` | 正式注册、仓储取得、必要MySQL接入、会话、就绪、工作台聚合 | 复用现有API与权限；受支持路径不随意改字段；不新增admin业务旁路 |
| `apps/mdm-platform/public/index.html` | 公共加载按需化、旧入口停用提示、V7待办定位与输入保护 | 保留四个流程工作区、中文提示、现有API标识；不加前端持久化或与3001运行通信 |
| `apps/mdm-platform/scripts/`及对应测试 | 显式迁移/检查、隔离验证、安全测试和盘点 | 执行前确认数据目标；不把正式库设为测试库；不恢复废弃开户脚本 |
| 根`scripts/`及`package.json` | 第03阶段所需3000独立运维入口和非敏感配置复用 | 不改变PMO/3001运行；不默认停止端口占用者；不重复建私有配置真源 |
| 应用README、相关规格、治理规则与本目录 | 同步确实变化的行为、状态、命令及证据 | 不批量改历史文档，不自动接受ADR，不改组织或业务责任事实 |

当前确定的数据库设计方向：运行检查不需要新业务模型；会话持久化若采用MySQL，需要独立会话表迁移和清理策略。身份结构与M1/M2复用已有迁移，不能因F02而重建。旧SQLite接口若必须MySQL化，先明确已有记录、ID/引用、MySQL目标能力和映射方案；不直接用同名表或默认值替代。正式地图与扩围表结构尚未设计，必须随范围决定形成迁移、兼容、失败处理和验收样例。

旧SQLite文件和其中记录、MySQL草稿与发布版本、审批历史、原始JSON及稳定引用均保留。停止注册旧接口不等于已迁移数据；如改为只读兼容，应有明确独立入口、维护主体和可追溯说明，不作为正式运行回退。

## 7. 本阶段完成证据和下一停点

已完成当前提交与3000改动核对、F01至F10静态审查、页面/接口/数据来源对应、只读路由盘点、整改分组及后续兼容边界。应用整改、完整安全测试、真实MySQL/浏览器/运行/业务验收均未执行，未关闭任何上线阻断项。

本阶段在报告方案后停止。下一阶段为02；其不依赖D00选择的本地整改可按用户继续指令推进，具体首发业务入口取舍、正式人员与材料、发布环境及扩围仍保持待定。完整交接见[04-执行交接](04-执行交接.md)。


## 8. 第02阶段实际整改与证据（2026-09-09）

### 8.1 当前结果

第02阶段已完成本地代码、维护说明及隔离验证，结束后停止，未进入03。F01/F02的应用运行路径已整改；真实MySQL最小权限、完整结构漂移和恢复证据仍未取得，不能据此宣告正式上线P0全部关闭。D00-01至D00-03、主链地图、三类待办及OFF-01至OFF-10原要求保持不变。

本轮先读取项目及3000规则和四份交接文档。用户所写嵌套提示词路径不存在，使用执行基线登记的实际文件`C:/Users/charl/Downloads/Infomat_3000上线系列提示词_2026-09-09.md`，SHA-256仍为`38C02D6C1C7BC6F37916BFEFE10132AC50473FC526BB608C2BF50865935003C6`。仅执行其中02。

### 8.2 实际变更

| 对应项 | 已实现内容 | 兼容与限制 |
|---|---|---|
| F01：正式注册与配置 | `runtimeBoundary.js`在应用监听前检查两种读模型和必要MySQL变量；production禁止遗留模式，正式配置不接受读模型缺失或artifact回退。旧接口族通过鉴权后返回410 | 不据此确定首发菜单；没有新建替代产品、组织或流程模型。正式模式不加载旧路由模块 |
| F01：公共加载 | 登录公共加载只取运行能力与MySQL部门；各业务模块按页面读取。旧页面暂停并说明已有资料保留；质量页仍读取MySQL字段身份进度 | 保留现有页面/接口标识及四个流程工作区，没有取消主链地图要求 |
| F01：首页与页面提示 | 待办与升级冲突复用既有MySQL仓储和权限范围；工作台上下文由流程治理MySQL仓储按固定快照及部门读取。未知部门不读取全局上下文，同名A1定位限制到所属部门 | 不复制待办模型；未分派待办不因部门为空自动对部门用户可见；本轮未补齐F07的V7办理任务聚合 |
| F01：SQLite保护 | `org/access/roleWorkbench/pageWorkflows/processGovernance/quality`改为仅在遗留分支延迟取得SQLite；`db.js`拒绝production或非遗留MySQL运行访问 | 测试助手显式使用临时库。共享`data/platform.db`不能作为遗留服务启动目标。历史文件未读取、删除或移动 |
| F02：仓储取得 | 原有运行工厂的`initSchema()`全部替换为必要表列的只读探测；流程草稿工厂移除四个补结构调用及承接、统一治理自动迁移 | 初始化方法仍保留给显式维护和既有测试；未改变V3/V7JSON、稳定ID、状态流转、正式事务、锁顺序和摘要条件 |
| F02：故障与维护 | 探测失败关闭失败池并允许下次重新取得；缺表、缺列、连接或访问错误返回503，不回SQLite或磁盘审核材料；表单补齐纳入已有`init:mysql`显式入口 | 只读探测覆盖所列依赖表的基础列，未核验全部类型、索引、外键和迁移记录；完整漂移仍用既有迁移检查 |
| 文档与权限 | 应用README、Tech-Spec和脚本README同步命令、副作用、运行/迁移账号权限以及遗留数据去向 | 运行账号不需DDL或固定权限模型种子写权限；真实账号、授权和数据库均未操作 |

历史接口涉及的原记录仍保存在原SQLite文件中，具体维护接续由项目负责人确认。是否承接及如何保留ID/引用需按D00-01和真实消费者另行确定。现有MySQL身份、部门、字段、术语、映射、待办、冲突、治理和V3/V7正式版本链继续复用。停止旧入口不能视为历史数据迁移完成。

### 8.3 已执行验证

所有命令从`apps/mdm-platform`执行，运行环境为本机Node v25.2.1；未安装或升级依赖。真实数据库和正式服务均未调用。

| 检查 | 实际结果 | 证据边界 |
|---|---|---|
| `npm run test:mysql-runtime-boundary` | 通过：production模块在禁止SQLite加载及启动建池条件下加载；配置缺失/不一致和共享遗留路径拒绝；正常合成身份、部门、首屏和治理请求成功；管理员业务写入403；V7关闭返回明确禁用；缺表/缺列/断库返回不可用且恢复后可重试 | SQL监测替身及合成身份，随机回环端口；不是真实MySQL账号权限或事务证明 |
| `npm run test:frontend` | 通过 | 静态资产合同；原先“先加载完整看板”的断言已按公共初始化变更同步 |
| `npm run test:project-roles` | 通过 | 七角色十九权限及admin业务只读静态合同 |
| `npm run test:role-workbench`、`npm run test:role-workbench-mysql` | 通过 | 合成仓储及遗留临时库。新增MySQL依赖显式注入，不使用默认连接 |
| `npm run test:process-governance` | 通过 | 已有MySQL模拟仓储、V3兼容、V7预览及迁移合同、统一治理、问题池等聚合；未连接正式库 |
| `npm run test:mainline` | 通过 | 包含模拟MySQL及临时SQLite兼容烟测；人员接口断言改为显式遗留模式，正式模式410由新测试单独证明。没有修复或宣称通过第04阶段的整套安全盘点 |
| 最终受影响复验与差异检查 | 运行边界、工作台、待办及前端复验通过；JavaScript语法和`git diff --check`通过 | 仅复验最后调整的故障响应、字段上下文和测试说明；最终文件摘要见证据清单 |
| Edge浏览器 | 合成用户登录首屏、历史产品暂停提示、质量页字段进度检查通过；1699×828及900×720无横向溢出；控制台0错误，localStorage/sessionStorage均0 | 真实Edge、100%缩放、合成空业务数据与随机隔离端口；未验证真实人员、完整V7编辑、IME或业务办理；临时服务已停止 |

本机证据集中保存在[stage02-20260909](../../../artifacts/mdm-3000-launch/stage02-20260909/)：`baseline.json`记录开始时32个未提交文件的摘要；`validation-evidence.json`记录最终文件摘要、命令及保护核对；对应`test-*.log`保存退出结果。浏览器证据为`workbench-edge.png`、`legacy-entry-edge.png`、`browser-network.txt`和`browser-console.txt`。该目录被Git忽略，不含真实业务数据或真实凭据。第01阶段原始证据继续保留。

### 8.4 本阶段未执行及下一停点

没有连接正式或共享数据库，没有启动/重启3000、3001或PMO正式服务，没有修改真实账号或开关，没有迁移、发布、暂存、提交或推送。应用代码为未提交工作区结果，不能将HEAD视为已部署版本。

F03至F05的会话、HTTPS、就绪接口和独立3000运维属于03，尚未实施。F06安全测试盘点、F07完整V7待办、F08未提交意见保护、F09扩围和F10正式地图仍按原阶段及待决范围推进。尤其`/api/health`仍不能证明MySQL就绪，共同开发启动脚本仍包含初始化和PMO操作，本轮没有执行。

第02阶段在本地交付和隔离验证后停止。下一阶段仅在用户明确交付03后开始。

## 9. 第03阶段实际整改与证据（2026-09-10）

### 9.1 基线与当前结果

用户本轮明确授权仅执行03。开始时记录78个未提交文件，核对第02阶段证据中的50个文件，全部哈希匹配。继续保留第01、第02阶段原证据，复用未受本轮变化影响的前端、流程治理与主线检查，没有重新停留在方案层面。HEAD仍为`dec8996c375d780edc0106a4c32584d2dd664944`，本次修改在工作区，不是已发布提交。

用户消息中的嵌套提示词路径经本次文件系统读取仍不存在；实际文件为`C:/Users/charl/Downloads/Infomat_3000上线系列提示词_2026-09-09.md`，SHA-256为`38C02D6C1C7BC6F37916BFEFE10132AC50473FC526BB608C2BF50865935003C6`，与执行基线相同。仅按其中03及用户本轮明确范围执行，没有进入04。

### 9.2 实际变更

| 对应项 | 本轮已实现 | 兼容与限制 |
|---|---|---|
| F03会话 | 使用已有mysql2和express-session实现MySQL会话仓储；专用Cookie、保存成功后才返回登录成功、滑动过期、重启保留、退出删除、实时身份/授权版本校验 | 不新增Redis或依赖；旧connect.sid要求重新登录。会话只保存身份及必要安全元数据，业务历史保持不变 |
| F03访问 | production必须配置HTTPS origin和可信IP/CIDR；不关闭Secure、不无条件信任代理头；首次改密可获取CSRF令牌 | 实际主机、代理来源、证书和网络隔离仍待正式环境核定 |
| F03维护 | 会话表独立迁移、结构匹配检查、重复执行、建表后中断续接、过期分批清理及非空回退拒绝 | 运行请求不建表、不补列、不批量清理。真实库会话表未准备 |
| F04存活/就绪 | 保留/api/health，新增/api/ready；限时只读检查配置、数据库、基础表列及会话结构，按已启用功能追加依赖；缓存和合并探测控制成本 | 缺结构、断库、错配置不报就绪；基础业务探测不是完整迁移、全部写权限或业务验收。关闭功能不成为强制结构前提 |
| F04版本 | health和ready报告启动源码摘要、package版本及追溯HEAD | 源码摘要体现工作区实际文件；HEAD不冒充部署版本 |
| F05运行 | 应用内service:start/stop/restart/check/supervise复用非敏感固定配置，核对进程入口、PID创建时间、监听、源码、就绪与首页 | 不调用共同开发脚本，不操作PMO、3001、数据库容器或其他进程；Secret由运维安全注入 |
| F05恢复 | Windows监督进程、有限异常重启、IPC受控停止、监督连接断开后停止、生命周期日志轮转、重复监督拒绝 | Windows开机任务和过期清理任务仅有操作办法，未注册或开启；正式维护身份和注入方式待定 |

详细变量、准确命令、权限、副作用及故障处理见[03-发布与恢复](03-发布与恢复.md)。已同步应用README、Tech-Spec、应用脚本README、根scripts说明和术语定义；根共同启动脚本及固定非敏感配置未修改。

### 9.3 验证范围

| 验证 | 结果与实际边界 |
|---|---|
| `npm run test:stage03-runtime` | 配置拒绝、精确维护目标、就绪并发合并/缓存/超时取消/恢复、可选依赖及PID归属负向检查；纯合成环境和SQL替身，不连库 |
| `npm run test:stage03-mysql-isolated` | 自建带唯一标记的MySQL 8.4容器，tmpfs数据，无绑定或已有数据卷，随机回环端口；合成身份和专用运行账号，运行账号无DDL权限。验证迁移/结构漂移/中断/回退、会话HTTP与真实应用进程重启、Cookie和TLS代理、授权版本变化/停用/首次改密/退出/过期、保存失败、必要结构缺失、数据库暂停和恢复 |
| `npm run test:stage03-service` | Windows临时合成应用与随机端口的真实进程验证；其他程序占用端口时拒绝、PID/入口核对、重复监督拒绝、源码变化拒绝、受控停止/重启、异常退出恢复和日志轮转 |
| `npm run test:mysql-runtime-boundary` | 受本轮入口变化影响的第02阶段边界复验；继续阻断SQLite加载、启动连接和DDL，保留代表性请求及admin业务写入拒绝 |
| `npm run test:identity-mysql` | 身份仓储、登录/会话、只读角色、权限、访问范围及停用批量入口回归；使用既有隔离替身。旧登录替身缺少save导致首次失败，补齐测试合同后通过，没有恢复正式会话绕过 |
| `npm run test:rbac-raci-v2` | 固定角色、身份API和迁移合同的受影响回归；不重做第04阶段完整安全盘点 |

各命令最终退出结果、日志摘要及实际文件哈希统一记录于[validation-evidence.json](../../../artifacts/mdm-3000-launch/stage03-20260910/validation-evidence.json)，不能将本表作为超出该证据范围的正式环境通过证明。第02阶段的完整流程、前端和主线证据按未受影响范围复用。

隔离数据库与证书只由本轮测试创建；测试删除容器前核对唯一标记，只删除本轮容器。Windows进程测试只对自己的临时应用操作，结束后关闭临时监听并清理临时文件。没有读取真实Secret、连接正式或共享数据库、启动或重启正式3000、修改真实账号、开启试点、发送通知、发布、暂存、提交或推送。

### 9.4 停止点与剩余限制

F03/F04/F05已完成本地实现和本阶段隔离验证；正式库会话迁移、正式HTTPS及代理网络、实际运行账号完整权限、开机与清理任务、业务并发容量、主机重启恢复及真实人员体验仍待对应环境证据。不能称为上线P0全部关闭或业务验收。

D00-01至D00-03仍待定。F06完整安全测试和路由分类、F07完整V7待办、F08未提交意见保护，以及第06阶段正式业务数据与V7事务恢复等均未在本轮执行。本阶段完成后停止，不进入04。

## 10. 第04阶段实际整改与证据（2026-09-10）

### 10.1 当前结果与基线

第04阶段已完成F06范围内的本地代码、测试入口、路由盘点、维护说明和隔离验证，完成后停止，未进入第05阶段。`npm run test:launch-stage04`最终52项全部通过，其中51项为静态检查、显式仓储替身或自有临时SQLite测试，1项为真实隔离MySQL 8.4.9与真实应用HTTP综合验证。聚合后增强路由证据追溯及“撤权但账号仍启用”用例，分别完成`npm run test:security`4项和`npm run test:stage04-mysql-isolated`定向复验，没有重复运行未受影响的全套测试。

开始时记录92个未提交文件。HEAD仍为`dec8996c375d780edc0106a4c32584d2dd664944`，分支`master`；本地成果未提交，HEAD不是部署版本。第03阶段证据中的25个文件全部与开始时工作区摘要一致；第02阶段50个文件中40个一致，其余10个变化均由第03阶段记录覆盖。本轮只在当前授权目标增量修改，3001及其他无关未提交资产的保护核对见最终证据。

实际提示词文件仍为`C:/Users/charl/Downloads/Infomat_3000上线系列提示词_2026-09-09.md`，SHA-256再次核对为`38C02D6C1C7BC6F37916BFEFE10132AC50473FC526BB608C2BF50865935003C6`。本轮仅执行其中第04阶段及本次明确补充范围。第8、9节保留各阶段当时的停止点，当前续接以本节及[执行交接](04-执行交接.md)为准。

### 10.2 复现与实际修复

| 对象 | 当前复现/原因 | 已实施与验证 |
|---|---|---|
| 废弃建号测试 | 先审查命令、副作用和连接入口，再在净化环境与自有临时库复现旧`test:security`首项失败：测试仍要求已退休脚本成功 | 两个废弃脚本分别覆盖已存在/不存在数据库共4场景，要求退出1及`LEGACY_ACCOUNT_SCRIPT_RETIRED`，阻断DB/Excel模块加载，核对既有数据库摘要不变且无新文件。废弃脚本本身未恢复 |
| 安全角色矩阵 | 旧测试使用已停用组织写入及自定义角色权限，不能代表正式固定模型 | 真实Express、session和CSRF配合显式合成仓储：保留有权角色的分派、部门办理及备注成功，保留admin、无权、跨部门、未登录、首次改密、停用、授权版本变化和旧Cookie拒绝。停用接口分别验证410/明确只读，未把全部成功断言改为403 |
| 质量单/映射待办备注 | 两个接口原来只检查可见性，admin依靠全局读取权限即可写备注；当前HTTP复现200而预期403 | 两个`comment`路由新增相应业务权限检查，保留原部门/参与可见性。合成业务角色正常留言；admin及跨部门拒绝且仓储零写 |
| 一般冲突协调结果 | 接口原来只依赖历史指派检查，已无处理权限的人仍可能提交；当前HTTP复现200而预期403 | 增加当前`governance:handle-assigned-conflict`权限检查，保留仓储协调状态和当前指派校验。有权且被指派成功，仅有权限、仅有历史指派或admin均拒绝 |
| 旧HTTP测试替身 | 第03阶段鉴权要求`accountId/authVersion/validateSession`，7个旧路由测试仍只设置userId，产生鉴权失败 | 通过显式登记合成身份的`syntheticSession`助手补齐测试合同。运行时鉴权未放宽，不启用遗留会话旁路；真实登录与撤权另外由MySQL HTTP验证 |
| 可重复入口 | 原分散测试不能单凭命令名称证明隔离 | 新增`test:launch-stage04`和`test:stage04-mysql-isolated`，`test:security`接入同一净化运行器；逐脚本记录源码/日志摘要、退出码和真实依赖。模拟子进程及其继承环境阻断真实MySQL，真实步骤仅自建标记容器 |

本轮应用改动仅涉及上述3个写接口的权限收紧；没有改变schema、字段、状态机、枚举、稳定ID或V3/V7数据格式。已有备注、协调结果、指派关系、审核和版本历史全部保留，不执行删除、回填或猜测迁移。管理员继续负责身份管理，对治理业务只读。

### 10.3 路由逐项追溯

在本轮开始时重新运行旧盘点，仍为216条记录、92条未分类、退出1。重写后的盘点加载正式Express注册但禁止监听、真实建池及SQLite，核对实际router stack与捕获条目一致，识别多行声明、`all`及动态问题池动作。结果为静态声明220条，动态5个动作相对模板净增4条，共224条追溯记录；其中正式注册161条，隔离遗留63条。数量是盘点口径，不是漏洞数或业务动作数。

| 当前分类 | 数量 | 结论边界 |
|---|---:|---|
| 身份写入 | 8 | 当前身份管理权限、状态、授权版本及访问事件；不产生治理业务权力 |
| 业务写入 | 127 | 逐项定位权限、部门/参与范围、对象状态、仓储方法、并发及审计线索 |
| 明确停用 | 13 | 正式注册但前置终止处理器拒绝写入 |
| 前置停用遮蔽的旧实现 | 7 | 同路径后方实现不可达；不能误报为正式业务写入 |
| 公共/本人服务 | 3 | 登录、退出、本人改密；不是业务写权限 |
| 内存校验/差异预览 | 3 | POST只承载校验或差异计算，未持久化业务行 |
| 隔离遗留 | 63 | 正式不加载，SQLite历史资产保留；流程设计由既有MySQL模块承接 |
| 未分类/未追溯注册 | 0 | 检查失败条件为出现任何未追溯项；本结果不代替权限或业务验收 |

旧92条全部按文件、方法、路径、重复声明序号逐项关联；动态模板展开到实际5个动作。每项保存源码位置与摘要、权限helper、仓储方法，以及权限、部门、对象状态、并发和审计五类线索，不能以整组白名单代替检查。可读索引见[route-write-inventory.md](../../../artifacts/mdm-3000-launch/stage04-20260910/route-write-inventory.md)，完整逐项证据见[route-write-inventory.json](../../../artifacts/mdm-3000-launch/stage04-20260910/route-write-inventory.json)，历史对照见[legacy-92-reconciliation.json](../../../artifacts/mdm-3000-launch/stage04-20260910/legacy-92-reconciliation.json)。

盘点明确保留既有局部接口的限制：旧分项编辑没有统一客户端修订令牌；部分术语动作只有状态/审核字段，没有独立全量变更事件；通用待办的事件与写入不保证同一事务。第04阶段没有把这些旧接口整体改成V7模型，亦没有以分类通过宣称其并发或原子审计已验收。D00-01首发范围仍待定，若这些接口纳入首发，须按具体业务失败影响确认并验证其一致性要求；本轮V7事务证据不覆盖它们。

### 10.4 实际验证与依赖

所有命令从`apps/mdm-platform`执行，Node v25.2.1，未新增或升级依赖。运行器只保留必要系统环境，不读取私有env、不继承真实数据库/会话/试点配置。真实测试用`--pull never`新建带唯一标记的MySQL容器，tmpfs数据、无绑定目录和已有卷、随机回环端口；应用运行账号只授予SELECT/INSERT/UPDATE/DELETE。合成结构准备、迁移和故障触发器仅在本轮容器内实施，未使用正式或共享数据库。

| 检查 | 实际结果 | 证据边界 |
|---|---|---|
| `npm run test:launch-stage04` | 52/52通过；覆盖安全、固定角色/身份、流程治理、冲突、映射、字段、后续治理、运行边界及真实MySQL HTTP | 前51项是静态、替身或自有临时SQLite；最后1项使用真实隔离MySQL 8.4.9。不是52项真实数据库测试 |
| 最后`npm run test:security` | 4/4通过，包含最终路由追溯改动 | 在完整聚合后仅追溯工具变更，定向复验，不重复未受影响检查 |
| 最后`npm run test:stage04-mysql-isolated` | 真实MySQL HTTP综合验证再次通过，独立补强角色撤回但账号仍启用的场景 | 只定向复验受用例变化影响的真实隔离部分 |
| 真实身份安全 | 未登录/CSRF/admin/无权拒绝；首改密弱密码拒绝、改密后正常办理；停用后旧会话401；另一账号撤回角色后保持active、auth_version递增，旧会话401，重新登录正常；停用/撤权事件分别存在 | 7个合成账号、3个合成部门，真实登录/session/身份仓储，无实际人员或真实账号 |
| V7预览与核对 | 预览不写正式三表；重复上传幂等；分别部门核对、跨部门读/写拒绝；受控提升、提交及审核退回 | 合成V7材料、精确合成process_ref，未开启真实试点 |
| V7修订与再办 | 差异预览不写，受影响核对项重开；两个并发修订201/409；旧提交/审核摘要409；双方重新核对、再次提升并提交审核成功 | 真实MySQL仓储及HTTP，不是伪session或直接调用业务写方法 |
| V7发布事务 | 发布事件插入时故障返回500，预览/正式业务表内容摘要及数量与发布前一致；撤除故障后并发发布200/409，重复发布409 | 临时数据库触发器在事务后部注入错误，证明本例版本、草稿、主档和事件一起回滚；不代表进程断电或全库恢复 |
| 不可变版本及开关 | 按`process_version_id`完整读回JSON并核对摘要；跨部门读取403；关闭开关503及范围外403，业务表零变化 | 只重启自建临时应用并调整其合成环境；正式服务和开关未操作 |
| V3兼容 | 合成历史V3版本原文完整读回，业务表不变；既有V3迁移/编辑合同聚合通过 | 没有读取真实历史库或备份，不是第06阶段历史数据迁移/恢复验收 |
| 前阶段与前端 | 复用第03阶段未变更的HTTPS、就绪、会话维护及Windows运维证据；本轮聚合含受影响鉴权和第02阶段运行边界回归 | 未运行浏览器；没有实施第05阶段待办聚合、未提交意见保护或人工IME/焦点验收 |

原始失败记录包括旧建号断言、两个备注越权和协调权限缺失的先失败证据。开发验证中的其他失败来自测试准备：旧会话替身不满足新鉴权合同、合成角色生效日/预览字段/核对项选取不符、触发器使用不支持的prepared执行方式、角色关系主键名称以及Windows预加载路径写法。已按真实合同纠正并复验，没有用skip绕过。完整命令最终退出0；后续安全专项也退出0。

完整52项日志及脚本摘要见[聚合test-results.json](../../../artifacts/mdm-3000-launch/stage04-20260910052822652/test-results.json)；最终安全专项见[安全test-results.json](../../../artifacts/mdm-3000-launch/stage04-20260910054045922/test-results.json)，最终真实MySQL定向结果见[mysql-http-final.log](../../../artifacts/mdm-3000-launch/stage04-20260910/mysql-http-final.log)。基线、先失败证据、最终源码/文档摘要、文件保护核对和证据索引统一见[第04阶段validation-evidence.json](../../../artifacts/mdm-3000-launch/stage04-20260910/validation-evidence.json)。该目录被Git忽略；日志只含测试合成数据，不含真实凭据。

### 10.5 停止点与剩余限制

第04阶段已授权本地工作完成并停止，不进入05。D00-01至D00-03、主链地图、三类待办、OFF-01至OFF-10、正式部署拓扑、真实办理人员及首批材料继续待定。F07完整V7待办、F08未提交意见保护未实施，不用会话持久化或既有工作包页面替代。

第03阶段真实MySQL证据只覆盖合成基础数据与会话；本阶段新增合成V3/V7事务证据，但仍未覆盖真实历史数据、正式迁移状态、备份恢复、真实主机恢复、实际并发容量、浏览器人工体验或业务验收。第06阶段仍须按当时具体授权取得对应证据。本轮未连接正式/共享库，未操作正式服务或真实账号，未开启试点、通知、发布、暂存、提交或推送。当前状态是“本地已实现、隔离验证通过”，不是“正式已开启”或“业务已验收”。

## 11. 第05阶段实际整改与证据（2026-09-10）

### 11.1 范围与基线

本轮只执行第05阶段F07、F08及用户补充的实际办理、输入保护、角色说明和隔离浏览器验证要求，完成后停止，不进入06。开始时HEAD仍为`dec8996c375d780edc0106a4c32584d2dd664944`，分支`master`，记录111个已有未提交文件摘要。第04阶段27个证据源码全部匹配；第03阶段25个中18个匹配，第02阶段50个中38个匹配，其余为后续阶段已有改动，不能误判成成果丢失。

提示词真源为已核对的扁平文件`C:/Users/charl/Downloads/Infomat_3000上线系列提示词_2026-09-09.md`，SHA-256=`38C02D6C1C7BC6F37916BFEFE10132AC50473FC526BB608C2BF50865935003C6`。本轮使用第05阶段，不将路径写成嵌套目录。主责仍为`apps/mdm-platform/`及上线文档，3001和其他既有资产保持原状。

### 11.2 F07：V7实际办理待办

角色工作台现在从V7案例、当前核对项、提升记录、正式草稿及审核任务读取实际事项，接入原聚合机制；没有新建通用任务引擎或持久任务表。

| 事项 | 来源与完成后的行为 |
|---|---|
| 部门预览核对 | 使用当前核对项ID和本部门核对状态；归口、承接部门分别办理。确认后本部门待办消失，待补证据仍保留；修订影响本项时重新出现 |
| 退回修改 | 使用部门已保存的退回依据或正式审核意见，明确原案例和当前修订；编制人员在3001修改后上传到同一案例 |
| 归口与范围 | 使用原归口状态和未解除的范围卡口；已记录且解除卡口的范围决定不再留在待办 |
| 提升与提交 | 核对完成后由组长明确选择正式主档并提升，随后由归口部门主对接人提交；同修订已提升时不再显示重复提升任务 |
| 正式审核 | 使用当前审核任务ID、草稿修订和摘要；归口部门审核员可找到任务及未满足的条件，结论不预选 |
| 待发布 | 使用当前草稿及已通过的审核记录；缺少前置条件时指向案例核对，实际发布动作仍受现有服务端门禁；发布成功后任务消失，固定版本可完整读回 |

多角色合并保留来源角色和所需权限，按原部门范围、状态和精确流程配置筛选；管理员即使叠加业务角色仍只读。深链接定位当前案例，部门项进一步定位意见框，正式阶段指向对应修订和审核/版本记录。详情增加只读`handling_summary`，不改变旧字段、写请求或V3/V7正文，旧客户端可忽略新增字段；兼容说明已同步接口文档。

可变MySQL工作台取消15秒响应缓存，并返回`no-store`。已启用来源失败时返回503，前端显示“暂不可用”并清除旧办理动作，允许重试，不能伪装成“没有待办”。正式功能已启用时，缺正式表也不再被降为无正式记录。固定角色说明仍可缓存，服务端权限仍是每次写入的最终检查。

### 11.3 F08：页面内未提交输入保护

沿用`public/index.html`、现有视觉及四个流程工作区。V7核对依据/结论、归口选择、范围依据/决定、正式主档选择字段、正式审核意见/结论统一绑定当前人员、案例和修订；核对项使用既有稳定键关联。输入只保留在当前页面内存，不自动发送，也不使用长期浏览器存储或新增服务端草稿。

保存只清除该提交组中与请求快照一致的字段；同页其他项、以及请求发出后新输入的值继续保留，并恢复焦点、选区和滚动。切换案例/工作区、上传和退出先明确处理未提交内容；取消保留原文。上传仅在成功后放弃先前明确同意放弃的输入，上传期间后续输入仍保留并要求核对新修订。退出等待期间禁用V7编辑控件，失败恢复原控件和输入。

浏览器刷新/关闭使用原生离页提示；取消时保留，明确离开后不保证恢复页面内存。真实401后保持原页面，由原账号重新登录恢复；其他账号不能继承原意见，身份切换后清除旧案例呈现。409保留旧修订意见，必须读取并核对当前修订后明确继续；移除或失去编辑权的项保留只读原文，不猜测映射到其他项。网络异常显示“未确认成功”，提示恢复后先核对服务器结果，不自动重试或承诺零写。

页面同步说明退回原因、下一办理角色和前置条件。正式审核结论为空起始值，未替部门补写意见或自动认定主数据。角色手册写明第一步、办完条件及退回路径，附合成操作截图。

### 11.4 实际缺陷、修复与兼容

- 真实Edge正常页面并发曾触发会话池满池503。会话池现最多允许32个等待请求，仍保持原默认2连接和整体超时；取得连接超时后的迟到连接销毁，业务提交不自动重试。就绪池仍不排队；无新增变量、迁移或会话格式。运行及恢复说明已同步。
- 实际检查修正了已提升修订仍出现提升编辑器、已解除范围仍留在待办，以及未提交字段在重绘、上传成功和身份切换时的处理缺口。退回任务按双方分别保存的状态判断，避免一方“存在分歧”的聚合状态遮住另一方“需要修改”；真实HTTP保留混合状态断言。以上均有后续定向HTTP或浏览器验证。
- 相关主线聚合发现9个旧路由测试仍使用不满足第03阶段合同的会话替身。数据地图、字段、术语、映射、待办及活动等测试接入已有`syntheticSession/validateSyntheticSession`，原正反向断言保持。另修正前端测试依赖固定字符截取长度的问题。未恢复旧建号、放宽真实鉴权或用skip绕过失败。
- 数据库表、稳定ID、状态枚举、V3/V7格式、不可变版本和正式事务机制不变；没有迁移、删除、清空或猜测回填历史业务内容。前阶段未变更检查按有效范围复用，受本轮运行与界面变化影响的部分定向复验。

### 11.5 验证结果与证据边界

Node v25.2.1，Microsoft Edge 152.0.4191.66。真实验证仅在新建的带唯一标记tmpfs MySQL 8.4和随机回环HTTP端口执行，运行账号无DDL权限；基础结构、合成身份、故障设置及清理均限定本轮自有实例。没有读取私有配置、连接现有数据库或执行共同启动脚本。

| 检查 | 结果与证据 | 不能据此推定的事项 |
|---|---|---|
| 相关回归聚合 | 去重67项：初次58通过、9失败；修正旧会话替身后9项全部通过，合并有效证据67项通过。涵盖前端、固定角色、流程治理、主线、角色工作台、会话及MySQL边界 | 这些是静态、替身或自有临时SQLite检查，不是67项真实MySQL业务测试；不伪称整套修改后又重跑一次 |
| 最终定向检查 | 前端、V7、3个角色工作台、会话运行及受控中文术语共7项通过 | 只证明受最终代码/文案变化影响的部分 |
| 真实MySQL待办 | 7组通过：12并发会话读取、部门与多角色/admin范围、完成即消失/退回/修订重开、提升/提交/正式退回、发布后消失、范围解除后消失、缺表503及恢复 | 合成内容与身份；不是正式容量、历史恢复或用户验收 |
| 真实Edge主要路径 | 14组通过：上传、双方部门核对、正式退回、新修订与范围、再次提交/审核、发布及固定版本读回；工作台来源角色、任务定位、失败显示与重试 | 浏览器连接本轮真实HTTP/MySQL；不是实际人员使用或真实试点开启 |
| 未提交与交互 | 同页另一项保存、真实案例切换、工作区切换、页面刷新、原生刷新/关闭取消、退出取消/失败、上传取消与请求期间新输入、真实401、真实部门409、正式审核409注入及网络失败均有断言；保留焦点、键盘路径、滚动，未捕获浏览器异常为0 | 正式审核409和部分故障为注入，不混写为正式环境故障；没有人工中文输入法组合输入验收 |
| 视口与截图 | Edge100%，桌面1699×828 CSS像素；窄视口390×844，部门可编辑表单及管理员只读页均检查，无页面级横向溢出，表格保留自身横向滚动 | 不是所有终端与缩放组合；10张截图均为合成业务 |
| 会话真实定向复验 | 第03阶段独立MySQL脚本再次通过：迁移保护、低权限运行、TLS/Cookie、登录、应用重启、撤权/停用、首改密、退出/到期、缺结构及断库恢复 | 本轮临时实例的会话/基础结构复验，不覆盖真实历史业务或正式部署拓扑 |

回归原始记录见[67项首次结果](../../../artifacts/mdm-3000-launch/stage05-20260910064736191/test-results.json)、[9项修正后结果](../../../artifacts/mdm-3000-launch/stage05-20260910065442180/test-results.json)、[最终7项定向](../../../artifacts/mdm-3000-launch/stage05-20260910/final-targeted-results.json)。真实结果见[MySQL待办7组](../../../artifacts/mdm-3000-launch/stage05-20260910/mysql-workbench-results.json)、[Edge14组](../../../artifacts/mdm-3000-launch/stage05-20260910/browser-results.json)及[会话MySQL定向日志](../../../artifacts/mdm-3000-launch/stage05-20260910/session-mysql-regression.log)。失败与纠正过程保留在本阶段证据目录，旧失败文件不代表当前结果。

合成截图和逐角色操作见[使用手册第11节](../../../apps/mdm-platform/docs/role-based-usage-guide.md#11-v7实际办理与未提交意见)。基线、最终源码/文档摘要、复用关系、资源清理及既有文件保护统一见[第05阶段validation-evidence.json](../../../artifacts/mdm-3000-launch/stage05-20260910/validation-evidence.json)。证据目录被Git忽略，不是部署包。新增入口为`test:launch-stage05`、`test:stage05-mysql-isolated`及`test:stage05-browser`；准确依赖和副作用见应用脚本README。

### 11.6 停止点与剩余限制

第05阶段已授权本地代码、测试、说明和隔离验证完成，停止，不进入第06阶段，也不继续美化或扩大范围。未启动/重启正式服务，未修改真实账号、开启真实试点、发送通知、发布、暂存、提交或推送。当前成果仍在未提交工作区；HEAD不是部署版本。

D00-01至D00-03、主链地图、三类待办、OFF-01至OFF-10、正式部署拓扑、实际人员、首批材料及其他环境待验事项继续如实保留。中文输入法组合输入、真实历史数据、正式迁移状态、备份恢复、正式容量、开机恢复与业务验收仍未完成。第03阶段只覆盖合成基础数据和会话，第04阶段补充合成V3/V7事务，本阶段补充实际浏览器及待办；这些证据均不能代替第06阶段授权范围内的真实环境验收。

## 12. 第06阶段实际整改与证据（2026-09-10）

### 12.1 范围、基线与复用

用户单独授权本阶段必要的本地代码、测试、查询/报告模板、维护说明和自有隔离MySQL合成验证，完成后停止。正式或共享数据库只读、真实备份、账号、正式启停、迁移、试点及Git写操作均未授权，本轮没有执行。

已读取根及3000主责AGENTS、scripts/AGENTS、上线00至04及第11节、迁移说明；扁平提示词SHA-256与用户提供值一致。HEAD仍为`dec8996c375d780edc0106a4c32584d2dd664944`，开始时129项未提交状态，已保存文件摘要基线。第05阶段30个证据文件全部匹配；第04阶段20/27、第03阶段16/25、第02阶段35/50匹配，其余为后续阶段已有修改，不能认定前阶段成果丢失。F07/F08按第05阶段已实现结果保留。

现有MySQL结构、M0/M1/M2/会话迁移函数和正式路由继续为唯一真源。新演练复用第04阶段真实登录、CSRF、持久会话和公开HTTP场景，未建平行模型。旧M0 CLI会加载私有固定配置，旧备份脚本固定真实源，旧isolated入口默认读取历史备份，故本轮仅复用安全函数，不运行这些默认入口。细项见[发布与恢复第10节](03-发布与恢复.md#10-第06阶段迁移核对与恢复)。

### 12.2 实际修复与兼容

| 本轮发现 | 实际处理与影响 |
|---|---|
| M2登记存在但缺索引时apply自动补建 | 真实MySQL先复现“应拒绝而未拒绝”。现要求已登记结构完整匹配才幂等返回，缺结构拒绝；匹配重复应用不再写迁移登记 |
| M2列默认值及索引前缀等漂移核对不足 | 核对默认值、扩展属性、索引完整列、类型、可见性和方向；实际默认值及前缀漂移注入均零写拒绝 |
| 未登记M2结构已有业务使用仍可能被认领 | 统计V7主档引用、草稿、版本、审核绑定及提升；非空停止，不补登记；dry-run同步表示不能应用 |
| 回退可能先删提升表，再在旧V3空值转NOT NULL时失败 | 任何DDL前核对登记、结构、V7使用及历史投影空值；不兼容空值返回`V7_FORMAL_ROLLBACK_LEGACY_NULLS`，结构和记录均保留 |
| 新检查入口须避免默认误连并明确证据边界 | 新增显式目标只读报告和不连接库的prepare模式；不读取私有配置、凭据摘要或会话正文。真实MySQL检验中修正事务控制语句的驱动调用方式 |

本轮没有新增业务表、变更迁移键、状态枚举、流程格式、业务路由或审批职责。M2维护报告增加完整性、一致性与使用统计，现有`applied`保持登记含义。历史V3全文、标识、版本和审核事件不迁写，旧审核缺少的修订/摘要绑定仍为空。会话队列仍最多32，原连接上限及超时保持；admin叠加业务角色仍只读。身份、会话、旧入口、审核、稳定引用和后续治理的逐项影响已同步发布恢复及迁移手册。

### 12.3 验证结果

最终使用Node v25.2.1、MySQL 8.4.9。实例均本轮新建，采用UUID标签、tmpfs和随机回环端口，无已有卷或目录挂载；应用运行账号仅有DML权限。先恢复合成旧V3备份并证明一致，再执行迁移和HTTP，发布后又恢复到第三实例核对。正常完成及可捕获失败后均清理自有容器，失败和最终证据保留。

| 检查 | 实际结果与边界 |
|---|---|
| M1迁移 | 首次/重复应用通过；真实建表后中断形成部分结构，M1及M2均停止；漂移、无登记完整结构、登记缺表及非空回退拒绝。中断是在已提交DDL后注入异常，不是主机断电演练 |
| M2迁移 | 兼容部分DDL及全部DDL完成但尚未登记的中断可显式续接；已登记缺索引、默认值/索引漂移、未登记业务使用拒绝；匹配重复应用零变更 |
| 会话迁移 | 首次、中断后空表续接、重复应用通过；索引漂移、非空无登记及非空回退拒绝。没有读取正式会话或改变第05阶段队列 |
| 合成旧V3保留 | 原主档6101、草稿6201、版本6301、审核6401、事件6501及人员/部门的迁移前后摘要一致；审核新增绑定仍为空；HTTP完整读回原V3。上述ID仅为合成夹具 |
| 迁移前备份恢复 | 99张合成表实际结构、行数和逐行摘要一致；备份194298字节，准确临时源、时间和SHA-256记入证据。比较含列、字符集/排序规则、索引、外键、约束和实时自增位置 |
| 迁移后真实HTTP | 8组通过：真实登录/首改密、预览隔离与重复上传、权限/CSRF、双方核对和提升、提交/退回/修订再办、过期绑定拒绝、发布故障回滚/并发一成一拒、固定版本完整读回及账号撤权。提升、提交、审核、发布和读回均走公开HTTP与真实会话 |
| 非空重复与回退 | 有V3/V7、预览、审核及会话后，重复M1/M2/会话迁移不重建或改变记录，破坏性回退零变更拒绝；未先清空数据再伪称回退成功 |
| 发布后再次恢复 | 105张表恢复到第三个独立MySQL，结构、数量和逐行摘要一致；只证明本轮合成V3/V7、预览、审核、事件和会话的逻辑恢复 |
| 可执行报告 | 查询在真实MySQL只读一致性事务执行，无缺表/缺列，13项扩展引用检查为0，原生内容和预览摘要匹配。正式目标报告仍待填，不授权DDL，不把本地源码当部署版本 |
| 后续数据治理 | 合成环境为未应用，未创建历史工作包；因D00范围待定，本轮不实施或开启可选治理迁移 |

独立迁移/恢复共15组通过，加上复用HTTP场景8组。定向静态/替身检查6项通过：第06阶段目标门禁、M0、M1、M2、会话运行及V3迁移合同。容器助手和HTTP场景变更后，第04阶段独立真实MySQL入口另行复验8组通过，日志写入本阶段。未重跑未受影响的第05阶段浏览器或全仓测试。

初期合成恢复比较出现SHOW文本差异和缓存自增值差异，已改为实际元数据、结构语义和SHOW实时自增值比较，未忽略数据差异。合成夹具缺少必填字段及报告事务控制调用问题均修正后复验；这些测试准备问题与M2实际维护缺陷分别记录，不混写为正式故障。

最终逐项结果及逐表摘要见[真实合成迁移恢复结果](../../../artifacts/mdm-3000-launch/stage06-1789033109176/test-results.json)，查询执行结果见[合成只读报告](../../../artifacts/mdm-3000-launch/stage06-1789033109176/synthetic-inspection-report.json)。[目标检查SQL](../../../artifacts/mdm-3000-launch/stage06-20260910/formal-preparation/inspection-plan.sql)和[正式报告模板](../../../artifacts/mdm-3000-launch/stage06-20260910/formal-preparation/inspection-report.template.json)已生成，真实目标及结果待确认。[第06阶段最终证据](../../../artifacts/mdm-3000-launch/stage06-20260910/validation-evidence.json)统一记录源码、运行摘要、基线保护、复用关系、失败及纠正、定向检查和清理。

### 12.4 停止点与未完成项

第06阶段全部已授权本地修复、测试、查询准备、维护及上线说明已完成，并在此停止。正式目标读取、真实旧数据备份及恢复未完成，不能写成完整旧数据恢复已验收。待批准的准确目标、动作、影响和恢复条件集中列在[发布与恢复10.5](03-发布与恢复.md#105-仍需单独批准的具体动作)；未知目标保持待确认，不重复索要已有合成授权。

D00-01至D00-03、主链地图、三类待办、OFF-01至OFF-10、正式拓扑及运行版本、实际人员、首批材料、正式容量和开机恢复、人工中文输入法与业务验收继续保留。第03阶段合成基础/会话、第04阶段合成事务、第05阶段待办/Edge按未受影响范围复用，均不覆盖真实历史、实际迁移状态、受控备份及正式恢复风险。未进入第07阶段，未开启真实试点，未暂存、提交或推送。

## 13. 第07阶段发布准备与证据（2026-09-11）

### 13.1 范围、决定与发布结论

本轮只执行第07阶段必要的本地发布准备、查询/报告及维护文档。已读取根和3000主责AGENTS，涉及根固定配置及共享校验器前读取`scripts/AGENTS.md`，读取上线00至04、第06/05阶段最终证据并按准确路径追溯前阶段。正式文本使用technical-chinese-writer技能。提示词真源为扁平文件`C:/Users/charl/Downloads/Infomat_3000上线系列提示词_2026-09-09.md`，SHA-256与用户指定的`38C02D6C1C7BC6F37916BFEFE10132AC50473FC526BB608C2BF50865935003C6`一致。

用户本轮确认首发包含正式版本后的治理工作包，D00-01仅就该分支收口；正式数据地图、正式主数据清单及新增/修改/编码申请等原问项仍待定。实际流程、文件、部门和人员由用户明确回复“待确认”。用户允许自行设置部署设计，已形成Windows同机HTTPS代理、回环3000、独立运行/维护身份及3个工作日观察、结束关闭的材料；没有把设计当作真实主机或人员事实。

**发布建议仍为不能进入真实单流程试点。** B07-01至07在[发布与恢复11.1](03-发布与恢复.md#111-发布判断与本轮决定)逐项写明阻止的动作和解除证据。完整旧数据恢复、准确正式目标、实际人员、唯一输入、已有线下前提及工作包专项证据未完成；发布准备材料完成不等于环境已准备或可上线。

### 13.2 当前候选与证据复用

开始时HEAD仍为`dec8996c375d780edc0106a4c32584d2dd664944`、分支master，按展开未跟踪文件的口径记录140项Git状态及3197个受保护文件摘要。不能与以前按目录折叠的状态数量直接比较，更不能把新增文件自动归入本阶段。工作区保留前阶段、3001及其他任务成果，本轮没有Git写操作。

| 核对对象 | 本轮实际结果 | 适用结论 |
|---|---|---|
| 第06阶段最终记录 | 23/23个记录文件摘要匹配；其最终证据JSON原样保留 | M2修复、迁移/恢复15组、HTTP8组及定向6项按原边界有效，不重新执行未受影响整套测试 |
| 第06阶段运行与当前源码 | `sourceDigest=fd41148dd2e67f5f93926254b6b37dbc3f9c1c9c65e14fd9b4f314a7c64d8492`相同 | 同一候选应用源码；不代表正式部署版本或外层共享资产已随运行验证 |
| 第05阶段 | 30项中25项匹配；5项差异为第06阶段已记录的package入口及4份说明。19份结果/日志/截图摘要均匹配 | F07/F08、32等待队列及admin叠加业务角色只读保持；V7 Edge证据复用，不称为本轮重新跑浏览器 |
| 第02至04阶段 | 按准确路径核对最终证据与当前差异，后续改动有归属 | 老摘要不一致不等于成果丢失；只沿用与当前实现相符的范围 |
| 当前完整候选清单 | 112项文件，逐项路径/字节/SHA-256已登记；组合摘要`9fa92c499cbdae9ecb43ef9ce218d4bc3923eecd416f7eb75c6d33819d7dd3b6` | 盘点范围含共享契约/校验器及维护源码，不是批准程序包；不复制整个脏工作区 |
| 锁定与本地依赖 | lockfileVersion3，package/lock根依赖一致；lock与HEAD相同；9项生产直接依赖与lock匹配，npm ls退出0 | 未安装或升级；目标Node、干净安装、传递依赖完整性和正式包仍待冻结 |
| 本机3000 | 本轮只读监听检查未发现监听 | 未启动/重启，未读取正式数据库或把候选运行摘要填作正式health证据 |

运行摘要只覆盖应用内特定后缀文件及package/lock；共享V7校验器、外层schema、PNG/XLSX、维护命令及固定目标配置须另按完整清单核对。共享校验器和4份运行schema另与第06阶段文件基线比较，5项均一致。已明确独立发布需保持相对目录层级，并补齐实际来源链接所需的获准资产；不以全量复制组织/制度/历史材料解决资源闭合。

### 13.3 实际交付

1. 在03第11节形成发布判断、候选版本/完整摘要、部署设计、唯一流程及任命核对、身份字段、迁移必要性、作业步骤L0/R1至R11及C1至C7、失败停止/恢复责任、验收、观察和未发送通知正文。
2. 根据用户确认补充“关口A正式流程版本→关口B固定版本工作包”。准确版本产生前不猜ID、不打开工作包；六表确需迁移时，恢复点须覆盖新增版本及审核/事件。工作包启用期间关闭V7写入，结束关闭三个开关并清空两个引用。
3. 在新[stage07证据目录](../../../artifacts/mdm-3000-launch/stage07-20260911/)生成已填写已知事实的发布记录、112项候选源码清单、依赖状态、只读查询和报告模板。具名身份SQL仅按批准person_id核对必要字段，空目标不能报告通过；Secret/会话正文不纳入查询。
4. 同步00/01的最新范围决定、04第0节当前停点、应用scripts README和工作包迁移手册。未修改应用实现、权限、schema、3001或其他服务；本阶段无新的历史数据迁移。

发现的命令使用边界已写实：`service:*`以根固定JSON覆盖MySQL目标；M1/M2/工作包CLI会加载私有env且没有显式target保障。正式操作材料要求批准的新布局、冻结配置、私有env不存在及安全注入，不能靠追加`--target`或继承变量假定准确。旧init和默认备份/演练入口不执行。

### 13.4 本轮验证

| 检查 | 实际结果及证据类型 | 保留限制 |
|---|---|---|
| 本地准备生成 | `prepare:launch-stage06`成功写入新stage07/formal-preparation目录，返回`STAGE06_PREPARED_NO_DATABASE_CONNECTION` | 复用旧生成器，模板保留第06阶段检查字段；未执行inspect或连接任何数据库 |
| 新首发范围定向检查 | 工作包规则、API、迁移合同、前端4项及stage06目标/prepare门禁1项全部退出0 | 子进程净化环境并阻断真实MySQL；API使用注入仓储HTTP，前端为静态；不称为真实工作包全链验证 |
| 原工作包证据 | 第04阶段4个脚本和对应日志摘要与当前/原记录相符 | 复用原结果边界，不升级为真实MySQL或真实浏览器 |
| 候选与材料静态核对 | 112项候选摘要匹配；身份SQL列名对应当前本地schema且无持久写语句/凭据字段；HTTPS设计被现有配置校验函数接受；三个开关默认关、异范围拒绝 | 使用合成origin和隔离配置对象；不验证真实域名/证书、正式SQL或实际私有开关 |
| 文档及保护 | 本地链接、命令入口、diff与限定文件保护结果写入最终证据 | 第02至06阶段证据不覆盖；其他对话新增文件单列，不归入本阶段 |

准确结果见[定向检查](../../../artifacts/mdm-3000-launch/stage07-20260911/targeted-checks.json)、[材料静态检查](../../../artifacts/mdm-3000-launch/stage07-20260911/material-static-checks.json)和[第07阶段最终证据](../../../artifacts/mdm-3000-launch/stage07-20260911/validation-evidence.json)。本轮没有新建MySQL容器、运行浏览器或执行实际部署；未重复前阶段已有效的完整测试。

### 13.5 停止点和正式授权缺口

本轮全部已授权本地准备已完成，停止，不进入第08阶段。正式/共享数据库未连接，真实旧数据备份与恢复未完成，真实人员账号未调整，正式服务和开关未操作，真实试点及通知/发布未执行，未暂存、提交或推送。

正式读取、备份与恢复继续使用03第10.5的同一批准清单。目标、UUID、窗口、执行身份、受控位置、独立恢复目标和具名恢复责任仍待确认；10.5不包含后续DDL、账号、启停或发布。实际差异确定后才按03第11.7提出具体变更单，不反复索要已经存在的合成隔离授权。

实际主链地图、三类待办、OFF-01至OFF-10、人工中文输入法、正式拓扑/容量/开机恢复及业务验收继续保留。新增工作包范围的真实MySQL、浏览器和包含六表的恢复证据仍须在启用前完成，不能以第06阶段合成旧V3/V7结果代替。

## 14. 后续人员真源与本机Docker方向补充（2026-09-11）

第07阶段停止后，用户另行要求转换行政人事部最新发布花名册、调整人员真源，明确公司领导层、部门、办公室/班组三级管理，以及后续按流程设置虚拟办公室或班组；并指定数据库使用本机Docker部署的MySQL。本次完成本地资料更新和受影响文件检查，不进入第08阶段。

[花名册](../../organization/花名册.md)已按源Excel转换297人（职员96人、工人201人），保留全部10个源字段、表页及行号；员工编号完整且唯一，办公室或班组缺失124人保持空白。旧版240人完整留存，本次未收录82个旧编号不据此判断离职或停用。项目映射86条全部保留，仅同步来源属性、匹配状态并保留历史工号，项目任命及职责依据不变。组织、人员真源边界与历史办公室表述已同步，未导入MySQL或调整真实账号、人员、角色。

本机Docker上下文、现有容器身份和3307→3306映射已只读核对。最新观察显示容器运行，本轮未执行启停；没有读取容器环境变量或Secret，没有连接数据库，实际库名及server UUID仍未核对。准确对象见[03第12节](03-发布与恢复.md#12-第07阶段停止后的来源与数据库方向补充2026-09-11)，正式数据读取、备份、恢复继续沿用03第10.5的授权缺口。

本轮生成工作角色受控快照（仍为0角色、0岗位映射、0别名）并运行工作角色、九部门域映射、项目人员快照和Codex文档路由检查。项目人员合同第一次因旧岗位字面值失败，已随新来源修正测试预期并复验；未修改运行生成器或应用代码。逐字段转换、历史副本、源文件未改写及既有成果保护结果见[独立验证证据](../../../artifacts/organization/roster-20260911/validation-evidence.json)，完整说明见[花名册更新记录](../../organization/花名册更新说明_2026-09-11.md)。第02至07阶段原证据保持原样，发布阻断、D00剩余决定及线下验收前提继续保留。

## 15. 阻断项访谈更新（2026-09-11）

Q11用户明确“先不设置备份”。已将备份位置、任务及实际备份/恢复安排标为暂缓，停止本分支追问；未执行任何备份设置、定时任务或数据库动作。此前结构查询和Q10责任安排保留，备份及适用恢复要求仍属后续上线前待落实事项，不记为通过。

Q10用户答复“是的”，确认正式运行后的运维和备份恢复由其作为责任对接人统一协调，具体执行人员上线前落实。已同步责任及交接记录，实际备份位置与恢复目标继续待定；没有据此执行备份、恢复、授权调整或正式启用，也没有变更Q09已获准的查询范围。

**Q09直接查询及离线核对已完成：** 用户新增sa并指定私有env文件供本机程序认证，Codex据此使用单连接、固定回环地址、容器/UUID校验及只读事务执行六条批准SELECT，没有读取业务行。UTC时间2026-09-11 05:36:52.080021，结果131表、1644项字段、673项索引列、402项键约束列及26条迁移登记，表名与先前用户回传清单完全相同。M1/M2有登记，4张预览表及提升表、正式表必要新增字段/索引在已查范围内未发现差异；会话表和工作包6表及各自迁移登记均不存在。基础DDL比较得到25项差异：3个缺列、5个缺索引、13个字段属性差异及4个外键目标未匹配；不把这些全部判为代码缺陷或自动修复对象。

一次性查询及离线比较脚本均放在Q09被忽略的材料目录，复用现有解析函数和mysql2，没有新增依赖或修改应用。口令只在本机进程中读取使用，未输出、写入报告或修改私有配置；没有将应用运行账号换为sa。原始报告、逐项差异和验证在[独立直接核验目录](../../../artifacts/mdm-3000-launch/interview-20260911-q09-preparation/results/automated-1789105011596/schema-findings.md)。语法检查、目标和六组结果、与前次表名一致性及报告读回通过。检查未覆盖默认值、生成表达式、CHECK执行、索引方向、外键动作及业务数据；不能据此宣称完整DDL、M0数据一致性或恢复通过。此前认证受限和用户DataGrip回传记录按各自时点保留。

**Q09前两项结果已取得：** 用户回传目标检查PASS（2026-09-11 05:17:30.565591 UTC）及131张表的附件。CSV四列与查询一致、表名无重复，全部报告为InnoDB基础表及utf8mb4_unicode_ci；按当前本地DDL静态比对，99张基础表、4张V7预览表、1张提升记录表名称均在清单中，`mdm_http_sessions`及6张治理工作包表未见。只核对表名，未将M1/M2判为已应用，也未读取业务行或迁移登记。已有结构与用户所述无业务内容可同时成立，不能将现库写成已验证无表。附件SHA-256为`ec9a9deabca8cf2e3b66385da4eb9c3b805f8fe9939d61fa4922443159d6fc11`，原附件未修改；[解析及对比记录](../../../artifacts/mdm-3000-launch/interview-20260911-q09-preparation/results/user-provided-tables.json)保留来源、全部表项及本地DDL摘要。这是用户实际库的可见清单，与第06阶段99/105张合成表恢复不是同一组证据。剩余列、索引、外键及迁移登记已获许可，待DataGrip结果，不增加审批问题。

**Q09已获授权：** 用户答复“同意，大胆一点，现在里面什么都没有，正式上线前我还会删库重建。”本次结构、索引、外键与迁移登记只读许可已取得；现库没有内容及上线前重建作为用户说明记录，尚未转为已验证空库或已执行重建。已调整操作材料：目标检查PASS且表清单为空时直接停止后续结构查询，避免对不存在的迁移表执行查询。后续准备考虑全新建库，原恢复能力证据保留且不写成正式旧数据恢复通过。当前仍等待用户DataGrip回传结果，未执行清空、初始化或任何真实试点。

**Q08用户已回传查询结果：** UUID`f606b83c-66c4-11f1-b7b2-ca0417131dd4`、服务器主机名`094fad79ea18`、内部端口3306、版本8.4.9、查询时间`2026-09-11 05:09:39.627502 UTC`；可见库名`infomat_mdm`。字段按此前提供的两条SQL映射，主机名与容器ID前缀、库名与DataGrip配置相符。原回复的Markdown下划线转义已保留原文并规范为库名，不改写源结果。证据类型为用户在DataGrip执行并提供，Codex未直接连接数据库；仅收口实例及可见库名。此前未连接记录按各自时点保留，结构、迁移、真实数据恢复和发布仍未通过。记录见[用户回传结果](../../../artifacts/mdm-3000-launch/interview-20260911-q08-preparation/user-provided-instance-identity.json)。

Q08后用户提供认证入口线索“datagrip里有”。本次仅核对DataGrip保存的目标元数据，找到`Infomat MDM Local`连接，配置为`localhost:3307/infomat_mdm`，未读取密码库或认证值。工具不支持操作DataGrip原生控制台或复用已登录会话，数据库查询仍未执行；准备由用户在该连接运行已批准SQL并回传结果字段。此时库名只得到配置层面的对应，不能写成真实数据库核验通过。

Q08用户已明确授权最小实例及可见库名读取。本次重新核对现有容器完整ID、Docker上下文、运行状态和3307→3306映射一致；执行进程未注入连接身份，依约停止，数据库连接尝试数与SQL执行数均为0，未读取私有配置或容器环境变量。该项许可保留，下一步缺的是安全认证通道。准确范围和[检查记录](../../../artifacts/mdm-3000-launch/interview-20260911-q08-preparation/instance-identity-result.json)见03第13节Q08；正式业务库完整读取、备份、恢复等原有缺口不变。

Q07用户答复“同意”，确认亲自担任本次3000试点治理的统一对接人，负责协调材料和部门参与；具体办理、审核和发布人员在实际启用前逐步落实。00/01、03及04已同步，不推定用户的person_id或授予系统角色。为准备下一项目标核验，仅检查现有容器指定元数据及stage06代码：容器`094fad79ea18`仍为running、3307→3306；既有inspect会读取业务记录，不用于仅确认实例及库名的窄范围检查。最小SQL另行准备，尚未连接MySQL。

**最新访谈口径见Q06：** 用户认为经营发起结算/付款申请、财务承接核对与付款的建议“基本符合”，同时要求不急于精确确定流程边界，具体内容在3000正式治理。以下Q03至Q05保留原答复，作为治理时需核对的访谈输入，不视为正式流程已经定稿。当前不继续追问全部交接、凭据和审批细节，也不把这些细节全部关闭作为本次准备访谈的完成条件。此轮仅同步记录，未修改原JSON、应用、人员授权或任何前阶段证据。

grill-me第1问已取得用户明确答复“是”：本次首发交付终点为流程正式版本发布及其治理工作包完成，正式数据地图、正式主数据清单发布及主数据新增/修改/编码申请留后续批次。D00-01范围决定收口，00/01及04已同步；准确问答和影响记录在[03第13节](03-发布与恢复.md#13-阻断项访谈决定2026-09-11)。这是业务范围决定，不是应用测试、数据恢复或正式执行批准，不关闭其他B07门槛。

准备下一问时只清点既有第11周交付目录元数据，发现50份JSON，未读取正文或认定为50个有效流程。该新观察不覆盖9月7日的历史记录。访谈本轮仅修改上线说明，未修改应用、前阶段证据、数据库或运行状态。

Q02后用户答复“同意”，选定经营发展部《采购到货验收及结算流程》作为唯一候选。随后仅对该文件完成正文读取、SHA-256前后核对及当前3000纯函数结构校验，结果通过；使用组织真源名称和合成临时数字ID投影得到3条物资保障部核对项，不使用或推定正式库部门ID。该文件实际8个节点、5个业务动作、10条关系，流程引用为`process_18adc844f996805d`。付款动作未体现财务范围、分类验收并行关系、质量会签与投影覆盖及空输入输出说明仍待业务核对。准确路径、摘要、发现和问答见[03第13节Q02](03-发布与恢复.md#q02唯一试点候选已同意进入核对)，证据另存[interview-20260911-q02](../../../artifacts/mdm-3000-launch/interview-20260911-q02/candidate-inspection.json)，未覆盖任何前阶段目录。候选选定和结构通过均不构成正式发布或业务验收。

Q03用户答复“是的”，确认财务部纳入该流程的参与及业务核对范围。已同步00/01、03访谈决定和04当前交接；经营与财务的具体交接、岗位、审批权限和人员仍待确认。此次只记录范围决定，没有修改原JSON、Q02证据、应用或运行状态，不将财务参与等同于已任命审核员或已完成付款职责核对。下一项访谈转入分类验收分支规则。

Q04用户答复“同意”：按本次到货实际类别启用相应验收分支，全部适用分支完成后汇总结论；完成验收不等于验收通过。已同步00/01、03和04，后续材料修订及单类别、多类别、含不合格结果的验证仍待执行。本轮仅记录业务规则，未改写原JSON、应用或任何前阶段证据。下一问转入质量管理部在三类验收中的职责和意见留存。

Q05用户答复“是”：A、B/C、D三类实际到货验收均由质量管理部独立确认质量核验结果，并与物资保障部的数量、外观核对结果分别留存。该决定明确了核对要求；D类会签表达、质量核对项覆盖和三类业务证据仍待落实并验证。没有据此修改原JSON或任命实际审核人员。

C01用户随后明确“所有的docs下文件都不再是真源”“3000平台就是要先治理流程，后生成程序文件”，并提醒保留3001与3000的关系。已修正仓库、组织、制度和合同目录的维护入口，以及本目录当前记录：3001独立编制、用户主动下载V7并手动上传3000，3000承接治理、核对、审核与正式版本；退回时回3001修订，再上传原案例，程序文件由治理结果派生。此前准备引用的旧财务程序及其审批链撤回；Q02中使用旧组织、花名册副本得到的名称匹配只保留为历史技术检查，不再证明当前部门、人员或职责。

本次保留花名册明细、旧工作角色快照和原始证据，不运行从docs生成组织或角色数据的入口。现有代码仍可能消费docs中的兼容资产；本次没有完成运行数据源切换，也没有验证程序文件生成能力。核对记录单独存入[来源边界补充证据](../../../artifacts/mdm-3000-launch/interview-20260911-source-boundary/validation-evidence.json)，发布建议仍为阻断，03第10.5及全部未完成恢复、人员、环境和业务验收条件保留。
