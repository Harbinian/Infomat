# 验证记录：3001 未应用修改与误导性交互整改

> 日期：2026-08-26  
> 当前状态：自动化技术验证通过；真实Windows中文输入法候选窗和实际业务编制仍待人工验收  
> 记录规则：只记录实际执行的命令、页面操作和结果；自动测试不替代真实流程业务验收

## 1. 实施基线

| 项目 | 实际值 | 状态 |
|---|---|---|
| Git HEAD | `976d16a15fbe083ad6d6dcc75e24ebe3d772e410` | 已记录 |
| `public/index.html`实施前SHA-256 | `B59800D9BE4944660B3E17D576AB7043AA9DEA9120905CDA9520D169AED160D6` | 已记录 |
| `public/index.html`实施后SHA-256 | `BDDFE7B474EE5C0360515AAAC12CF3038A9B1AE8A2496FEE8D70938B7F484984` | 已记录 |
| `public/edit-session-manager.js` SHA-256 | `3D781A31997BA91E6D63F164707C2278B5ABC96A87B631E17D4792F8BE04BC9C` | 已记录 |
| `public/graph-edit-commands.js` SHA-256 | `B131CD7BD7932BFDED825D499EE133B336EC289AB9BF209F3AF12BD7E84746EA` | 已记录 |
| `public/graph-editor-state.js` SHA-256 | `D91CB5E84FEABBDD139A2E8C042949EA6E32BBA97015BDE4EC3A0F12F3351BEC` | 已记录 |
| 工作区差异 | 实施前`git status --porcelain`为61项；本记录形成时为79项。工作区同时存在其他任务的并行修改，数量变化不能全部归因于本整改 | 已保留；未执行`reset`、`clean`、暂存或提交 |
| 独立候选实例 | `http://127.0.0.1:33001`；启动提交同Git HEAD；验证结束后已关闭。既有3001未被启动、停止或重绑，关闭候选后仍返回`status=ok`、v7、released | 已验证 |

## 2. 自动检查

| 检查 | 实际结果 | 证据位置 | 状态 |
|---|---|---|---|
| 编辑会话控制器单元测试 | 创建、修改、应用、放弃、重置、候选隔离、字段冲突和非本面板字段保留通过 | `scripts/test-edit-session-manager.js` | 通过 |
| `npm.cmd test` | 12组命令全部通过，包括治理流程、评分、服务约定、DOCX安全与解析池、导入兼容、v1至v6版本夹具、图编辑、v7生命周期、字段复用、网页表格和编辑会话 | `apps/structured-output-service/package.json`中的`test`入口 | 通过 |
| `npm.cmd audit --json` | 137项依赖；`info`、`low`、`moderate`、`high`、`critical`均为0 | npm审计输出 | 通过 |
| `git diff --check` | 退出码0；只有工作区既有LF/CRLF提示，没有空白错误 | Git检查输出 | 通过 |
| JavaScript语法检查 | 编辑会话、图命令和三组浏览器场景脚本均通过`node --check` | 对应脚本 | 通过 |
| 根页和静态资源 | 根页HTTP 200；`edit-session-manager.js` HTTP 200，类型为JavaScript | 独立候选实例33001 | 通过 |
| `/api/health` | `status=ok`、`schema_version=process-governance-v7`、`release_status=released`、`app_commit=976d16a...`、`schema_digest=e1d5b33b...` | 独立候选实例33001 | 通过 |

## 3. 数据往返与失败恢复

| 场景 | 实际结果 | 状态 |
|---|---|---|
| 当前v7应用后下载并重新导入 | 数据对象名称、说明和信息类型先应用再下载；重新导入后与下载内容一致 | 通过 |
| 当前v7放弃后下载并重新导入 | 未应用输入在下载前明确放弃；文件和重新导入结果均不含被放弃内容 | 通过 |
| 阶段下载与最终下载 | 两种入口均先处理未应用修改；最终下载仍执行既有业务提示确认，放弃的输入未进入文件 | 通过 |
| 上一受支持版本导入当前版本 | v6合法夹具导入v7通过；同一测试同时覆盖v1至v5 | 通过 |
| 重复执行兼容处理 | v1至v6迁移重复执行结果稳定，源对象保持不变；当前v7下载后可重新导入 | 通过 |
| 导入失败后保持当前状态 | 导入损坏JSON后，当前候选、当前JSON、未应用输入和页面选择保持不变 | 通过 |

## 4. 专项浏览器交互

浏览器环境为Windows上的Playwright托管Headless Chrome 151，`navigator.language=zh-CN`。三组脚本均连接独立候选端口，不启动或代管服务。

| 场景 | 实际结果 | 状态 |
|---|---|---|
| 数据对象未应用修改与生命周期往返 | 继续编辑保留名称、说明和类型；应用进入JSON；放弃恢复已应用值；“返回数据流编辑对象信息”返回同一对象 | 通过 |
| 数据对象属性可编辑性 | 可编辑控件存在有效编辑会话，“应用修改”不再静默无效 | 通过 |
| 顶部和侧栏候选切换 | 两个入口均触发同一三项保护；选择继续编辑后候选和值恢复 | 通过 |
| 业务行为、流程关系和数据关系 | 行为和关系属性只应用允许字段；动作说明、完成标准、会签和关系条件保留；数据操作及更新字段作为一次显式应用写入 | 通过 |
| 不完整流程关系向导 | 三项保护可见，“应用修改并继续”禁用；继续编辑保留阶段，放弃后完成导航 | 通过 |
| 更新字段弹窗 | 取消、背景或切换前先处理未应用选择；三项保护层级高于原弹窗；继续编辑保留勾选 | 通过 |
| 网页表格和对象字段引用 | 表格工作副本仍需显式应用；业务式和表格式引用对象字段后，`value_usage_mode`和`value_origin_mode`均保持待确认 | 通过 |
| 新建、导入和清空顺序 | 清空先处理未应用修改，再显示未下载修改确认；只有第二层明确放弃后才清空。导入失败保持原状态 | 通过 |
| 撤销来源变化保护 | 图操作后再修改普通即时字段，撤销返回来源变化提示；当前JSON和历史记录未被旧快照覆盖 | 通过 |
| 生命周期纯读取 | 切换视图、选择对象、展开主数据提示和返回数据流前后，JSON、未下载状态和撤销状态一致 | 通过 |
| 生命周期显式分析 | 首次有变化时形成一次事务；重复分析无变化时不增加撤销记录；一次撤销恢复分析前内容 | 通过 |
| 删除安全 | 12类删除分别验证取消不改数据、确认后一次删除和一次撤销完整恢复；被引用对象字段阻断且无级联 | 通过 |
| 生命周期删除引用 | 生命周期路径阻止删除其流程关系；生命周期事件阻止删除其触发行为 | 通过 |
| 三档内容可视区 | 1920×1080、1536×864、1280×720均执行三项保护，确认框完整可见，继续编辑保留输入，页面级横向溢出不超过1像素 | 通过 |
| 控制台和页面异常 | 交互脚本记录的`console warning/error`与`pageerror`均为0 | 通过 |

三组浏览器脚本的实际检查数如下：

- `pending-edit-browser-scenario.js`：15项，包括原问题、阶段与最终下载、导入失败、离页、生命周期、两类候选入口、三档内容可视区和无状态边界；
- `flow-pending-browser-scenario.js`：13项，包括行为、关系、向导、数据操作、更新字段、字段补丁、撤销来源变化和清空两级保护；
- `deletion-safety-browser-scenario.js`：16项，包括12类删除、对象字段引用阻断、生命周期引用阻断，以及业务式和表格式字段引用不推断业务事实。

## 5. 输入法与焦点

| 检查 | 实际结果 | 状态 |
|---|---|---|
| 组合输入事件和回车判定 | 单元测试覆盖`isComposing`、`keyCode=229`、`compositionend`后保护时间窗和随后一次明确回车导航 | 自动测试通过 |
| 候选切换焦点和值恢复 | 顶部候选和侧栏候选选择继续编辑后，原候选和输入值恢复 | 浏览器测试通过 |
| 更新字段弹窗焦点上下文 | 继续编辑后原弹窗和勾选保持，三项保护未被遮挡 | 浏览器测试通过 |
| 真实Windows中文输入法候选窗 | 本轮使用无头浏览器，无法显示和人工观察操作系统输入法候选窗 | 待人工验收 |
| 全矩阵键盘焦点和滚动位置 | 自动化覆盖主要候选、数据对象和更新字段路径，未逐项人工检查全部切换矩阵 | 待人工抽样验收 |

## 6. 无状态和网络边界

| 检查 | 实际结果 | 状态 |
|---|---|---|
| `localStorage` | 0项 | 通过 |
| `sessionStorage` | 0项 | 通过 |
| IndexedDB | 0个数据库 | 通过 |
| Cookie | 空 | 通过 |
| 3000、AI或第三方请求 | 浏览器记录中没有3001源站以外的请求 | 通过 |
| 服务端草稿和数据库 | 本整改没有新增API、数据库、服务端草稿或业务内容持久化代码；候选实例健康约定未变化 | 源码与接口边界检查通过 |

## 7. 结论与剩余边界

本整改已经达到自动化技术验证门槛：未应用修改、未下载修改和文件基线已分开；显式应用编辑器、切换、下载、生命周期和删除使用一致保护；字段补丁与撤销来源变化保护避免旧快照覆盖新内容；3001的单流程无状态边界保持不变。

仍未完成两项人工验收：真实Windows中文输入法候选窗，以及业务人员对全部切换矩阵的键盘焦点和滚动位置抽样。它们不影响已取得的自动化技术证据，但在正式面向编制人宣布交互验收完成前仍需执行。软件技术验证不代表具体流程事实已经通过业务验收。
