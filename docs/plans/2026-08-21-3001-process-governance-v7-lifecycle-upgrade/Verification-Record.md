# 3001 v7对象字段复用与数据生命周期候选验证记录

> 日期：2026-08-21  
> 对象：`codex/3001-v7`隔离候选  
> 结论：实现、自动化和真实浏览器技术验证通过；尚未替换3001正式服务，尚未完成真实脱敏流程业务验收

## 1. 自动化验证

在`apps/structured-output-service/`执行`npm.cmd test`，以下检查全部通过：

- 七步治理工作流；
- 结构化学习评分及第六评审方面；
- 服务接口、v7结构规则和静态资产；
- 图编辑、撤销重做、候选隔离和性能回归；
- v1至v6导入v7、v7往返、重复迁移和失败恢复；
- 同一对象内同名同类型字段归并、同名不同类型字段分离和源文件不变；
- 早期不含对象字段的v7候选补齐字段引用，并保留既有生命周期；
- 表单跨对象字段引用、断裂引用和字段类型不一致被拒绝；
- 首个字段引用为权威录入点，后续引用自动沿用已有值；
- 九类生命周期动作及动作结果状态；
- 高风险事件不能由系统自动确认；
- 人工确认、否决和不适用决定在重新分析后保留；
- 来源变化后，已确认事件进入“需要重新核对”；
- 同一行为同一动作的多字段线索合并为一个候选。

`git diff --check`通过。终端只显示Git的LF转CRLF工作区提示，不属于差异错误。

## 2. 接口验证

隔离候选在`127.0.0.1:3101`运行：

- 根页面可访问；
- `/api/health`返回`ok`和`process-governance-v7`；
- `/api/template`返回v7空白模板；
- `/api/schema`返回v7结构规则；
- v7结构规则包含`data_objects[].fields[]`、`data_field_ref`和`value_usage_mode`；
- `/api/validate`对浏览器验收样例返回`valid=true`和零项错误；
- v7分析器静态资产可以独立加载。

正式发布前必须从候选提交重新启动服务，再核对健康接口中的提交号。当前3101仅用于候选验证。

## 3. 真实浏览器验证

使用Playwright控制Chromium完成以下操作：

1. 导入v7财务样例“供应商状态维护流程”。
2. 在步骤5切换“数据流”和“生命周期”。
3. 系统按“供应商”对象集中形成“停用、失效、销毁”三个事件，未要求逐个业务行为定义数据。
4. 同一行为多个文本字段的重复线索已合并；修正前13项候选压缩为3项。
5. 销毁事件保持高风险和待确认，用户可补充单条记录、纸质原件和例外处理。
6. 用户否决停用建议并填写“语义不一致”及说明后，重新分析仍保留该决定。
7. 步骤7显示“数据生命周期与异常处理”第六评审方面，并明确v7暂不能直接导入3000。
8. 页面显示主数据认定提示，明确“所有主数据都是数据对象，但不是所有数据对象都是主数据；表单通常不是主数据”。
9. 在“表单、明细和字段”中先建立“供应商”数据对象及“供应商编码”对象字段，再建立“发票登记单”和“付款申请单”。
10. 两张表单均引用同一个对象字段，字段名称和类型从对象字段带入；对象字段明细显示2处引用。
11. “发票登记单”自动标为“本流程权威录入点”，“付款申请单”自动标为“沿用已有值”，后者不再要求重复登记取值来源。

本次对象字段复用回归的浏览器控制台为零项错误、零项警告。此前生命周期图形回归出现过既有Cytoscape静态库的自定义滚轮灵敏度提示，本次升级没有新增图形警告。

## 4. 三档视口

| 内容可视区 | 页面级横向溢出 | 七步导航 | 关键操作 |
|---|---|---|---|
| 1920×1080 | 无 | 七步完整可见 | 可见 |
| 1536×864 | 无 | 七步完整可见 | 可见 |
| 1280×720 | 无 | 七步完整可见 | 可见 |

三档视口均实际打开包含一个数据对象、一个对象字段和两张财务表单的“表单、明细和字段”页面。`documentElement.scrollWidth`与`clientWidth`一致；1280×720下七步导航和两个分区标题仍可访问，表格只在自身容器滚动。

## 5. 未完成门槛

- 尚未使用仓库外的真实脱敏顺序、分支、并行、循环、跨部门和期限触发流程完成业务验收。
- 尚未由真实编制人和真实核对人签署业务核对结论。
- 尚未把v7候选部署到正式3001端口；现有3001 v6服务未被替换。
- 3000尚未完成v7兼容、迁移审核和验证，v7文件不得直接导入3000。
