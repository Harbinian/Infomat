interface:
  display_name: "DCM Alias"
  short_description: "已合并到 process-evidence-mapping"
  brand_color: "#1A56DB"
  default_prompt: "Use $process-evidence-mapping instead. Treat this skill only as a compatibility alias for old DCM prompts."
policy:
  allow_implicit_invocation: true
