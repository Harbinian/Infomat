interface:
  display_name: "BBM Alias"
  short_description: "已合并到 process-evidence-mapping"
  brand_color: "#0F766E"
  default_prompt: "Use $process-evidence-mapping instead. Treat this skill only as a compatibility alias for old BBM/A1 prompts."
policy:
  allow_implicit_invocation: true
