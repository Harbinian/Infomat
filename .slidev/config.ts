import { defineConfig } from '@slidev/theme-default'

export default defineConfig({
  theme: 'default',
  transitions: 'slide',
  slideNo: true,
  readonly: false,
  highlight: 'shiki',
  info: true,
  drawings: true,
  plantuml: true,
  mermaid: true,
})
